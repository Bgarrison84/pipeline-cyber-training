---
phase: 05-simulated-powershell-terminal-exercise-view
plan: 04
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - src/views/module-view.js
  - src/quiz-engine.js
  - src/style.css
autonomous: true
requirements:
  - TERM-01
  - TERM-02
  - TERM-03
  - TERM-04

must_haves:
  truths:
    - "The module-view lesson list renders an exercise link button below ps-logging lesson (Start Exercise →)"
    - "After exercise completion, the exercise link in module-view shows 'Exercise complete — revisit →' in green"
    - "computeModuleProgress counts exercise completion toward module progress percentage"
    - "Sidebar progress bar updates after exercise completion (via refreshSidebarProgress)"
    - "The cursor blink keyframe animation is defined in style.css"
    - "All quiz-engine.test.js computeModuleProgress exercise branch tests are GREEN"
    - "Full Vitest suite is GREEN"
  artifacts:
    - path: "src/views/module-view.js"
      provides: "Exercise link button in lesson rows for lessons with exerciseId"
      contains: "exerciseId"
    - path: "src/quiz-engine.js"
      provides: "computeModuleProgress with exerciseId branch"
      contains: "exerciseId"
    - path: "src/style.css"
      provides: "terminal-cursor-blink keyframe"
      contains: "terminal-cursor-blink"
  key_links:
    - from: "src/views/module-view.js"
      to: "progressStore.getExerciseCompletion"
      via: "lesson.exerciseId check + progressStore call per lesson row"
      pattern: "getExerciseCompletion"
    - from: "src/quiz-engine.js computeModuleProgress"
      to: "progressStore.getExerciseCompletion"
      via: "else if (lesson.exerciseId) branch"
      pattern: "lesson.exerciseId"
---

<objective>
Wave 3 wires the exercise into the module-level UI: exercise link buttons in module-view, exercise completion counted in computeModuleProgress, and the cursor blink keyframe in style.css. After this wave the complete vertical slice is shipped: learner sees exercise link in module, completes exercise, progress bar updates, sidebar reflects completion.

Purpose: Without this wave, completing the exercise doesn't affect the sidebar or module view. This closes the loop on TERM-01 (exercise completion recorded) and makes the progress surface (SHELL-03 — already shipped in Phase 4) reflect exercise completion.

Output: 3 files modified; all quiz-engine.test.js computeModuleProgress exercise tests GREEN; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-CONTEXT.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-RESEARCH.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-PATTERNS.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-UI-SPEC.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-01-SUMMARY.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-02-SUMMARY.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-03-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs — no codebase exploration required. -->

From src/quiz-engine.js computeModuleProgress (lines 258–281 — current code):
  for (const lesson of mod.lessons) {
    denominator++;
    if (lesson.quizId) {
      const quizScore = progressStore.getQuizScore(mod.id, lesson.quizId);
      if (quizScore !== null) numerator++;
    } else {
      const progress = progressStore.getLessonProgress(mod.id, lesson.id);
      if (progress && progress.visited) numerator++;
    }
  }
Target change — insert else-if between quizId branch and else branch:
    } else if (lesson.exerciseId) {
      const ex = progressStore.getExerciseCompletion(mod.id, lesson.exerciseId);
      if (ex !== null) numerator++;
    } else {

JSDoc update on line 255 — extend @param type:
  @param {{ id: string, lessons: Array<{id: string, quizId?: string, exerciseId?: string}> }} mod

From src/views/module-view.js (current lessonRows map pattern — lines 42–48):
  const lessonRows = mod.lessons.map(lesson => `
    <div style="display:flex;align-items:center;justify-content:space-between;padding:...">
      <a href="#/lesson/${esc(mod.id)}/${esc(lesson.id)}"...>${esc(lesson.title)}</a>
      ${lessonStatusBadge(mod, lesson)}
    </div>
  `).join('');
Target: extend the lesson row div to include an exercise link below the lesson <a> when lesson.exerciseId exists.
  Compute exerciseDone before template:
    const exerciseDone = lesson.exerciseId
      ? progressStore.getExerciseCompletion(mod.id, lesson.exerciseId) !== null
      : false;

From src/progress-store.js:
  progressStore.getExerciseCompletion(moduleId, exerciseId): { completed, completedAt } | null

From tests/quiz-engine.test.js (exercise branch tests added in Wave 0 — currently RED):
  Test 1: getExerciseCompletion returns { completed: true, completedAt: '...' } → computeModuleProgress({ id, lessons: [{ id: 'ps-logging', exerciseId: '01' }] }) → pct === 100, complete === true
  Test 2: getExerciseCompletion returns null → same mod → pct === 0, complete === false

UI-SPEC C-07 (Exercise Link Button in module-view):
  Element: <a> tag (hash link)
  href: "#/exercise/{moduleId}/{exerciseId}" — esc() applied to moduleId and exerciseId
  Inline style: display inline-flex, align-items center, gap var(--spacing-xs), font-size var(--text-body), font-weight 600, text-decoration none, padding var(--spacing-xs) 0
  Not done: color var(--color-accent) = #f97316; icon data-lucide="terminal" 14x14; label "Start Exercise →"
  Done: color #22c55e; icon data-lucide="check-circle" 14x14; label "Exercise complete — revisit →"

UI-SPEC style.css addition (one keyframe only):
  @keyframes terminal-cursor-blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }
  Add after the Phase 2 block comment in style.css.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend computeModuleProgress with exerciseId branch + exercise link button in module-view + cursor keyframe in style.css</name>
  <files>src/quiz-engine.js, src/views/module-view.js, src/style.css</files>
  <read_first>
    - src/quiz-engine.js lines 250–282 — current computeModuleProgress function and JSDoc (edit the else-if branch into the existing loop)
    - src/views/module-view.js — full file (find lessonRows map; understand where to compute exerciseDone and inject exercise link)
    - src/style.css — find end of file or the Phase 2 block to locate insertion point for keyframe
    - tests/quiz-engine.test.js lines 270–end — find the computeModuleProgress describe block; the two exercise branch tests added in Wave 0 are RED here; confirm their exact mock usage pattern
    - .planning/phases/05-simulated-powershell-terminal-exercise-view/05-PATTERNS.md — sections "src/quiz-engine.js — computeModuleProgress" and "src/views/module-view.js" (exact code targets and diff shapes)
    - .planning/phases/05-simulated-powershell-terminal-exercise-view/05-UI-SPEC.md — section "C-07: Exercise Link Button" and "New CSS Additions to src/style.css"
  </read_first>
  <behavior>
    RED→GREEN targets (from tests/quiz-engine.test.js):
    - computeModuleProgress with lesson { id: 'ps-logging', exerciseId: '01' } and getExerciseCompletion returning non-null → { pct: 100, complete: true }
    - computeModuleProgress with same lesson and getExerciseCompletion returning null → { pct: 0, complete: false }
  </behavior>
  <action>
    Edit 1 — src/quiz-engine.js, computeModuleProgress:
    In the for (const lesson of mod.lessons) loop, between the closing brace of the if (lesson.quizId) block and the else block, insert the exercise branch:
      } else if (lesson.exerciseId) {
        // Exercise-backed lesson: counts as complete when exercise saved
        const ex = progressStore.getExerciseCompletion(mod.id, lesson.exerciseId);
        if (ex !== null) numerator++;
    Also update the JSDoc @param on the line before export function computeModuleProgress:
      @param {{ id: string, lessons: Array<{id: string, quizId?: string, exerciseId?: string}> }} mod
    No other changes to quiz-engine.js.

    Edit 2 — src/views/module-view.js:
    Inside the lessonRows map callback (for each lesson), before the template literal:
    a. Add: const exerciseDone = lesson.exerciseId ? progressStore.getExerciseCompletion(mod.id, lesson.exerciseId) !== null : false;
    b. Inside the lesson row div template, after the ${lessonStatusBadge(mod, lesson)} line, add the exercise link template:
       ${lesson.exerciseId ? `
         <a href="#/exercise/${esc(mod.id)}/${esc(lesson.exerciseId)}"
            style="display:inline-flex;align-items:center;gap:var(--spacing-xs);font-size:var(--text-body);font-weight:600;text-decoration:none;padding:var(--spacing-xs) 0;color:${exerciseDone ? '#22c55e' : 'var(--color-accent)'};">
           <i data-lucide="${exerciseDone ? 'check-circle' : 'terminal'}"
              style="width:14px;height:14px;color:${exerciseDone ? '#22c55e' : 'var(--color-accent)'};flex-shrink:0;"></i>
           ${exerciseDone ? 'Exercise complete — revisit →' : 'Start Exercise →'}
         </a>
       ` : ''}
    Note: use → (Unicode rightwards arrow) rather than → in the template literal to avoid encoding issues.
    Note: esc() is applied to mod.id and lesson.exerciseId in the href attribute per security requirement.
    Note: the lesson row div layout may need to change from display:flex (horizontal) to display:block or a two-row structure to accommodate the exercise link below the lesson title row. Inspect the current div structure in module-view.js and adjust — the exercise link should render below the lesson <a> + status badge row, not inline with it. Wrap the existing lesson link + badge in an inner div if needed.

    Edit 3 — src/style.css:
    Append the cursor blink keyframe at the end of the file (or after the Phase 2 CSS block if there is a comment marker):
      /* Phase 5: Terminal cursor blink */
      @keyframes terminal-cursor-blink {
        0%, 50% { opacity: 1; }
        51%, 100% { opacity: 0; }
      }
    This is the only CSS addition permitted in Phase 5 (UI-SPEC constraint). No new layout classes.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-engine.test.js tests/exercise-view.test.js tests/terminal-engine.test.js tests/router.test.js</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/quiz-engine.test.js exits 0 (all tests GREEN including two new exercise branch tests)
    - src/quiz-engine.js contains exerciseId branch: grep -c "lesson.exerciseId" src/quiz-engine.js equals 1 (in computeModuleProgress loop)
    - src/views/module-view.js contains exercise link: grep -c "exerciseId" src/views/module-view.js is >= 2 (exerciseDone variable + href template)
    - src/views/module-view.js exercise link uses esc() on moduleId and exerciseId in href: grep -c "esc(mod.id)" src/views/module-view.js is >= 2 (at least one in lesson link, one in exercise link)
    - src/style.css contains terminal-cursor-blink: grep -c "terminal-cursor-blink" src/style.css equals 2 (keyframe name appears twice: declaration + 0%/50% block)
    - npx vitest run exits 0 (full suite GREEN — all Waves 0–3 tests pass, all Phase 1–4 regression tests pass)
  </acceptance_criteria>
  <done>computeModuleProgress counts exercise completion; module-view shows exercise link; style.css has cursor blink keyframe; full suite GREEN</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All four plans of Phase 5 are now complete:
    - Wave 0: Test scaffolds (RED) + 01.json upgraded to 3-step schema + modules-config exerciseId field
    - Wave 1: src/terminal-engine.js — createTerminal factory, all terminal tests GREEN
    - Wave 2: src/views/exercise-view.js + router.js exercise route — all exercise tests GREEN
    - Wave 3: module-view exercise link buttons + computeModuleProgress exercise branch + style.css keyframe — full suite GREEN
  </what-built>
  <how-to-verify>
    1. Run full test suite: `npx vitest run` — confirm all tests GREEN, zero failures
    2. Run `npm run dev` and open the app in a browser
    3. Navigate to the Logging & Auditing module (#/module/logging-auditing)
       - Confirm: "Enabling PowerShell Script Block Logging" lesson row shows a "Start Exercise →" link in orange (var(--color-accent))
    4. Click "Start Exercise →"
       - Confirm: exercise view renders with "EXERCISE" category label, "Enable Script Block Logging" title, step panel "Step 1 of 3", and the simulator banner "PS SIMULATOR — commands do not run on any real system" in red text above the terminal
    5. Type in the terminal: Get-ItemProperty HKLM:\SOFTWARE\Policies\Microsoft\Windows\PowerShell\ScriptBlockLogging
       - Confirm: step advances to "Step 2 of 3", multi-line PS error output appears in terminal (realistic format with CategoryInfo and FullyQualifiedErrorId lines)
    6. Type something wrong: regedit
       - Confirm: hint text appears in the step panel (not in the terminal) with text containing "Get-ItemProperty" guidance
    7. Complete all 3 steps with correct commands
       - Confirm: completion banner "Exercise complete — well done." appears, terminal input is disabled (grayed out), sidebar progress bar for Logging & Auditing module updates
    8. Navigate back to #/module/logging-auditing
       - Confirm: exercise link now shows "Exercise complete — revisit →" in green (#22c55e)
    9. Navigate back to #/exercise/logging-auditing/01 (re-visit)
       - Confirm: all 3 steps shown in completed state, terminal is disabled, banner shows "Exercise previously completed — {date}"
    10. Verify no JS exceptions appear in browser console during any of the above steps
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 verification steps pass, or describe which steps failed and what was observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module-view → progressStore | getExerciseCompletion called synchronously in render; result used for template conditional only |
| module-view href → URL | moduleId and exerciseId values wrapped in esc() before href attribute insertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W3-01 | Tampering | exercise link href in module-view | mitigate | esc(mod.id) and esc(lesson.exerciseId) applied in href construction; acceptance criteria verifies esc(mod.id) count |
| T-05-W3-02 | Tampering | exerciseDone template conditional | accept | Boolean derived from progressStore return value; no user-controlled string injected into innerHTML |
| T-05-W3-03 | DoS | computeModuleProgress exerciseId lookup | accept | getExerciseCompletion is a synchronous localStorage read; O(1); no risk for expected module sizes |
</threat_model>

<verification>
Final phase gate — run full suite:

```
npx vitest run
```

All tests must be GREEN. Then run the manual verification steps listed in the human-verify checkpoint.

Before `/gsd-verify-work`, confirm:
- TERM-01: Correct command advances step, saves exercise completion — verified in exercise-view tests + manual step 7
- TERM-02: successOutput contains realistic PS 5.1 output (multi-line with CategoryInfo) — verified in 01.json + manual step 5
- TERM-03: Near-miss → contextual hint in step panel — verified in exercise-view tests + manual step 6
- TERM-04: Persistent simulator label with exact text — verified in exercise-view tests + manual step 4
</verification>

<success_criteria>
- src/quiz-engine.js computeModuleProgress has exerciseId branch: else if (lesson.exerciseId) block present
- src/views/module-view.js renders exercise link for lessons with exerciseId
- src/style.css has @keyframes terminal-cursor-blink block
- All quiz-engine.test.js tests GREEN (including 2 new exercise branch tests)
- npx vitest run exits 0 — full suite GREEN (all Phases 1–5 tests pass)
- Human checkpoint: all 10 manual verification steps pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-simulated-powershell-terminal-exercise-view/05-04-SUMMARY.md`
</output>
