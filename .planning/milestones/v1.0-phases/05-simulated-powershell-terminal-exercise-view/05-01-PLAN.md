---
phase: 05-simulated-powershell-terminal-exercise-view
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/terminal-engine.test.js
  - tests/exercise-view.test.js
  - tests/router.test.js
  - tests/quiz-engine.test.js
  - public/data/modules/logging-auditing/exercises/01.json
  - src/modules-config.js
autonomous: true
requirements:
  - TERM-01
  - TERM-02
  - TERM-03
  - TERM-04

must_haves:
  truths:
    - "All Wave 0 test files exist and run to RED (failing assertions, not syntax errors)"
    - "01.json has 3 steps, hintPatterns on each step, no status field, realistic PS 5.1 successOutput strings"
    - "modules-config.js ps-logging lesson has exerciseId: '01'"
    - "router.test.js has an exercise route match test that currently FAILS (route not yet registered)"
    - "quiz-engine.test.js computeModuleProgress describe block has two new exercise-branch tests"
  artifacts:
    - path: "tests/terminal-engine.test.js"
      provides: "RED test stubs for createTerminal API, appendOutput, disable, command history, Enter key handler"
      min_lines: 60
    - path: "tests/exercise-view.test.js"
      provides: "RED test stubs for renderExercise fetch-error, simulator label, step progression, hint display, completion flow"
      min_lines: 80
    - path: "public/data/modules/logging-auditing/exercises/01.json"
      provides: "3-step exercise with hintPatterns, realistic PS output, no status field"
      contains: "hintPatterns"
    - path: "src/modules-config.js"
      provides: "ps-logging lesson with exerciseId field"
      contains: "exerciseId"
  key_links:
    - from: "tests/terminal-engine.test.js"
      to: "src/terminal-engine.js"
      via: "dynamic import in beforeEach"
      pattern: "import.*terminal-engine"
    - from: "tests/exercise-view.test.js"
      to: "src/views/exercise-view.js"
      via: "dynamic import in beforeEach"
      pattern: "import.*exercise-view"
---

<objective>
Wave 0 establishes the RED test scaffolds and data prerequisites that all subsequent waves build against. This plan creates failing tests that define the contracts for terminal-engine.js and exercise-view.js before any implementation exists, upgrades the exercise JSON to the 3-step schema with hintPatterns, and wires the exerciseId field into modules-config.js.

Purpose: Nyquist compliance requires test files to exist (RED) before implementation. The exercise JSON upgrade and modules-config change are data prerequisites for Waves 1–3. Both can land here without any implementation risk.

Output: 4 test files updated/created, 1 JSON file upgraded, 1 config file updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-CONTEXT.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-RESEARCH.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-PATTERNS.md
@.planning/phases/05-simulated-powershell-terminal-exercise-view/05-VALIDATION.md

<interfaces>
<!-- Key interfaces from existing files the executor needs. -->

From tests/quiz-engine.test.js (mock block pattern — copy for new test files):
```javascript
vi.mock('../src/progress-store.js', () => ({
  progressStore: {
    init: vi.fn().mockResolvedValue(undefined),
    isStorageAvailable: vi.fn().mockReturnValue(true),
    markVisited: vi.fn(),
    markLessonCompleted: vi.fn(),
    saveQuiz: vi.fn(),
    getQuizScore: vi.fn().mockReturnValue(null),
    getLessonProgress: vi.fn().mockReturnValue({ visited: false, completed: false }),
    getExerciseCompletion: vi.fn().mockReturnValue(null),
    saveExercise: vi.fn(),
  },
}))
vi.mock('../src/utils/icons.js', () => ({ activateIcons: vi.fn() }))
vi.mock('../src/sidebar.js', () => ({ refreshSidebarProgress: vi.fn() }))
```

From tests/quiz-engine.test.js (import pattern — all vi.mock() BEFORE imports):
```javascript
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'
```

From tests/quiz-engine.test.js (KeyboardEvent dispatch for happy-dom):
```javascript
input.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }))
```

From tests/router.test.js (existing test style to match):
```javascript
it('matches exercise route and extracts moduleId and exerciseId', () => {
  expect(matchRoute('#/exercise/logging-auditing/01')).toEqual({
    view: 'exercise',
    params: { moduleId: 'logging-auditing', exerciseId: '01' }
  })
})
```

From src/modules-config.js (ps-logging lesson — line 17, add exerciseId):
```javascript
{ id: 'ps-logging', title: 'Enabling PowerShell Script Block Logging' }
// becomes:
{ id: 'ps-logging', title: 'Enabling PowerShell Script Block Logging', exerciseId: '01' }
```

From src/quiz-engine.js computeModuleProgress (lines 262–272, currently no exerciseId branch):
```javascript
for (const lesson of mod.lessons) {
  denominator++;
  if (lesson.quizId) {
    const quizScore = progressStore.getQuizScore(mod.id, lesson.quizId);
    if (quizScore !== null) numerator++;
  } else {
    const progress = progressStore.getLessonProgress(mod.id, lesson.id);
    if (progress && progress.visited) numerator++;
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED test stubs for terminal-engine.js and exercise-view.js</name>
  <files>tests/terminal-engine.test.js, tests/exercise-view.test.js</files>
  <read_first>
    - tests/quiz-engine.test.js — full file (mock structure, import pattern, vi.hoisted absent here, vi.mock before imports pattern, KeyboardEvent dispatch)
    - .planning/phases/05-simulated-powershell-terminal-exercise-view/05-PATTERNS.md — sections "tests/terminal-engine.test.js" and "tests/exercise-view.test.js" (complete test case lists and mock blocks)
    - .planning/phases/05-simulated-powershell-terminal-exercise-view/05-VALIDATION.md — Wave 0 Requirements and Per-Task Verification Map
    - .planning/phases/05-simulated-powershell-terminal-exercise-view/05-RESEARCH.md — section "What Can Be Unit Tested"
  </read_first>
  <behavior>
    terminal-engine.test.js RED behaviors (all must FAIL since src/terminal-engine.js does not exist yet):
    - createTerminal(container, handler) returns object with appendOutput, disable, setPrompt, focus methods
    - appendOutput(text) creates DOM element with textContent === text in the terminal output container
    - appendOutput(text, color) sets style.color on the output element to the provided color
    - disable() sets readonly attribute on the input element
    - disable() sets input.style.pointerEvents === 'none'
    - Enter key with empty trimmed string: commandHandler is NOT called
    - Enter key with non-empty string: commandHandler IS called with the trimmed value
    - Enter key: input.value === '' after commandHandler call (input cleared)
    - ArrowUp key: input.value becomes last submitted command
    - ArrowUp twice: input.value becomes second-to-last submitted command
    - ArrowDown after ArrowUp: input.value advances forward through history
    - ArrowDown at history start (historyIndex === 0): input.value === '' (cleared)

    exercise-view.test.js RED behaviors (all must FAIL since src/views/exercise-view.js does not exist yet):
    - Failed fetch (ok: false): DOM contains text "Exercise content could not be loaded"
    - Valid exercise JSON: exercise title appears in #app innerHTML
    - Valid exercise JSON: text "PS SIMULATOR — commands do not run on any real system" present in #app innerHTML (TERM-04)
    - Valid exercise JSON: step panel text "Step 1 of 2" present on initial render
    - Correct command on step 1 of 2-step exercise: progressStore.saveExercise NOT called (not last step)
    - Correct command on step 1: step panel text changes to "Step 2 of 2"
    - Correct command on last step (step 2 of 2): progressStore.saveExercise('logging-auditing', '01') called
    - Correct command on last step: progressStore.markLessonCompleted('logging-auditing', 'ps-logging') called
    - Near-miss command matching a hintPattern: hint area in DOM contains the hint text from that hintPattern (TERM-03)
    - Wrong command (no hint match): terminal.appendOutput called with feedbackOnWrong text
    - Re-visit mode (getExerciseCompletion returns non-null): terminal.disable() called
    - Re-visit: DOM contains text "previously completed"
  </behavior>
  <action>
    Create tests/terminal-engine.test.js:
    - Place all vi.mock() calls before imports (Vitest hoisting requirement — same as quiz-engine.test.js)
    - No mocks needed for terminal-engine.test.js itself (it has no external deps) — only mock DOM globals if needed
    - beforeEach: set document.body.innerHTML to a div with id="terminal-container"; dynamically import createTerminal from '../src/terminal-engine.js' using await import()
    - afterEach: vi.clearAllMocks()
    - Use document.createElement + container.appendChild pattern to mount terminal into a container div
    - For Enter/ArrowUp/ArrowDown tests: get the input element via container.querySelector('input'), then call input.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }))
    - For commandHandler call assertion: use vi.fn() as the handler and assert expect(handler).toHaveBeenCalledWith(trimmedValue)
    - These tests MUST fail (RED) because src/terminal-engine.js does not exist — the import will throw; wrap with try/catch in beforeEach or use expect.assertions(N) to confirm failure

    Create tests/exercise-view.test.js:
    - vi.mock('../src/progress-store.js', ...) with progressStore having: init, isStorageAvailable, saveExercise, getExerciseCompletion (mockReturnValue(null) default), markLessonCompleted, getLessonProgress
    - vi.mock('../src/utils/icons.js', ...) with activateIcons: vi.fn()
    - vi.mock('../src/modules-config.js', ...) with MODULES containing one module: id='logging-auditing', lessons=[{ id: 'ps-logging', title: 'Enabling PowerShell Script Block Logging', exerciseId: '01' }]
    - vi.mock('../src/sidebar.js', ...) with refreshSidebarProgress: vi.fn()
    - vi.mock('../src/terminal-engine.js', ...) — mock createTerminal to return { appendOutput: vi.fn(), disable: vi.fn(), setPrompt: vi.fn(), focus: vi.fn() } so exercise-view tests are decoupled from terminal DOM
    - EXERCISE_JSON fixture: 2-step object with id, moduleId, title, description, complianceControls, context, steps array. Step 1: expectedCommands pattern 'Get-ItemProperty.*ScriptBlockLogging', hintPatterns [{ pattern: 'Get-Item\\\\s+HKLM', hint: 'Close — try Get-ItemProperty.' }], successOutput 'Cannot find path...', feedbackOnWrong 'Navigate to HKLM...'. Step 2: expectedCommands pattern 'New-Item.*ScriptBlockLogging', hintPatterns [], successOutput 'ScriptBlockLogging created.', feedbackOnWrong 'Use New-Item...'
    - beforeEach: document.body.innerHTML = '<div id="app"></div>'; vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: true, json: vi.fn().mockResolvedValue(EXERCISE_JSON) }))
    - afterEach: vi.unstubAllGlobals(); vi.clearAllMocks()
    - For command-dispatch tests: renderExercise resolves with null (per pattern); exercise-view exposes commandHandler via the mocked createTerminal's first call arg — retrieve it via createTerminal.mock.calls[0][1] and call it directly with the test command string
    - For step progression assertions: check document.getElementById('app').innerHTML contains 'Step 2 of 2'
    - For simulator label: check document.getElementById('app').innerHTML includes 'PS SIMULATOR — commands do not run on any real system'
    - For hint test: after near-miss command dispatched, check DOM for the hint text value from hintPatterns[0].hint
  </action>
  <verify>
    <automated>npx vitest run tests/terminal-engine.test.js tests/exercise-view.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/terminal-engine.test.js exists and contains at minimum 12 it() blocks covering the behaviors listed above
    - tests/exercise-view.test.js exists and contains at minimum 12 it() blocks covering the behaviors listed above
    - Running the test suite produces FAILED output (RED), not a syntax/parse error — tests are structurally valid but the imports of non-existent modules cause failures
    - No test has the word "todo" or is a no-op empty it() block — every test has at least one expect() assertion
    - vi.mock() calls appear before import statements in both files (Vitest hoisting rule)
  </acceptance_criteria>
  <done>Both test files exist, structurally valid, RED (failing due to missing src files, not syntax errors)</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade 01.json to 3-step schema + update modules-config.js + add exercise tests to existing test files</name>
  <files>
    public/data/modules/logging-auditing/exercises/01.json,
    src/modules-config.js,
    tests/router.test.js,
    tests/quiz-engine.test.js
  </files>
  <read_first>
    - public/data/modules/logging-auditing/exercises/01.json — current file (1 step, status field, no hintPatterns)
    - src/modules-config.js — current file (ps-logging lesson on line 17, no exerciseId)
    - tests/router.test.js — full file (existing test style to match; find the describe block for matchRoute)
    - tests/quiz-engine.test.js — lines 270–330 (computeModuleProgress describe block; add 2 tests here)
    - .planning/phases/05-simulated-powershell-terminal-exercise-view/05-RESEARCH.md — section "Recommended 3-Step Exercise Schema for 01.json" (exact JSON target) and section "Pattern 4: computeModuleProgress Exercise Awareness" (exact else-if branch and test shapes)
    - .planning/phases/05-simulated-powershell-terminal-exercise-view/05-PATTERNS.md — section "tests/router.test.js" and section "tests/quiz-engine.test.js — computeModuleProgress exercise branch"
  </read_first>
  <action>
    Upgrade public/data/modules/logging-auditing/exercises/01.json:
    - Remove the "status": "placeholder" field from root level
    - Add hintPatterns array to step-1 with 2 entries: { "pattern": "Get-Item\\s+HKLM:\\\\SOFTWARE\\\\Policies|gci.*HKLM", "hint": "Close — try Get-ItemProperty instead of Get-Item for reading registry values." } and { "pattern": "regedit|reg\\s+query", "hint": "In PowerShell, use Get-ItemProperty with the HKLM: provider path rather than regedit or reg.exe." }
    - Update step-1 successOutput to the full PS 5.1 Get-ItemProperty error format from RESEARCH.md (multi-line with CategoryInfo and FullyQualifiedErrorId)
    - Add step-2: id "step-2", instruction "Create the ScriptBlockLogging registry key under the PowerShell policy path.", hint "Use New-Item to create the registry key at HKLM:\\SOFTWARE\\Policies\\Microsoft\\Windows\\PowerShell\\ScriptBlockLogging.", hintPatterns [{ "pattern": "Set-ItemProperty.*ScriptBlockLogging", "hint": "The key does not exist yet — use New-Item to create it first, then Set-ItemProperty to set the value." }], expectedCommands [{ "pattern": "New-Item.*ScriptBlockLogging", "matchType": "regex", "caseSensitive": false }], successOutput with New-Item registry output (Hive, Name, Property table), feedbackOnWrong "Use New-Item -Path 'HKLM:\\SOFTWARE\\Policies\\Microsoft\\Windows\\PowerShell\\ScriptBlockLogging' -Force to create the key."
    - Add step-3: id "step-3", instruction "Enable Script Block Logging by setting EnableScriptBlockLogging to 1.", hint "Use Set-ItemProperty with -Name EnableScriptBlockLogging -Value 1 on the key you just created.", hintPatterns [{ "pattern": "New-ItemProperty.*EnableScriptBlockLogging", "hint": "Close — Set-ItemProperty works on existing registry keys. Since you just created the key, use Set-ItemProperty -Name EnableScriptBlockLogging -Value 1." }], expectedCommands [{ "pattern": "Set-ItemProperty.*EnableScriptBlockLogging.*1|Set-ItemProperty.*1.*EnableScriptBlockLogging", "matchType": "regex", "caseSensitive": false }], successOutput with EnableScriptBlockLogging : 1 output plus PSPath/PSParentPath/PSChildName/PSDrive/PSProvider lines, feedbackOnWrong "Use Set-ItemProperty -Path 'HKLM:\\SOFTWARE\\Policies\\Microsoft\\Windows\\PowerShell\\ScriptBlockLogging' -Name 'EnableScriptBlockLogging' -Value 1."
    - All backslashes in JSON string values must be double-escaped: a literal backslash in PS output is \\, which in JSON must be \\\\

    Update src/modules-config.js:
    - Line 17: change { id: 'ps-logging', title: 'Enabling PowerShell Script Block Logging' } to add exerciseId: '01' field — per D-05 (exerciseId parallel to quizId) and RESEARCH.md Pitfall 6 (use ps-logging not audit-policies to avoid quizId conflict)
    - No other changes to modules-config.js

    Add test to tests/router.test.js:
    - Inside the existing describe block for matchRoute, add one new it() block: 'matches exercise route and extracts moduleId and exerciseId' that calls matchRoute('#/exercise/logging-auditing/01') and expects { view: 'exercise', params: { moduleId: 'logging-auditing', exerciseId: '01' } }
    - This test will FAIL (RED) because the exercise route is not registered in router.js yet — that happens in Wave 2

    Add tests to tests/quiz-engine.test.js:
    - Inside the existing describe('computeModuleProgress') block, add two new it() blocks per PATTERNS.md section "tests/quiz-engine.test.js — computeModuleProgress exercise branch":
      1. 'exercise-backed lesson with non-null completion counts as complete': mock getExerciseCompletion to return { completed: true, completedAt: '2026-05-15T10:00:00Z' }, create mod with one lesson { id: 'ps-logging', exerciseId: '01' }, call computeModuleProgress(mod), expect pct === 100 and complete === true
      2. 'exercise-backed lesson with null completion does not count': mock getExerciseCompletion to return null, same mod shape, expect pct === 0 and complete === false
    - Access the progressStore mock via the imported progressStore object and call .mockReturnValue() per-test using beforeEach reset pattern already established in the file; or use progressStoreMock reference if the file exposes it — check the actual quiz-engine.test.js structure for the mock reference variable name before writing
    - These two tests will FAIL (RED) because computeModuleProgress has no else-if exerciseId branch yet
  </action>
  <verify>
    <automated>npx vitest run tests/router.test.js tests/quiz-engine.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - public/data/modules/logging-auditing/exercises/01.json is valid JSON (no parse errors): node -e "JSON.parse(require('fs').readFileSync('public/data/modules/logging-auditing/exercises/01.json','utf8'))" exits 0
    - 01.json root object has no "status" field: grep -c '"status"' public/data/modules/logging-auditing/exercises/01.json equals 0
    - 01.json steps array has 3 elements: node -e "const j=JSON.parse(require('fs').readFileSync('public/data/modules/logging-auditing/exercises/01.json','utf8')); console.log(j.steps.length)" outputs 3
    - 01.json step-1 has hintPatterns array: node -e "const j=JSON.parse(require('fs').readFileSync('public/data/modules/logging-auditing/exercises/01.json','utf8')); console.log(j.steps[0].hintPatterns.length)" outputs a positive integer
    - src/modules-config.js ps-logging lesson contains exerciseId: '01': grep -c "exerciseId: '01'" src/modules-config.js equals 1
    - tests/router.test.js contains 'matches exercise route': grep -c 'exercise route' tests/router.test.js equals 1
    - tests/quiz-engine.test.js router test FAILS with assertion error (not syntax error): npx vitest run tests/router.test.js 2>&1 | grep -c 'FAIL' is positive
    - tests/quiz-engine.test.js computeModuleProgress exercise tests FAIL with assertion error: npx vitest run tests/quiz-engine.test.js 2>&1 shows at least 2 new failing tests
  </acceptance_criteria>
  <done>01.json has 3 steps with hintPatterns; modules-config has exerciseId on ps-logging; router.test.js and quiz-engine.test.js have RED exercise tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exercise JSON authors → test fixtures | hintPatterns regex strings in test fixtures must be syntactically valid JS regex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W0-01 | Tampering | 01.json regex patterns | mitigate | All backslashes double-escaped in JSON; validated by node -e JSON.parse() in acceptance criteria |
| T-05-W0-02 | Tampering | test fixture EXERCISE_JSON | accept | Test-only data; no production surface; happy-dom environment is sandboxed |
</threat_model>

<verification>
Run full suite after both tasks complete:

```
npx vitest run
```

Expected: pre-existing tests remain GREEN; new tests in terminal-engine.test.js, exercise-view.test.js, router.test.js (exercise route), and quiz-engine.test.js (exercise branch) are RED with assertion/import errors, not syntax errors.
</verification>

<success_criteria>
- tests/terminal-engine.test.js: exists, >= 12 it() blocks, runs RED
- tests/exercise-view.test.js: exists, >= 12 it() blocks, runs RED
- tests/router.test.js: 1 new exercise route test, RED
- tests/quiz-engine.test.js: 2 new computeModuleProgress exercise tests, RED
- public/data/modules/logging-auditing/exercises/01.json: 3 steps, hintPatterns, no status field, valid JSON
- src/modules-config.js: ps-logging lesson has exerciseId: '01'
- All pre-existing test files remain GREEN (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-simulated-powershell-terminal-exercise-view/05-01-SUMMARY.md`
</output>
