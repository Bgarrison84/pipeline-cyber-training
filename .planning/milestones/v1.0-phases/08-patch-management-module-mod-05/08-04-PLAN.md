---
phase: 08-patch-management-module-mod-05
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - src/modules-config.js
  - public/data/compliance-index.json
autonomous: true
requirements:
  - MOD-05

must_haves:
  truths:
    - "modules-config.js patch-management entry has 3 lessons: wsus-patching (scenarioId:'01'), ot-patching (exerciseId:'01', scenarioId:'02'), patch-policy (quizId:'01', scenarioId:'03')"
    - "compliance-index.json has 3 new control entries: TSA-PatchMgmt, NIST-SI-2, NIST-MA-2"
    - "TSA-PatchMgmt items array contains all 3 lessons, the exercise, and all 3 scenarios (7 items total)"
    - "NIST-SI-2 items array contains all 3 lessons and the exercise (4 items total)"
    - "NIST-MA-2 items array contains ot-patching lesson, patch-policy lesson, and scenario-02 (3 items total)"
    - "The existing 11 compliance controls in compliance-index.json are unchanged"
    - "npm test exits 0 (167 tests green)"
  artifacts:
    - path: "src/modules-config.js"
      provides: "patch-management module config with 3 lessons and all content-type IDs"
      contains: "patch-policy"
    - path: "public/data/compliance-index.json"
      provides: "3 new MOD-05 control entries appended to controls array"
      contains: "TSA-PatchMgmt"
  key_links:
    - from: "modules-config.js patch-management lessons[2]"
      to: "quiz-engine.js renderQuiz()"
      via: "quizId: '01' on patch-policy lesson config"
      pattern: "quizId: '01'"
    - from: "modules-config.js patch-management lessons[1]"
      to: "terminal-engine.js renderExercise()"
      via: "exerciseId: '01' on ot-patching lesson config"
      pattern: "exerciseId: '01'"
    - from: "modules-config.js patch-management lessons[*]"
      to: "scenario-view.js renderScenario()"
      via: "scenarioId: '01', '02', '03' on each lesson config"
      pattern: "scenarioId: '0[123]'"
---

<objective>
Wire the Patch Management module into the application by updating modules-config.js with the complete 3-lesson array (including the new patch-policy lesson and all content-type IDs) and appending 3 new compliance control entries to compliance-index.json (TSA-PatchMgmt, NIST-SI-2, NIST-MA-2).

Purpose: After this plan, navigating to the Patch Management module renders all 3 lessons in the sidebar with working quiz, exercise, and scenario links; the compliance index page lists all MOD-05 content under the correct control IDs.

Output: 2 modified files that wire all content authored in Plans 01-03 into the running application.
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\ROADMAP.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\08-patch-management-module-mod-05\08-CONTEXT.md

<interfaces>
<!-- Current patch-management entry in src/modules-config.js (lines 65-76, read before editing):
  {
    id: 'patch-management',
    title: 'Patch Management',
    icon: 'Wrench',
    description: 'Windows/IT patching, OT/ICS patching workflows, and compliance reporting.',
    order: 5,
    estimatedMinutes: 60,
    lessons: [
      { id: 'wsus-patching', title: 'Windows Update and WSUS' },
      { id: 'ot-patching',   title: 'OT/ICS Patching in Air-Gapped Environments' },
    ],
    complianceTags: ['TSA', 'NIST'],
  },

Target state (per D-01, D-03):
    lessons: [
      { id: 'wsus-patching', title: 'Windows Update and WSUS',                          scenarioId: '01' },
      { id: 'ot-patching',   title: 'OT/ICS Patching in Air-Gapped Environments',       exerciseId: '01', scenarioId: '02' },
      { id: 'patch-policy',  title: 'Patch Management Policy and Compliance Documentation', quizId: '01', scenarioId: '03' },
    ],
-->

<!-- compliance-index.json current state: 11 controls (TSA-Monitoring through NIST-AU-12).
3 new entries to APPEND at end of controls array (per D-08):

TSA-PatchMgmt — 7 items: 3 lessons + exercise + 3 scenarios
NIST-SI-2 — 4 items: 3 lessons + exercise
NIST-MA-2 — 3 items: ot-patching lesson + patch-policy lesson + scenario-02

Item object shape (from existing entries):
{ "type": "lesson"|"exercise"|"scenario", "moduleId": "patch-management", "contentId": "{lessonId|01}", "title": "..." }
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update src/modules-config.js — patch-management entry with 3 lessons and all content-type IDs</name>
  <files>
    src/modules-config.js
  </files>

  <read_first>
    - src/modules-config.js — read the entire file to see the current patch-management entry (lines 65-76) and the established lesson object pattern (lines 16-19 for logging-auditing); must not alter any other module entry
    - .planning/phases/08-patch-management-module-mod-05/08-CONTEXT.md — D-01 (lesson IDs: wsus-patching/ot-patching/patch-policy), D-03 (exact fields for each lesson config object)
  </read_first>

  <action>
Edit the patch-management entry in src/modules-config.js to match the target state per D-01 and D-03. Only the patch-management lessons array changes — all other fields (id, title, icon, description, order, estimatedMinutes, complianceTags) remain as-is.

Replace the current 2-lesson array:
```
lessons: [
  { id: 'wsus-patching', title: 'Windows Update and WSUS' },
  { id: 'ot-patching',   title: 'OT/ICS Patching in Air-Gapped Environments' },
],
```

With the 3-lesson array:
```
lessons: [
  { id: 'wsus-patching', title: 'Windows Update and WSUS',                                   scenarioId: '01' },
  { id: 'ot-patching',   title: 'OT/ICS Patching in Air-Gapped Environments',                exerciseId: '01', scenarioId: '02' },
  { id: 'patch-policy',  title: 'Patch Management Policy and Compliance Documentation',       quizId: '01',     scenarioId: '03' },
],
```

Key constraints:
- scenarioId and exerciseId and quizId belong ONLY in the modules-config.js lesson objects — never in .md frontmatter
- ot-patching has BOTH exerciseId: '01' AND scenarioId: '02' (new pattern per D-03 — L2 hosts both exercise and scenario)
- patch-policy has BOTH quizId: '01' AND scenarioId: '03' (new pattern per D-03 — L3 hosts both quiz and scenario)
- Do NOT change any other module entry in the MODULES array
  </action>

  <verify>
    <automated>
      node -e "
const {MODULES}=require('./src/modules-config.js');
const pm=MODULES.find(m=>m.id==='patch-management');
if(!pm) throw new Error('patch-management module not found');
if(pm.lessons.length!==3) throw new Error('expected 3 lessons, got '+pm.lessons.length);
const [l1,l2,l3]=pm.lessons;
if(l1.id!=='wsus-patching') throw new Error('L1 id must be wsus-patching');
if(l1.scenarioId!=='01') throw new Error('L1 must have scenarioId:01');
if(l1.exerciseId||l1.quizId) throw new Error('L1 must not have exerciseId or quizId');
if(l2.id!=='ot-patching') throw new Error('L2 id must be ot-patching');
if(l2.exerciseId!=='01') throw new Error('L2 must have exerciseId:01');
if(l2.scenarioId!=='02') throw new Error('L2 must have scenarioId:02');
if(l3.id!=='patch-policy') throw new Error('L3 id must be patch-policy');
if(l3.quizId!=='01') throw new Error('L3 must have quizId:01');
if(l3.scenarioId!=='03') throw new Error('L3 must have scenarioId:03');
if(MODULES.length!==5) throw new Error('MODULES must still have 5 entries, found '+MODULES.length);
console.log('modules-config.js patch-management entry: valid — 3 lessons, all content IDs correct');
"
    </automated>
  </verify>

  <acceptance_criteria>
    - src/modules-config.js MODULES array still has exactly 5 entries (no modules added or removed)
    - patch-management entry has exactly 3 lessons in the lessons array
    - lessons[0]: id:'wsus-patching', title:'Windows Update and WSUS', scenarioId:'01'; no exerciseId or quizId
    - lessons[1]: id:'ot-patching', title:'OT/ICS Patching in Air-Gapped Environments', exerciseId:'01', scenarioId:'02'; no quizId
    - lessons[2]: id:'patch-policy', title:'Patch Management Policy and Compliance Documentation', quizId:'01', scenarioId:'03'; no exerciseId
    - All other module entries (logging-auditing, network-hardening, account-access, incident-response) are unchanged
    - node validation script exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update public/data/compliance-index.json — append 3 new MOD-05 control entries</name>
  <files>
    public/data/compliance-index.json
  </files>

  <read_first>
    - public/data/compliance-index.json — read the entire file to see the 11 existing control entries and the exact item object shape; the 3 new entries are appended AFTER the NIST-AU-12 entry — do not alter existing entries
    - .planning/phases/08-patch-management-module-mod-05/08-CONTEXT.md — D-08 (exact control IDs, labels, and items per control), D-09 (compliance-refs.json is NOT modified)
  </read_first>

  <action>
Append 3 new control objects to the end of the controls[] array in public/data/compliance-index.json. All existing controls remain unchanged.

New entry 1 — TSA-PatchMgmt:
id: "TSA-PatchMgmt"
label: "TSA SD-02F — Patch Management"
items (7 total):
  1. type:"lesson", moduleId:"patch-management", contentId:"wsus-patching", title:"Windows Update and WSUS"
  2. type:"lesson", moduleId:"patch-management", contentId:"ot-patching", title:"OT/ICS Patching in Air-Gapped Environments"
  3. type:"lesson", moduleId:"patch-management", contentId:"patch-policy", title:"Patch Management Policy and Compliance Documentation"
  4. type:"exercise", moduleId:"patch-management", contentId:"01", title:"Patch Compliance Reporting"
  5. type:"scenario", moduleId:"patch-management", contentId:"01", title:"Critical CVE vs. OT Vendor Qualification Window"
  6. type:"scenario", moduleId:"patch-management", contentId:"02", title:"Maintenance Window vs. Ops Manager Pressure"
  7. type:"scenario", moduleId:"patch-management", contentId:"03", title:"TSA Audit: Missing Compensating Controls Documentation"

New entry 2 — NIST-SI-2:
id: "NIST-SI-2"
label: "NIST SP 800-82 Rev 3 — SI-2: Flaw Remediation"
items (4 total):
  1. type:"lesson", moduleId:"patch-management", contentId:"wsus-patching", title:"Windows Update and WSUS"
  2. type:"lesson", moduleId:"patch-management", contentId:"ot-patching", title:"OT/ICS Patching in Air-Gapped Environments"
  3. type:"lesson", moduleId:"patch-management", contentId:"patch-policy", title:"Patch Management Policy and Compliance Documentation"
  4. type:"exercise", moduleId:"patch-management", contentId:"01", title:"Patch Compliance Reporting"

New entry 3 — NIST-MA-2:
id: "NIST-MA-2"
label: "NIST SP 800-82 Rev 3 — MA-2: Controlled Maintenance"
items (3 total):
  1. type:"lesson", moduleId:"patch-management", contentId:"ot-patching", title:"OT/ICS Patching in Air-Gapped Environments"
  2. type:"lesson", moduleId:"patch-management", contentId:"patch-policy", title:"Patch Management Policy and Compliance Documentation"
  3. type:"scenario", moduleId:"patch-management", contentId:"02", title:"Maintenance Window vs. Ops Manager Pressure"

Do NOT modify any existing control entry. Do NOT modify public/data/compliance-refs.json (per D-09).
  </action>

  <verify>
    <automated>
      node -e "
const idx=JSON.parse(require('fs').readFileSync('public/data/compliance-index.json','utf8'));
if(idx.controls.length!==14) throw new Error('expected 14 controls (11 existing + 3 new), got '+idx.controls.length);
const pm=idx.controls.find(c=>c.id==='TSA-PatchMgmt');
const si2=idx.controls.find(c=>c.id==='NIST-SI-2');
const ma2=idx.controls.find(c=>c.id==='NIST-MA-2');
if(!pm) throw new Error('TSA-PatchMgmt not found');
if(!si2) throw new Error('NIST-SI-2 not found');
if(!ma2) throw new Error('NIST-MA-2 not found');
if(pm.items.length!==7) throw new Error('TSA-PatchMgmt must have 7 items, got '+pm.items.length);
if(si2.items.length!==4) throw new Error('NIST-SI-2 must have 4 items, got '+si2.items.length);
if(ma2.items.length!==3) throw new Error('NIST-MA-2 must have 3 items, got '+ma2.items.length);
// Verify TSA-PatchMgmt has all 3 lesson types
const pmLessons=pm.items.filter(i=>i.type==='lesson');
const pmExercises=pm.items.filter(i=>i.type==='exercise');
const pmScenarios=pm.items.filter(i=>i.type==='scenario');
if(pmLessons.length!==3) throw new Error('TSA-PatchMgmt must have 3 lesson items');
if(pmExercises.length!==1) throw new Error('TSA-PatchMgmt must have 1 exercise item');
if(pmScenarios.length!==3) throw new Error('TSA-PatchMgmt must have 3 scenario items');
// Verify NIST-MA-2 has scenario-02 only (not 01 or 03)
const ma2Scenarios=ma2.items.filter(i=>i.type==='scenario');
if(ma2Scenarios.length!==1 || ma2Scenarios[0].contentId!=='02') throw new Error('NIST-MA-2 must have exactly scenario-02');
// Verify first 11 controls are unmodified (spot check first and last original)
if(idx.controls[0].id!=='TSA-Monitoring') throw new Error('first control must remain TSA-Monitoring');
if(idx.controls[10].id!=='NIST-AU-12') throw new Error('11th control must remain NIST-AU-12');
console.log('compliance-index.json: 14 controls, all new MOD-05 entries valid, existing entries unchanged');
"
    </automated>
  </verify>

  <acceptance_criteria>
    - public/data/compliance-index.json controls array has exactly 14 entries (11 existing + 3 new)
    - Existing entries at positions 0-10 are unchanged (first: TSA-Monitoring, last-original: NIST-AU-12)
    - TSA-PatchMgmt entry: id "TSA-PatchMgmt", label "TSA SD-02F — Patch Management", 7 items (3 lessons + 1 exercise + 3 scenarios), all moduleId "patch-management"
    - NIST-SI-2 entry: id "NIST-SI-2", label "NIST SP 800-82 Rev 3 — SI-2: Flaw Remediation", 4 items (3 lessons + 1 exercise)
    - NIST-MA-2 entry: id "NIST-MA-2", label "NIST SP 800-82 Rev 3 — MA-2: Controlled Maintenance", 3 items (ot-patching lesson + patch-policy lesson + scenario-02 only)
    - compliance-refs.json is NOT modified
    - File is valid JSON (JSON.parse does not throw)
    - node validation script exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run test suite and verify no regressions</name>
  <files></files>

  <read_first>
    - No files to read — this task runs the existing test suite to confirm config changes did not break anything
  </read_first>

  <action>
Run the full Vitest test suite to confirm all 167 existing tests remain green after the modules-config.js and compliance-index.json updates. The test suite does not test content files (lessons/quiz/exercise/scenario JSON) directly, but it does test that modules-config.js exports a valid MODULES array and that compliance-index.json parses correctly.

Run: npm test
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    - npm test exits 0
    - All 167 tests pass (no new failures introduced by the config changes)
    - If any test fails, investigate whether it is due to the modules-config.js lessons array change (e.g., a test that checks the exact lesson count for patch-management) and fix accordingly
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/modules-config.js → module-view.js | Module config is imported statically; lesson objects are iterated to render sidebar and content-type links |
| compliance-index.json → compliance-index-view.js | JSON is fetched and iterated to build the compliance index page; item.moduleId and item.contentId are passed through safePath() before href construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | src/modules-config.js MODULES array | accept | Version-controlled in git; static import — no runtime write path |
| T-08-12 | Injection | compliance-index.json item.moduleId / item.contentId | accept | safePath() is applied to both fields in compliance-index-view.js before they are used in href construction (established in Phase 6 per ROADMAP cross-cutting constraints) |
| T-08-13 | Elevation of Privilege | New scenarioId on L2/L3 lessons (new pattern) | accept | module-view.js already handles rendering scenario links from any lesson config entry that has a scenarioId field — this is not new code path, just new data feeding an existing tested path |
</threat_model>

<verification>
After all three tasks complete:

1. Run npm test — all 167 tests must pass
2. Run the modules-config.js validation: node -e with MODULES import — exits 0 with "valid — 3 lessons, all content IDs correct"
3. Run the compliance-index.json validation: node -e — exits 0 with "14 controls, all new MOD-05 entries valid"
4. Manual smoke check (developer): navigate to #/module/patch-management in the browser — sidebar shows 3 lessons; ot-patching lesson shows both Exercise and Scenario buttons; patch-policy lesson shows both Quiz and Scenario buttons; wsus-patching shows Scenario button
5. Manual smoke check: navigate to #/compliance — TSA-PatchMgmt, NIST-SI-2, and NIST-MA-2 control entries appear with correct item links
</verification>

<success_criteria>
- src/modules-config.js: patch-management has 3 lessons; wsus-patching has scenarioId:'01'; ot-patching has exerciseId:'01' and scenarioId:'02'; patch-policy has quizId:'01' and scenarioId:'03'; no other module entries changed
- public/data/compliance-index.json: 14 controls total; TSA-PatchMgmt (7 items), NIST-SI-2 (4 items), NIST-MA-2 (3 items) all appended correctly; first 11 controls unchanged
- npm test: 167 tests green, exit 0
- compliance-refs.json: unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/08-patch-management-module-mod-05/08-04-SUMMARY.md`
</output>
