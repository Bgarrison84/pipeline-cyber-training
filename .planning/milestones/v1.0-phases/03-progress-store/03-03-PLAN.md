---
phase: 03-progress-store
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/sidebar.js
  - src/views/lesson-view.js
autonomous: true
requirements:
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "The sidebar footer contains 'Export my progress' and 'Import progress' controls visible below the module list and above the toggle button"
    - "Clicking 'Export my progress' downloads a file named pipeline-cyber-training-progress-{YYYY-MM-DD}.json"
    - "Clicking 'Import progress' opens a file picker; selecting a valid JSON file replaces the store and re-renders the lesson view"
    - "Selecting an invalid JSON file shows an inline error in the sidebar footer without overwriting existing progress"
    - "Opening any lesson calls progressStore.markVisited() and progressStore.setLastVisited()"
    - "When progressStore.isStorageAvailable() returns false, a div.storage-warning with role=alert is prepended to .lesson-column"
    - "When progressStore.isStorageAvailable() returns true, no storage-warning element appears in the DOM"
  artifacts:
    - path: "src/sidebar.js"
      provides: "Export/import footer controls wired to progressStore"
      contains: "sidebar-progress-footer"
    - path: "src/views/lesson-view.js"
      provides: "markVisited + setLastVisited calls + storage warning injection in post-render block"
      contains: "progressStore.markVisited"
  key_links:
    - from: "sidebar.js btn-export-progress click"
      to: "progressStore.exportProgress()"
      via: "addEventListener('click', ...)"
      pattern: "btn-export-progress"
    - from: "sidebar.js import-file-input change"
      to: "progressStore.importProgress(file)"
      via: "fileInput.addEventListener('change', async (e) => { const result = await progressStore.importProgress(e.target.files[0]); ... })"
      pattern: "importProgress"
    - from: "lesson-view.js renderLesson() post-render block"
      to: "progressStore.markVisited(moduleId, lessonId)"
      via: "called after app.innerHTML = lessonHtml"
      pattern: "markVisited"
---

## Phase Goal

**As a** pipeline IT/OT admin learner, **I want** my training progress saved automatically, **so that** I can close the browser and resume exactly where I left off without losing any work.

<objective>
Wire progressStore into the two remaining integration points: (1) sidebar.js footer — export/import controls always accessible from the sidebar, and (2) lesson-view.js — markVisited + setLastVisited called after every successful lesson render, plus an inline storage warning when the fallback is active.

Purpose: Completes Success Criteria 3 (storage full shows visible warning) and 4 (Export my progress downloads JSON). Also ensures lessons record visited state so the sidebar can eventually show progress indicators (Phase 4).

Output: src/sidebar.js (modified — footer DOM insertion + event wiring), src/views/lesson-view.js (modified — post-render progress calls + storage warning)
</objective>

<execution_context>
@C:\Users\psyco\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-progress-store/03-CONTEXT.md
@.planning/phases/03-progress-store/03-RESEARCH.md
@.planning/phases/03-progress-store/03-PATTERNS.md
@.planning/phases/03-progress-store/03-01-SUMMARY.md

<interfaces>
<!-- Current file state the executor must read before modifying. -->

From src/sidebar.js (current structure):
- initSidebar() function: sidebarModules innerHTML built (lines 23-44); then activateIcons() (line 47); then collapse toggle (lines 49-67)
- DOM elements: #sidebar-modules (flex:1), then #sidebar-toggle (button at bottom)
- Import pattern: uses esc() from './utils/escape.js'
- Null-guard pattern: `if (!sidebarModules) return;` at line 9; `if (toggleBtn && shell) { ... }` at line 54
- Footer MUST be inserted via sidebar.insertBefore(footer, toggleBtn) — NOT appendChild (RESEARCH.md Pitfall 5)

From src/views/lesson-view.js (current post-render block, lines 66-71):
- Step 5 — Post-render wiring:
  setActiveLesson(moduleId, lessonId);
  activateIcons();
  attachCopyHandlers();
  return null;
- Storage warning must be inserted AFTER app.innerHTML = lessonHtml (line 62-64) and BEFORE return null
- Selector for warning insertion: document.querySelector('.lesson-column') — the inner column div rendered in buildLessonHtml

From src/utils/escape.js:
- export function esc(str) — already imported in both sidebar.js (line 3) and lesson-view.js (line 11)

progressStore public API (from 03-01 output):
- progressStore.markVisited(moduleId, lessonId)
- progressStore.setLastVisited(moduleId, lessonId)
- progressStore.isStorageAvailable() -> boolean
- progressStore.exportProgress() -> void (triggers download)
- progressStore.importProgress(file) -> Promise<{ ok: boolean, error?: string }>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add export/import footer to sidebar.js (per D-12, D-13, D-14)</name>
  <files>src/sidebar.js</files>
  <read_first>
    - src/sidebar.js (read full file — must understand current structure at lines 1-68 before modifying)
    - .planning/phases/03-progress-store/03-PATTERNS.md (sidebar.js section — footer insertion, event listener pattern, insertBefore pattern)
    - .planning/phases/03-progress-store/03-RESEARCH.md (Pitfall 5: sidebar-toggle is last element; footer must use insertBefore; Pattern 5: import file handling)
    - .planning/phases/03-progress-store/03-CONTEXT.md (D-12: footer in sidebar; D-13: export filename pattern; D-14: import validation + re-render with handleRoute not reload)
  </read_first>
  <behavior>
    - import { progressStore } from './progress-store.js' added at top alongside existing imports
    - import { handleRoute } from './router.js' added at top (needed for re-render after import success)
    - After the collapse toggle block (after line 67 in current file), append footer insertion code
    - const sidebar = document.getElementById('sidebar'); const toggleBtn = document.getElementById('sidebar-toggle'); — with null-guard: if (!sidebar || !toggleBtn) return;
    - footer div: id="sidebar-progress-footer"; innerHTML contains one div wrapper with border-top style; two button elements (id="btn-export-progress" and id="btn-import-progress"); one hidden file input (id="import-file-input" type="file" accept=".json" style="display:none")
    - Button text: "Export my progress" and "Import progress" — small text link style: font-size var(--text-body), color var(--color-text-muted), background none, border none, cursor pointer, padding 0, text-decoration underline, text-underline-offset 3px
    - sidebar.insertBefore(footer, toggleBtn) — NOT sidebar.appendChild(footer)
    - Event wiring (all with null-guards before addEventListener):
      - const exportBtn = document.getElementById('btn-export-progress'); if (exportBtn) exportBtn.addEventListener('click', () => progressStore.exportProgress())
      - const importBtn = document.getElementById('btn-import-progress'); const fileInput = document.getElementById('import-file-input'); if (importBtn) importBtn.addEventListener('click', () => fileInput?.click())
      - if (fileInput) fileInput.addEventListener('change', async (e) => { const file = e.target.files?.[0]; if (!file) return; const result = await progressStore.importProgress(file); if (result.ok) { await handleRoute(); } else { const errEl = document.getElementById('import-error-msg'); if (errEl) errEl.textContent = result.error ?? 'Import failed.'; } e.target.value = ''; })
    - Add a span#import-error-msg inside the footer div (after the file input, before the closing div tag); style: display:block; font-size:var(--text-body); color:var(--color-destructive); margin-top:var(--spacing-xs); empty by default
    - esc() is already imported — use it if any user-controlled value is injected into innerHTML; the error message from result.error is set via .textContent (not innerHTML) so esc() is not needed for that assignment
    - No changes to any existing initSidebar logic (module list, activateIcons, collapse toggle)
  </behavior>
  <action>
    Edit src/sidebar.js. Add two import statements at the top: `import { progressStore } from './progress-store.js';` and `import { handleRoute } from './router.js';`. At the end of the initSidebar() function body (after the `if (toggleBtn && shell) { ... }` block), add the footer insertion and event wiring code as described in behavior. The footer HTML must use only CSS custom property values for spacing and color — no hardcoded hex or px values except text-underline-offset: 3px. Insertion must be sidebar.insertBefore(footer, toggleBtn). All element accesses guarded with null checks per WR-04 pattern.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npx vitest run</automated>
  </verify>
  <done>src/sidebar.js contains 'sidebar-progress-footer'; 'sidebar.insertBefore(footer, toggleBtn)' appears (not appendChild); 'btn-export-progress' and 'btn-import-progress' appear; full test suite passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire markVisited + storage warning into lesson-view.js (per D-03, D-10)</name>
  <files>src/views/lesson-view.js</files>
  <read_first>
    - src/views/lesson-view.js (read full file — must understand post-render block at lines 66-71 and buildLessonHtml structure at lines 93-129)
    - .planning/phases/03-progress-store/03-PATTERNS.md (lesson-view.js section — post-render wiring, storage warning injection, null-guard)
    - .planning/phases/03-progress-store/03-RESEARCH.md (storage warning code example; .storage-warning class; role=alert; lessonColumn.prepend)
    - .planning/phases/03-progress-store/03-CONTEXT.md (D-10: inline warning in lesson-view, not global banner; D-11: transparent to callers)
  </read_first>
  <behavior>
    - import { progressStore } from '../progress-store.js' added at top alongside existing imports
    - In renderLesson(), Step 5 post-render block (after `app.innerHTML = lessonHtml; app.removeAttribute('aria-busy');` and before `setActiveLesson(...)`:
      - Call progressStore.markVisited(moduleId, lessonId) — sets visited:true and updates lastVisited
      - Call progressStore.setLastVisited(moduleId, lessonId) — ensures lastVisited is set even if markVisited already set it (idempotent; belt-and-suspenders)
      - Storage warning: if (!progressStore.isStorageAvailable()) { const warningDiv = document.createElement('div'); warningDiv.className = 'storage-warning'; warningDiv.setAttribute('role', 'alert'); warningDiv.innerHTML = '<p style="font-size: var(--text-body); color: var(--color-text-muted); margin: 0 0 var(--spacing-sm) 0;">Progress cannot be saved — storage is unavailable (private browsing or quota full). Your progress this session will be lost when you close the tab.</p>'; const lessonColumn = document.querySelector('.lesson-column'); if (lessonColumn) lessonColumn.prepend(warningDiv); }
    - warningDiv.innerHTML: the message string contains no user-supplied values; esc() is NOT needed here (literal static text only)
    - Warning uses .storage-warning CSS class and role="alert" so screen readers announce it
    - The null-guard `if (lessonColumn)` before prepend follows WR-04 pattern
    - When isStorageAvailable() returns true, no DOM change is made (no warning element, no class added)
    - No changes to buildLessonHtml, renderLessonLoading, attachCopyHandlers, or any other function
  </behavior>
  <action>
    Edit src/views/lesson-view.js. Add `import { progressStore } from '../progress-store.js';` at the top alongside the existing import lines. In the renderLesson() function, find the Step 5 post-render block (currently: setActiveLesson -> activateIcons -> attachCopyHandlers -> return null). Insert BEFORE the setActiveLesson line: (1) progressStore.markVisited(moduleId, lessonId); (2) progressStore.setLastVisited(moduleId, lessonId); (3) the storage warning block described in behavior. Keep setActiveLesson, activateIcons, attachCopyHandlers, and return null in their current positions — only prepend the three new lines/blocks before them.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npx vitest run</automated>
  </verify>
  <done>src/views/lesson-view.js contains 'progressStore.markVisited'; 'progressStore.setLastVisited'; 'storage-warning'; 'isStorageAvailable'; full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Import file (user-controlled) | User-selected .json file content is parsed and replaces store; error message displayed in DOM |
| Sidebar footer DOM | Footer HTML is built by progress-store caller code; no user-supplied values in the initial render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | sidebar.js — import error message rendered to DOM | mitigate | Error message set via .textContent (not .innerHTML) — XSS impossible regardless of error string content |
| T-03-09 | Tampering | lesson-view.js — storage warning innerHTML | accept | Warning message is a static string literal; no user-supplied values interpolated; esc() not needed here; no injection risk |
| T-03-10 | Denial of Service | sidebar import — multiple rapid file selections | accept | Each change event calls importProgress(); happy-path race is resolved by e.target.value = '' reset at end; no resource leak; single-user static app |
</threat_model>

<verification>
After both tasks:

1. `npx vitest run` — full suite exits 0
2. `grep -n "sidebar.insertBefore" src/sidebar.js` — returns one match
3. `grep -c "localStorage" src/sidebar.js` — returns 0
4. `grep -c "localStorage" src/views/lesson-view.js` — returns 0
5. `grep -n "markVisited" src/views/lesson-view.js` — returns one match in the post-render block
6. `grep -n "storage-warning" src/views/lesson-view.js` — returns at least one match
7. Manual smoke test: `npm run dev`, navigate to any lesson — no storage-warning div visible in DOM (storage is available); open browser DevTools Application tab and confirm localStorage key 'pipeline-cyber-training:progress' is updated with the lesson's visited: true entry; click "Export my progress" — a .json file downloads
</verification>

<success_criteria>
1. Full Vitest suite (npx vitest run) exits 0 after both file modifications
2. src/sidebar.js: footer with id="sidebar-progress-footer" is inserted between #sidebar-modules and #sidebar-toggle via insertBefore; export button triggers progressStore.exportProgress(); import button triggers file input click; file change event calls progressStore.importProgress(file) and on success calls handleRoute()
3. src/views/lesson-view.js: progressStore.markVisited(moduleId, lessonId) and progressStore.setLastVisited(moduleId, lessonId) are called in the post-render block after app.innerHTML = lessonHtml; a div.storage-warning with role=alert is prepended to .lesson-column when isStorageAvailable() returns false
4. Neither src/sidebar.js nor src/views/lesson-view.js contains the string "localStorage"
</success_criteria>

<output>
After completion, create .planning/phases/03-progress-store/03-03-SUMMARY.md
</output>
