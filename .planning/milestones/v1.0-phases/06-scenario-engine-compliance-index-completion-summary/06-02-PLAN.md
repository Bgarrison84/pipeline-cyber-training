---
phase: 06
plan: 02
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - src/views/scenario-view.js
autonomous: true
requirements:
  - ASSESS-02
must_haves:
  truths:
    - "Navigating to #/scenario/logging-auditing/01 fetches and renders the scenario title and narrative"
    - "Clicking an option locks all buttons in the current phase and shows outcome text"
    - "Advancing past isFinal phase calls progressStore.saveScenario and sidebar refresh"
    - "Re-visit mode renders all phases locked with a completion banner immediately"
  artifacts:
    - path: "src/views/scenario-view.js"
      provides: "renderScenario async renderer with decision-tree progression and completion"
      exports:
        - "renderScenario"
  key_links:
    - from: "src/views/scenario-view.js"
      to: "progressStore.saveScenario"
      via: "completeScenario() call on isFinal phase"
      pattern: "saveScenario"
    - from: "src/views/scenario-view.js"
      to: "sidebar.js"
      via: "dynamic import('../sidebar.js').then(m => m.refreshSidebarProgress(moduleId))"
      pattern: "import\\('\\.\\.\/sidebar\\.js'\\)"
---

<objective>
Implement src/views/scenario-view.js: the async renderer for the scenario decision-tree UI.
This is the core ASSESS-02 feature — branching scenario exercises with outcome explanations and
completion saving. Mirrors exercise-view.js exactly, replacing terminal interaction with
phase-by-phase option selection.

Purpose: Turn the RED stubs in scenario-view.test.js GREEN. Deliver the decision-tree state
machine: initial render, option selection, outcome reveal, Continue button, completion on isFinal.

Output: src/views/scenario-view.js (new file).
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\06-scenario-engine-compliance-index-completion-summary\06-PATTERNS.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\06-scenario-engine-compliance-index-completion-summary\06-UI-SPEC.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\ROADMAP.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\06-scenario-engine-compliance-index-completion-summary\06-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/views/exercise-view.js (exact analog — full file already read):
- safePath(segment): throws if not /^[a-zA-Z0-9_-]+$/, returns segment
- import { esc } from '../utils/escape.js'
- import { activateIcons } from '../utils/icons.js'
- import { progressStore } from '../progress-store.js'
- import { renderBadge } from '../badge.js'
- Dynamic: import('../sidebar.js').then(m => m.refreshSidebarProgress(moduleId))
- Returns null (owns #app DOM directly)
- .lesson-wrapper / .lesson-column layout classes with max-width var(--lesson-reading-width,720px)
- Loading skeleton: .lesson-skeleton-line elements with aria-busy="true"
- Error state: role="alert", alert-circle icon, esc(moduleId) in back link href

From src/progress-store.js (saveScenario / getScenarioCompletion):
- progressStore.saveScenario(moduleId, scenarioId) → stores { completed: true, completedAt: ISO string }
- progressStore.getScenarioCompletion(moduleId, scenarioId) → { completed, completedAt } or null
- progressStore.isStorageAvailable() → boolean

From public/data/modules/logging-auditing/scenarios/01.json (schema):
- phases[]: { id, type, title, prompt, options[], isFinal? }
- options[]: { id, text, outcome, correct, nextPhaseId }
- top-level: id, moduleId, title, narrative, complianceControls[]

From 06-UI-SPEC.md — component contracts:
- C-02 Scenario Header: category label "SCENARIO", h1 title (28px/600), narrative (muted), badges row
- C-03 Phase Node (active): border-left 3px solid var(--color-accent), help-circle icon (accent)
- C-03 Phase Node (locked/revealed): border-left 3px solid var(--color-border), check-circle icon (muted)
- C-03 Phase Node (re-visit locked): border-left 3px solid #22c55e, check-circle icon (#22c55e), opacity 0.8
- C-04 Option Button: data-option-id attr, min-height 44px, 100% width, 1px border, pointer-events none after pick
  - Correct after pick: border #22c55e, bg rgba(34,197,94,0.06), check-circle icon at right
  - User's wrong pick: border var(--color-destructive), opacity 0.7, x-circle icon at right
  - Other wrong options: border var(--color-border), opacity 0.5
- C-05 Completion Banner: border #22c55e, check-circle 24px, "Scenario complete — well done.", badges, nav hint
- C-05 Re-visit variant: "Scenario previously completed — YYYY-MM-DD" (date from completedAt.slice(0,10))
- C-07 Error: "Scenario content could not be loaded", "This scenario may still be in development.", "Return to module"
- C-08 Loading: widths 80%, 65%, 45%, aria-live "Loading scenario..."

From 06-UI-SPEC.md — copywriting (exact strings):
- Category label: "SCENARIO"
- Outcome label: "Outcome:"
- Continue button: "Continue →"
- Completion heading: "Scenario complete — well done."
- Re-visit heading: "Scenario previously completed — {YYYY-MM-DD}"
- Storage warning: "Progress cannot be saved — storage unavailable. Your scenario completion will not be recorded."

From 06-PATTERNS.md — validateScenario pattern:
```javascript
function validateScenario(scenario) {
  const phaseIds = new Set((scenario.phases ?? []).map(p => p.id));
  for (const phase of scenario.phases ?? []) {
    for (const option of phase.options ?? []) {
      if (option.nextPhaseId && !phaseIds.has(option.nextPhaseId)) return false;
    }
  }
  return true;
}
```
Call after await res.json(). If false, render error and return null.

From 06-PATTERNS.md — runScenarioFlow pattern:
- State: currentPhase = phases[0]
- On option click: lock all options in phase card, apply correct/incorrect styles, inject outcome block, inject Continue button
- On Continue click: if currentPhase.isFinal → completeScenario(); else find nextPhaseId phase → inject new phase card, scrollIntoView, activateIcons
- completeScenario: progressStore.saveScenario, dynamic import sidebar, inject C-05 banner
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/views/scenario-view.js</name>
  <files>src/views/scenario-view.js</files>
  <read_first>
    - src/views/exercise-view.js (master analog — copy safePath, loading skeleton, error state, completion banner structure verbatim, then adapt)
    - tests/scenario-view.test.js (test contracts that must pass — read before writing any line)
    - public/data/modules/logging-auditing/scenarios/01.json (real schema the code must handle)
    - src/progress-store.js (saveScenario, getScenarioCompletion, isStorageAvailable signatures — lines 215–265)
    - src/badge.js (renderBadge signature)
    - src/utils/escape.js (esc signature)
  </read_first>
  <behavior>
    - renderScenario({ moduleId, scenarioId }) is exported and async, returns null
    - safePath throws on segments containing anything other than /^[a-zA-Z0-9_-]+$/
    - Invalid moduleId/scenarioId shows "Scenario content could not be loaded" in #app
    - Failed fetch shows error state in #app
    - validateScenario returns false for broken nextPhaseId references → shows error state
    - On success: renders header card (C-02) + first phase node (C-03 active) + all subsequent phases NOT yet injected
    - Re-visit mode: getScenarioCompletion non-null → all phases rendered as locked, completion banner injected, no event handlers wired
    - Clicking option: locks all buttons (pointer-events:none), applies correct/incorrect visual states per C-04, injects Outcome block, injects Continue button
    - Continue click on non-final phase: injects next phase node, scrolls into view, calls activateIcons
    - Continue click on isFinal phase: calls progressStore.saveScenario(moduleId, scenarioId), calls import('../sidebar.js').then(m => m.refreshSidebarProgress(moduleId)), injects C-05 completion banner
    - All scenario JSON strings pass through esc() before innerHTML: title, narrative, phase.title, phase.prompt, option.text, option.outcome
    - Storage unavailable: prepend warning paragraph to .lesson-column (role="alert")
    - No static import of sidebar.js
    - No direct localStorage calls
  </behavior>
  <action>
    Create src/views/scenario-view.js with the following structure:

    Imports: esc (utils/escape.js), activateIcons (utils/icons.js), progressStore (progress-store.js), renderBadge (badge.js). No static sidebar import.

    safePath: copy verbatim from exercise-view.js lines 19–22.

    validateScenario(scenario): as per 06-PATTERNS.md (checks every option.nextPhaseId resolves to a real phase id).

    renderScenarioLoading(): returns loading skeleton HTML with .lesson-wrapper, .lesson-column, 3 .lesson-skeleton-line divs at widths 80%/65%/45%, aria-busy="true", aria-live="polite" sr-only span "Loading scenario...".

    renderScenarioError(moduleId): returns error HTML with role="alert", alert-circle icon (20px, color var(--color-destructive)), heading "Scenario content could not be loaded", detail "This scenario may still be in development.", back link href="#/module/${esc(moduleId)}" text "Return to module".

    buildScenarioHeaderHtml(scenario): returns C-02 header card HTML — category label "SCENARIO" (monospace, muted, uppercase), h1 esc(scenario.title) (28px/600), narrative paragraph esc(scenario.narrative) (body/muted), compliance controls row with renderBadge() per control in complianceControls array (role="list" aria-label="Compliance controls covered").

    buildPhaseNodeHtml(phase, state, priorPickedOptionId): returns C-03 card HTML for given state ('active'|'locked'|'revisit-locked'). For 'active': border-left 3px solid var(--color-accent), help-circle icon (accent). For 'locked': border-left 3px solid var(--color-border), check-circle icon (muted). For 'revisit-locked': border-left 3px solid #22c55e, check-circle (#22c55e), opacity 0.8. Options rendered per C-04 inside each card. All option text via esc(option.text). For locked/revisit states, inject outcome block showing the picked option's outcome via esc().

    buildCompletionBannerHtml(scenario, priorCompletion): returns C-05 HTML — check-circle 24px (#22c55e), heading (fresh: "Scenario complete — well done." / re-visit: "Scenario previously completed — " + priorCompletion.completedAt.slice(0,10)), compliance badges row, nav hint "Use the sidebar or the back link to continue.".

    export async function renderScenario({ moduleId, scenarioId }):
    1. null-guard app element
    2. app.innerHTML = renderScenarioLoading()
    3. safePath guard (try/catch → renderScenarioError, return null)
    4. fetch url = import.meta.env.BASE_URL + 'data/modules/' + safePath(moduleId) + '/scenarios/' + safePath(scenarioId) + '.json'
    5. fetch try/catch (non-ok or throw → renderScenarioError, return null)
    6. validateScenario check (false → renderScenarioError, return null)
    7. priorCompletion = progressStore.getScenarioCompletion(moduleId, scenarioId)
    8. Build full HTML: lesson-wrapper > lesson-column > header card + (re-visit: all phase cards locked + completion banner) OR (fresh: first phase card active only)
    9. app.innerHTML = that HTML
    10. Storage warning prepend if !progressStore.isStorageAvailable()
    11. activateIcons()
    12. If priorCompletion: return null (no event wiring)
    13. Wire runScenarioFlow(app, scenario, moduleId, scenarioId)
    14. return null

    runScenarioFlow(app, scenario, moduleId, scenarioId) internal function:
    - currentPhaseId = scenario.phases[0].id
    - Wire option click handler on the active phase card using event delegation on app: on click of [data-option-id] inside [data-phase-id] card — lock all options, apply visual states (correct option: border #22c55e / bg rgba(34,197,94,0.06) / check-circle; user's wrong pick: border var(--color-destructive) / x-circle; other wrong: opacity 0.5), inject outcome block, inject Continue button
    - Continue button click: find the clicked option's nextPhaseId; if current phase isFinal → completeScenario(); else inject next phase card, scroll into view, activateIcons(), update event delegation target

    completeScenario() closure: progressStore.saveScenario(moduleId, scenarioId), then dynamic import('../sidebar.js').then(m => m.refreshSidebarProgress(moduleId)), then inject completion banner div into .lesson-column via appendChild.

    Key attribute names for test assertions: data-phase-id on each phase card, data-option-id on each option button, data-completion-banner on completion banner div.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test -- --reporter=dot 2>&1 | tail -30</automated>
  </verify>
  <done>
    - npm test: scenario-view.test.js is GREEN (all 5 stubs pass)
    - All 13 prior test files remain green
    - src/views/scenario-view.js exists and exports renderScenario
    - No static import of sidebar.js in the file (grep confirms: grep -c "^import.*sidebar" src/views/scenario-view.js returns 0)
    - All JSON string fields use esc() before innerHTML (grep confirms: grep "esc(" src/views/scenario-view.js has matches for title, narrative, prompt, text, outcome)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash → moduleId/scenarioId | User-controlled hash segments cross into fetch URL construction |
| scenario JSON → innerHTML | Developer-authored JSON fields injected into DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W1-01 | Tampering | safePath on moduleId/scenarioId | mitigate | safePath allowlist /^[a-zA-Z0-9_-]+$/ applied to both URL segments before fetch — throws and shows error state on invalid input |
| T-06-W1-02 | Tampering | scenario JSON fields in innerHTML | mitigate | esc() applied to all scenario JSON strings (title, narrative, phase.title, phase.prompt, option.text, option.outcome) before innerHTML injection — matches exercise-view.js pattern |
| T-06-W1-03 | Tampering | sidebar.js circular dep | accept | Dynamic import pattern prevents circular module graph — no static import of sidebar.js in scenario-view.js |
| T-06-W1-04 | Tampering | nextPhaseId reference integrity | mitigate | validateScenario() checks all nextPhaseId values resolve to real phase IDs before rendering — broken references show error state |
</threat_model>

<verification>
npm test green for all 13 prior files plus scenario-view.test.js (14 total green). src/views/scenario-view.js exports renderScenario. No static sidebar.js import. esc() applied to all scenario JSON string fields.
</verification>

<success_criteria>
- scenario-view.test.js passes all 5 stubs
- 14/16 total test files green (compliance-index and completion-summary still RED)
- renderScenario handles: loading → fetch → validate → re-visit vs. fresh → option selection → continue → completion
- completeScenario uses dynamic sidebar import — no static
</success_criteria>

<output>
After completion, create .planning/phases/06-scenario-engine-compliance-index-completion-summary/06-02-SUMMARY.md
</output>
