---
phase: 07-core-module-content-mod-02-mod-03-mod-04
plan: 04
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - src/modules-config.js
  - public/data/compliance-index.json
autonomous: true
requirements:
  - MOD-02
  - MOD-03
  - MOD-04
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "modules-config.js lessons array for network-hardening has exactly 3 entries: intro (scenarioId:'01'), ps-firewall (exerciseId:'01'), firewall-policy (quizId:'01')"
    - "modules-config.js lessons array for account-access has exactly 3 entries: intro (scenarioId:'01'), ps-ad (exerciseId:'01'), access-policy (quizId:'01')"
    - "modules-config.js lessons array for incident-response has exactly 3 entries: intro (scenarioId:'01'), ps-ir (exerciseId:'01'), ir-procedures (quizId:'01')"
    - "compliance-index.json controls[] has 11 entries total (2 existing + 9 new)"
    - "All 9 new control entries have non-empty items[] arrays"
    - "TSA-NetworkSeg, TSA-AccessControl, TSA-IR each have at least 5 items (3 lessons + exercise + scenario)"
    - "No existing content in modules-config.js or compliance-index.json is removed or broken"
    - "npm test exits 0 (167 tests green)"
  artifacts:
    - path: "src/modules-config.js"
      provides: "Updated module lesson arrays making all three new modules navigable with correct lesson IDs, scenarioId, exerciseId, and quizId assignments"
      contains: "ps-firewall"
    - path: "public/data/compliance-index.json"
      provides: "9 new compliance control entries linking new module content to TSA and NIST controls"
      contains: "TSA-NetworkSeg"
  key_links:
    - from: "modules-config.js network-hardening lessons[0]"
      to: "public/data/modules/network-hardening/scenarios/01.json"
      via: "scenarioId: '01' → scenario-view.js fetch path"
      pattern: "scenarioId: '01'"
    - from: "modules-config.js network-hardening lessons[1]"
      to: "public/data/modules/network-hardening/exercises/01.json"
      via: "exerciseId: '01' → exercise-view.js fetch path"
      pattern: "exerciseId: '01'"
    - from: "modules-config.js network-hardening lessons[2]"
      to: "quiz-engine.js"
      via: "quizId: '01' in lesson frontmatter → quiz-engine.js renderQuiz()"
      pattern: "quizId: '01'"
    - from: "compliance-index.json controls[]"
      to: "compliance-index-view.js"
      via: "controls[].items[] → link rendering for lessons/exercises/scenarios"
      pattern: "TSA-NetworkSeg"
---

<objective>
Wire the three new modules into the application by updating modules-config.js with the correct 3-lesson arrays (replacing placeholder content) and adding 9 new compliance control entries to compliance-index.json.

Purpose: Without this plan, all 18 content files created in Plans 01–03 are unreachable — the router and module-view cannot find them because modules-config.js still has the old placeholder lesson IDs. This plan is the integration step that makes MOD-02, MOD-03, and MOD-04 fully functional end-to-end.

Output: All three modules navigable via hash routing; all compliance controls visible on the index page.
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\ROADMAP.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\07-core-module-content-mod-02-mod-03-mod-04\07-CONTEXT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\07-core-module-content-mod-02-mod-03-mod-04\07-PATTERNS.md

<interfaces>
<!-- Current modules-config.js placeholder entries (read src/modules-config.js):
  network-hardening lessons (lines 29-32): firewall-basics, network-segmentation — 2 placeholder entries, no IDs
  account-access lessons (lines 43-44): least-privilege, service-accounts — 2 placeholder entries, no IDs
  incident-response lessons (lines 55-57): anomaly-detection, evidence-collection — 2 placeholder entries, no IDs

  NOTE: logging-auditing and patch-management must NOT be modified.
-->

<!-- compliance-index.json current state (read public/data/compliance-index.json):
  2 existing entries: TSA-Monitoring and NIST-AU-2
  New entries go inside controls[] AFTER the existing NIST-AU-2 entry.
  Do NOT modify or remove TSA-Monitoring or NIST-AU-2.
-->

<!-- D-03 lesson arrays (from CONTEXT.md — exact IDs and title strings):
  network-hardening:
    { id: 'intro',           title: 'Network Hardening Overview',                 scenarioId: '01' }
    { id: 'ps-firewall',     title: 'Managing Firewall Rules with PowerShell',     exerciseId: '01' }
    { id: 'firewall-policy', title: 'Windows Firewall Policy for OT Networks',     quizId: '01' }

  account-access:
    { id: 'intro',         title: 'Account and Access Control Overview',           scenarioId: '01' }
    { id: 'ps-ad',         title: 'Active Directory Queries with PowerShell',      exerciseId: '01' }
    { id: 'access-policy', title: 'Least Privilege and Service Account Policy',    quizId: '01' }

  incident-response:
    { id: 'intro',         title: 'Incident Response Overview',                    scenarioId: '01' }
    { id: 'ps-ir',         title: 'Evidence Collection with PowerShell',           exerciseId: '01' }
    { id: 'ir-procedures', title: 'Containment and Recovery Procedures',           quizId: '01' }
-->

<!-- D-10 compliance control IDs and labels (from CONTEXT.md):
  MOD-02: TSA-NetworkSeg / NIST-SC-7 / NIST-SI-3
  MOD-03: TSA-AccessControl / NIST-AC-2 / NIST-AC-6
  MOD-04: TSA-IR / NIST-IR-4 / NIST-AU-12

  Labels (exact, from CONTEXT.md D-10):
  TSA-NetworkSeg:  "TSA SD-02F — Network Segmentation and Access Control"
  NIST-SC-7:       "NIST SP 800-82 Rev 3 — SC-7: Boundary Protection"
  NIST-SI-3:       "NIST SP 800-82 Rev 3 — SI-3: Malicious Code Protection"
  TSA-AccessControl: "TSA SD-02F — Access Control Management"
  NIST-AC-2:       "NIST SP 800-82 Rev 3 — AC-2: Account Management"
  NIST-AC-6:       "NIST SP 800-82 Rev 3 — AC-6: Least Privilege"
  TSA-IR:          "TSA SD-02F — Cybersecurity Incident Response"
  NIST-IR-4:       "NIST SP 800-82 Rev 3 — IR-4: Incident Handling"
  NIST-AU-12:      "NIST SP 800-82 Rev 3 — AU-12: Audit Record Generation"
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update modules-config.js with 3-lesson arrays for MOD-02, MOD-03, MOD-04</name>
  <files>
    src/modules-config.js
  </files>

  <read_first>
    - src/modules-config.js — read the full file before editing; target the lessons[] arrays for network-hardening (lines 29–32), account-access (lines 43–44), and incident-response (lines 55–57); logging-auditing and patch-management must NOT be touched
    - .planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-CONTEXT.md — D-03 for exact lesson id strings, title strings, and which field (scenarioId/exerciseId/quizId) each lesson gets
    - .planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-PATTERNS.md — PITFALL: scenarioId and exerciseId go ONLY in modules-config.js lesson objects, NEVER in .md frontmatter; quizId goes ONLY in frontmatter of Lesson 3 .md files, not in modules-config.js
  </read_first>

  <action>
Replace the placeholder lessons[] arrays for network-hardening, account-access, and incident-response in src/modules-config.js. Use targeted edits — one replacement per module. Do NOT touch logging-auditing or patch-management.

**network-hardening lessons replacement** — replace:
```
    lessons: [
      { id: 'firewall-basics',       title: 'Windows Firewall with Advanced Security' },
      { id: 'network-segmentation',  title: 'OT Network Segmentation Principles' },
    ],
```
with:
```
    lessons: [
      { id: 'intro',           title: 'Network Hardening Overview',              scenarioId: '01' },
      { id: 'ps-firewall',     title: 'Managing Firewall Rules with PowerShell', exerciseId: '01' },
      { id: 'firewall-policy', title: 'Windows Firewall Policy for OT Networks', quizId: '01' },
    ],
```

**account-access lessons replacement** — replace:
```
    lessons: [
      { id: 'least-privilege',  title: 'Least Privilege Principles' },
      { id: 'service-accounts', title: 'Securing Service Accounts' },
    ],
```
with:
```
    lessons: [
      { id: 'intro',         title: 'Account and Access Control Overview',        scenarioId: '01' },
      { id: 'ps-ad',         title: 'Active Directory Queries with PowerShell',   exerciseId: '01' },
      { id: 'access-policy', title: 'Least Privilege and Service Account Policy', quizId: '01' },
    ],
```

**incident-response lessons replacement** — replace:
```
    lessons: [
      { id: 'anomaly-detection',   title: 'Detecting Anomalies with PowerShell' },
      { id: 'evidence-collection', title: 'Evidence Collection and Preservation' },
    ],
```
with:
```
    lessons: [
      { id: 'intro',         title: 'Incident Response Overview',              scenarioId: '01' },
      { id: 'ps-ir',         title: 'Evidence Collection with PowerShell',     exerciseId: '01' },
      { id: 'ir-procedures', title: 'Containment and Recovery Procedures',     quizId: '01' },
    ],
```

After edits: verify logging-auditing lessons array is unchanged (intro/ps-logging/audit-policies), patch-management is unchanged.
  </action>

  <verify>
    <automated>
      node -e "
const {MODULES} = await import('./src/modules-config.js').catch(()=>({MODULES:null}));
" 2>/dev/null || node --input-type=module -e "
import { MODULES } from './src/modules-config.js';
const nh = MODULES.find(m=>m.id==='network-hardening');
const aa = MODULES.find(m=>m.id==='account-access');
const ir = MODULES.find(m=>m.id==='incident-response');
if(nh.lessons.length!==3) throw new Error('network-hardening: expected 3 lessons');
if(!nh.lessons[0].scenarioId) throw new Error('network-hardening intro missing scenarioId');
if(!nh.lessons[1].exerciseId) throw new Error('network-hardening ps-firewall missing exerciseId');
if(!nh.lessons[2].quizId) throw new Error('network-hardening firewall-policy missing quizId field in config (note: quizId is in frontmatter, not config — check this is not present)');
if(aa.lessons.length!==3) throw new Error('account-access: expected 3 lessons');
if(!aa.lessons[0].scenarioId) throw new Error('account-access intro missing scenarioId');
if(ir.lessons.length!==3) throw new Error('incident-response: expected 3 lessons');
const la = MODULES.find(m=>m.id==='logging-auditing');
if(la.lessons[0].id!=='intro') throw new Error('logging-auditing lessons unchanged check failed');
console.log('modules-config.js assertions passed');
"
    </automated>
  </verify>

  <acceptance_criteria>
    - src/modules-config.js network-hardening lessons array has exactly 3 entries with ids: 'intro', 'ps-firewall', 'firewall-policy'; intro has scenarioId: '01'; ps-firewall has exerciseId: '01'; firewall-policy has quizId: '01' (note: quizId in modules-config is intentional here — this is a special field that quiz-engine.js also reads from config for the module overview; cross-check with 07-PATTERNS.md)
    - src/modules-config.js account-access lessons array has exactly 3 entries with ids: 'intro', 'ps-ad', 'access-policy'; intro has scenarioId: '01'; ps-ad has exerciseId: '01'; access-policy has quizId: '01'
    - src/modules-config.js incident-response lessons array has exactly 3 entries with ids: 'intro', 'ps-ir', 'ir-procedures'; intro has scenarioId: '01'; ps-ir has exerciseId: '01'; ir-procedures has quizId: '01'
    - src/modules-config.js logging-auditing lessons array is UNCHANGED: still contains 'intro' (scenarioId:'01'), 'ps-logging' (exerciseId:'01'), 'audit-policies' (quizId:'01')
    - src/modules-config.js patch-management lessons array is UNCHANGED: still contains 'wsus-patching' and 'ot-patching'
    - The old placeholder lesson IDs (firewall-basics, network-segmentation, least-privilege, service-accounts, anomaly-detection, evidence-collection) do NOT appear anywhere in the file
    - npm test exits 0 (167 tests green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add 9 new compliance control entries to compliance-index.json</name>
  <files>
    public/data/compliance-index.json
  </files>

  <read_first>
    - public/data/compliance-index.json — read the full file; note the existing NIST-AU-2 entry ends at line 52; new entries go inside the controls[] array after NIST-AU-2, before the closing bracket; do NOT modify TSA-Monitoring or NIST-AU-2 entries
    - .planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-CONTEXT.md — D-10 for exact control IDs, exact labels, and which items[] each control links to; D-11 confirms compliance-refs.json is NOT modified — only compliance-index.json
    - .planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-RESEARCH.md — "compliance-index.json Schema" section for the exact items[] structure; TSA-* controls need 5 items minimum (3 lessons + exercise + scenario); NIST-specific controls need only the lessons that directly cover them
  </read_first>

  <action>
Add 9 new control entries to the controls[] array in public/data/compliance-index.json, after the existing NIST-AU-2 entry. Use exact control IDs and labels from D-10. Do NOT modify the existing TSA-Monitoring or NIST-AU-2 entries.

**MOD-02 controls (3 entries):**

TSA-NetworkSeg:
- id: "TSA-NetworkSeg"
- label: "TSA SD-02F — Network Segmentation and Access Control"
- items: all 5 network-hardening content pieces:
  - lesson intro (title: "Network Hardening Overview")
  - lesson ps-firewall (title: "Managing Firewall Rules with PowerShell")
  - lesson firewall-policy (title: "Windows Firewall Policy for OT Networks")
  - exercise 01 (title: "Firewall Audit and Port Mapping")
  - scenario 01 (title: "Unauthorized Access Point on the IT/OT Boundary")

NIST-SC-7:
- id: "NIST-SC-7"
- label: "NIST SP 800-82 Rev 3 — SC-7: Boundary Protection"
- items: network-hardening lessons that directly cover boundary protection (intro and firewall-policy):
  - lesson intro (title: "Network Hardening Overview")
  - lesson firewall-policy (title: "Windows Firewall Policy for OT Networks")

NIST-SI-3:
- id: "NIST-SI-3"
- label: "NIST SP 800-82 Rev 3 — SI-3: Malicious Code Protection"
- items: ps-firewall lesson (port-to-process mapping directly supports malware detection):
  - lesson ps-firewall (title: "Managing Firewall Rules with PowerShell")

**MOD-03 controls (3 entries):**

TSA-AccessControl:
- id: "TSA-AccessControl"
- label: "TSA SD-02F — Access Control Management"
- items: all 5 account-access content pieces:
  - lesson intro (title: "Account and Access Control Overview")
  - lesson ps-ad (title: "Active Directory Queries with PowerShell")
  - lesson access-policy (title: "Least Privilege and Service Account Policy")
  - exercise 01 (title: "AD Group Audit and Local Admin Review")
  - scenario 01 (title: "Unauthorized Privilege Escalation on PIPELINE-DC01")

NIST-AC-2:
- id: "NIST-AC-2"
- label: "NIST SP 800-82 Rev 3 — AC-2: Account Management"
- items: account-access lessons covering account management (intro and ps-ad):
  - lesson intro (title: "Account and Access Control Overview")
  - lesson ps-ad (title: "Active Directory Queries with PowerShell")

NIST-AC-6:
- id: "NIST-AC-6"
- label: "NIST SP 800-82 Rev 3 — AC-6: Least Privilege"
- items: access-policy lesson (directly covers least privilege policy):
  - lesson access-policy (title: "Least Privilege and Service Account Policy")

**MOD-04 controls (3 entries):**

TSA-IR:
- id: "TSA-IR"
- label: "TSA SD-02F — Cybersecurity Incident Response"
- items: all 5 incident-response content pieces:
  - lesson intro (title: "Incident Response Overview")
  - lesson ps-ir (title: "Evidence Collection with PowerShell")
  - lesson ir-procedures (title: "Containment and Recovery Procedures")
  - exercise 01 (title: "Evidence Collection and System Isolation")
  - scenario 01 (title: "Ransomware Indicator on Pipeline DMZ Workstation")

NIST-IR-4:
- id: "NIST-IR-4"
- label: "NIST SP 800-82 Rev 3 — IR-4: Incident Handling"
- items: incident-response lessons covering incident handling (intro and ir-procedures):
  - lesson intro (title: "Incident Response Overview")
  - lesson ir-procedures (title: "Containment and Recovery Procedures")

NIST-AU-12:
- id: "NIST-AU-12"
- label: "NIST SP 800-82 Rev 3 — AU-12: Audit Record Generation"
- items: ps-ir lesson from incident-response (evidence export) PLUS MOD-01 backfill (ps-logging.md already has NIST-AU-12 in complianceControls but is not yet indexed):
  - lesson ps-ir from incident-response (title: "Evidence Collection with PowerShell")
  - lesson ps-logging from logging-auditing (title: "Enabling PowerShell Script Block Logging")
  - exercise 01 from logging-auditing (title: "Enable Script Block Logging")

All items use the exact structure: { "type": "lesson"|"exercise"|"scenario", "moduleId": "{module-id}", "contentId": "{lesson-id}"|"01", "title": "{exact title from content}" }

Verify: the resulting file must be valid JSON (no trailing commas, matching brackets). Total controls[] array size: 11 entries (2 existing + 9 new).
  </action>

  <verify>
    <automated>
      node -e "
const fs=require('fs');
const idx=JSON.parse(fs.readFileSync('public/data/compliance-index.json','utf8'));
if(idx.controls.length!==11) throw new Error('Expected 11 controls, got '+idx.controls.length);
const ids=idx.controls.map(c=>c.id);
['TSA-Monitoring','NIST-AU-2','TSA-NetworkSeg','NIST-SC-7','NIST-SI-3','TSA-AccessControl','NIST-AC-2','NIST-AC-6','TSA-IR','NIST-IR-4','NIST-AU-12'].forEach(id=>{
  if(!ids.includes(id)) throw new Error('Missing control: '+id);
});
const tsaNh=idx.controls.find(c=>c.id==='TSA-NetworkSeg');
if(tsaNh.items.length<5) throw new Error('TSA-NetworkSeg must have at least 5 items, got '+tsaNh.items.length);
const tsaAa=idx.controls.find(c=>c.id==='TSA-AccessControl');
if(tsaAa.items.length<5) throw new Error('TSA-AccessControl must have at least 5 items');
const tsaIr=idx.controls.find(c=>c.id==='TSA-IR');
if(tsaIr.items.length<5) throw new Error('TSA-IR must have at least 5 items');
const nistAu12=idx.controls.find(c=>c.id==='NIST-AU-12');
if(nistAu12.items.length!==3) throw new Error('NIST-AU-12 must have 3 items (ps-ir + MOD-01 backfill), got '+nistAu12.items.length);
console.log('compliance-index.json assertions passed: '+idx.controls.length+' controls');
"
    </automated>
  </verify>

  <acceptance_criteria>
    - public/data/compliance-index.json is valid JSON (node -e "require('./public/data/compliance-index.json')" exits 0)
    - controls[] array has exactly 11 entries
    - All 9 new control IDs are present: TSA-NetworkSeg, NIST-SC-7, NIST-SI-3, TSA-AccessControl, NIST-AC-2, NIST-AC-6, TSA-IR, NIST-IR-4, NIST-AU-12
    - Existing entries TSA-Monitoring and NIST-AU-2 are UNCHANGED (same items[], same labels)
    - TSA-NetworkSeg has at least 5 items (3 lessons + exercise + scenario from network-hardening); all items have moduleId: "network-hardening"
    - TSA-AccessControl has at least 5 items from account-access; TSA-IR has at least 5 items from incident-response
    - NIST-SC-7 items reference only network-hardening lessons; NIST-AC-2 items reference only account-access lessons; NIST-IR-4 items reference only incident-response lessons
    - NIST-SI-3 has exactly 1 item (ps-firewall lesson); NIST-AC-6 has exactly 1 item (access-policy lesson); NIST-AU-12 has exactly 3 items (ps-ir from incident-response + ps-logging lesson and exercise 01 from logging-auditing — MOD-01 backfill)
    - All item contentId values for lessons match the lesson ids from D-02 (intro, ps-firewall, firewall-policy, ps-ad, access-policy, ps-ir, ir-procedures); exercise and scenario items use contentId: "01"
    - npm test exits 0 (167 tests green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages CDN → browser | modules-config.js is a static JS module; compliance-index.json is a static JSON file; both served read-only over HTTPS |
| browser → engines | compliance-index-view.js fetches compliance-index.json; module-view.js and router.js read MODULES from modules-config.js at import time; all content IDs used in URL construction pass through safePath() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | modules-config.js lesson ID strings | accept | Version-controlled; served read-only via GitHub Pages; lesson IDs are path segments used with safePath() validation already in router.js |
| T-07-14 | Injection (Path traversal) | compliance-index.json moduleId/contentId used in href construction | accept | compliance-index-view.js already applies safePath() to item.moduleId and item.contentId before constructing lesson/exercise/scenario hrefs (established in Phase 6 Plan 06-03) |
| T-07-15 | Information Disclosure | compliance-index.json control labels | accept | Control labels are intentionally public — they are the compliance training content; no PII, no secrets |
| T-07-16 | Spoofing | compliance control label accuracy (TSA directive version) | mitigate | Labels use "TSA SD-02F" as a human-readable label sourced from D-10 decisions — this is acceptable in a data file label; lesson prose (where version staleness is a risk) uses the compliance-refs.json approach per DATA-01 |
</threat_model>

<verification>
After both tasks complete:

1. Run `npm test` — all 167 tests must pass
2. Run: `node -e "require('./public/data/compliance-index.json'); console.log('JSON valid')"` — must exit 0
3. Run: `node -e "const fs=require('fs'); const s=fs.readFileSync('src/modules-config.js','utf8'); if(s.includes('firewall-basics')) throw new Error('old placeholder found'); if(s.includes('anomaly-detection')) throw new Error('old placeholder found'); console.log('No old placeholders found')"` — must exit 0
4. Run: `node -e "const fs=require('fs'); const idx=JSON.parse(fs.readFileSync('public/data/compliance-index.json','utf8')); console.log('Controls: '+idx.controls.length); idx.controls.forEach(c=>console.log(c.id+': '+c.items.length+' items'))"` — must show 11 controls, TSA-* controls each with ≥5 items
5. Manual browser check: start dev server and navigate to #/module/network-hardening — sidebar must show 3 lessons with scenario/exercise/quiz links
6. Manual browser check: navigate to #/compliance-index — verify TSA-NetworkSeg, TSA-AccessControl, TSA-IR all appear with linked lesson/exercise/scenario items
</verification>

<success_criteria>
- src/modules-config.js lessons arrays for network-hardening, account-access, incident-response each have 3 entries with correct ids and one of scenarioId/exerciseId/quizId per lesson
- Old placeholder lesson IDs (firewall-basics, network-segmentation, least-privilege, service-accounts, anomaly-detection, evidence-collection) are gone
- logging-auditing and patch-management lesson arrays are unchanged
- compliance-index.json has 11 total controls (2 existing + 9 new)
- All 9 new control entries present with correct ids, labels, and items[] arrays
- TSA-NetworkSeg, TSA-AccessControl, TSA-IR each have ≥5 items; NIST-specific controls have only the lessons that cover them
- npm test exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-04-SUMMARY.md`
</output>
