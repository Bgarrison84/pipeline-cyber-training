---
phase: 07-core-module-content-mod-02-mod-03-mod-04
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - public/data/modules/incident-response/lessons/intro.md
  - public/data/modules/incident-response/lessons/ps-ir.md
  - public/data/modules/incident-response/lessons/ir-procedures.md
  - public/data/modules/incident-response/quizzes/01.json
  - public/data/modules/incident-response/exercises/01.json
  - public/data/modules/incident-response/scenarios/01.json
autonomous: true
requirements:
  - MOD-04
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "The incident-response module directory contains all 6 content files (3 lessons + quiz + exercise + scenario)"
    - "Every lesson contains exactly one > [!OT] callout block"
    - "intro.md references CIP-008 with the verbatim NERC CIP disclaimer"
    - "ir-procedures.md references CIP-008 with the verbatim NERC CIP disclaimer"
    - "The quiz has exactly 3 questions with 4 answers each and exactly 1 correct answer per question"
    - "The exercise has exactly 4 steps matching the D-06 commands: Get-Process CPU sort, Get-NetTCPConnection external connections, Get-WinEvent logon export to CSV, Disable-NetAdapter"
    - "The scenario id is incident-response-scenario-01, phase-1 isFinal:false, phase-2 isFinal:true"
    - "Evidence staging path C:\\Evidence\\ is used in the exercise (from D-06)"
    - "No hardcoded TSA version strings in lesson prose"
  artifacts:
    - path: "public/data/modules/incident-response/lessons/intro.md"
      provides: "Incident Response Overview — scenario host, CIP-008 reference"
      contains: "lessonId: intro"
    - path: "public/data/modules/incident-response/lessons/ps-ir.md"
      provides: "Evidence Collection with PowerShell — exercise host"
      contains: "lessonId: ps-ir"
    - path: "public/data/modules/incident-response/lessons/ir-procedures.md"
      provides: "Containment and Recovery Procedures — quiz host"
      contains: "quizId: '01'"
    - path: "public/data/modules/incident-response/quizzes/01.json"
      provides: "Incident Response quiz — 3 questions on evidence, isolation, IR procedures"
      contains: "incident-response-quiz-01"
    - path: "public/data/modules/incident-response/exercises/01.json"
      provides: "Evidence collection and system isolation exercise — 4 steps"
      contains: "incident-response-ex-01"
    - path: "public/data/modules/incident-response/scenarios/01.json"
      provides: "Ransomware Indicator scenario — 2-phase OT-priority decision tree"
      contains: "incident-response-scenario-01"
  key_links:
    - from: "public/data/modules/incident-response/lessons/ir-procedures.md"
      to: "quiz-engine.js renderQuiz()"
      via: "quizId: '01' in frontmatter"
      pattern: "quizId: '01'"
    - from: "public/data/modules/incident-response/scenarios/01.json phase-1"
      to: "phase-2"
      via: "nextPhaseId: phase-2 on both options"
      pattern: "\"nextPhaseId\": \"phase-2\""
    - from: "public/data/modules/incident-response/scenarios/01.json phase-2"
      to: "terminal (completion)"
      via: "isFinal: true and nextPhaseId: null"
      pattern: "\"isFinal\": true"
---

<objective>
Author all six content files for MOD-04 Incident Response: three lesson Markdown files, one quiz JSON, one terminal exercise JSON, and one scenario JSON. All files are created from scratch under public/data/modules/incident-response/.

Purpose: Delivers a complete vertical slice — a real user can navigate to the Incident Response module, read lessons on evidence collection and containment procedures, practice PS evidence-gathering commands in the terminal, work through a ransomware indicator scenario with OT-priority decision logic, and take a quiz.

Output: 6 new files under public/data/modules/incident-response/ ready to be served by the existing engines.
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\ROADMAP.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\07-core-module-content-mod-02-mod-03-mod-04\07-CONTEXT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\07-core-module-content-mod-02-mod-03-mod-04\07-PATTERNS.md

<interfaces>
<!-- Lesson frontmatter schema for incident-response:
     - intro.md:        complianceControls: [TSA-IR, NIST-IR-4]; order: 1; NO quizId
     - ps-ir.md:        complianceControls: [TSA-IR, NIST-AU-12]; order: 2; NO quizId
     - ir-procedures.md: complianceControls: [TSA-IR, NIST-IR-4]; order: 3; quizId: '01'
     scenarioId / exerciseId NEVER in .md frontmatter.
-->

<!-- D-06 exercise commands (exact, from CONTEXT.md):
  Step 1: Get-Process | Sort-Object CPU -Descending | Select-Object -First 10 Name, Id, CPU, Path
  Step 2: Get-NetTCPConnection -State Established | Where-Object {$_.RemoteAddress -notlike '10.*'} | Select-Object LocalPort, RemoteAddress, RemotePort, OwningProcess
  Step 3: Get-WinEvent -LogName Security -MaxEvents 50 | Where-Object {$_.Id -eq 4624} | Select-Object TimeCreated, Message | Export-Csv 'C:\Evidence\logons.csv' -NoTypeInformation
  Step 4: Disable-NetAdapter -Name 'Ethernet' -Confirm:$false
  TEACHING NOTE for Step 4: isolation ONLY after evidence collected (CONTEXT.md D-06 note)
-->

<!-- D-09 scenario (exact, from CONTEXT.md):
  id: incident-response-scenario-01
  title: "Ransomware Indicator on Pipeline DMZ Workstation"
  Phase 1 "Triage": EDR alert on DMZ workstation — file encryption + lateral movement toward historian VLAN.
    opt-a correct: isolate immediately (OT safety priority; spread to safety systems > evidence loss)
    opt-b wrong: collect full forensic evidence before isolation (OT context: operational risk of spread is unacceptable)
  Phase 2 "Reporting": Workstation isolated; script block logging shows encoded PS command as initial vector.
    opt-a correct: notify TSA within 24 hours + preserve isolated workstation for forensics
    opt-b wrong: restore from backup immediately to minimize downtime (destroys evidence, violates reporting)
-->

<!-- Evidence staging path from D-06 / CONTEXT.md specifics: C:\Evidence\ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three Incident Response lesson Markdown files</name>
  <files>
    public/data/modules/incident-response/lessons/intro.md
    public/data/modules/incident-response/lessons/ps-ir.md
    public/data/modules/incident-response/lessons/ir-procedures.md
  </files>

  <read_first>
    - public/data/modules/logging-auditing/lessons/intro.md — Pattern A (scenario-host): frontmatter layout, OT callout block syntax `> [!OT]`, NERC CIP disclaimer block format, body section order
    - public/data/modules/logging-auditing/lessons/ps-logging.md — Pattern B (exercise-host): code block comment `# NIST {control}: {purpose}`, OT callout after verification section
    - public/data/modules/logging-auditing/lessons/audit-policies.md — Pattern C (quiz-host): quizId: '01' frontmatter, OT callout, NERC CIP block, body opens with compliance rationale
    - .planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-CONTEXT.md — D-02 (lesson IDs/titles), D-12 (NERC CIP: intro gets CIP-008, ir-procedures gets CIP-008), D-06 specifics (C:\Evidence\ path)
    - .planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-PATTERNS.md — Pattern A/B/C body structures; shared rules: OT callout syntax, generic identifiers, TSA version string rule, PowerShell code block format
  </read_first>

  <action>
Create directory public/data/modules/incident-response/lessons/ and author three lesson files using exact MOD-01 analog patterns.

**FILE 1: intro.md** (Pattern A — scenario host, D-02)
Frontmatter:
- title: Incident Response Overview
- lessonId: intro
- moduleId: incident-response
- order: 1
- complianceTags: [TSA, NIST]
- complianceControls: [TSA-IR, NIST-IR-4]
- NO quizId, scenarioId, or exerciseId in frontmatter

Body sections (Pattern A structure):
- H2 "What Is Incident Response in a Pipeline Context?" — The current TSA pipeline security directive requires pipeline operators to establish and implement a cybersecurity incident response plan; in a pipeline OT environment, an incident is any cybersecurity event that affects or could affect the availability, integrity, or confidentiality of operational technology systems including SCADA, DCS, historian servers, and HMI workstations; incident response in OT differs from IT: OT incidents carry operational safety implications — a delayed or incorrect response can affect pipeline pressure control, leak detection, or safety instrumented systems
- H2 "The Four IR Phases in Pipeline OT" — table: Detection (SIEM/EDR alert + monitoring logs — NIST IR-4), Triage (scope the incident; determine OT vs IT impact), Containment (isolate affected systems; coordinate with OT operations), Recovery (restore from verified backup; re-verify operational integrity before reconnecting to control network); explain that in OT the order matters — containment before recovery, and evidence before containment where operationally safe
- H2 "Identifying Suspicious Processes with PowerShell" — code block: `# NIST IR-4: Identify high-CPU processes that may indicate ransomware or cryptomining activity on PIPELINE-DC01` then `Get-Process | Sort-Object CPU -Descending | Select-Object -First 10 Name, Id, CPU, Path` — explain: anomalous high-CPU processes (especially with no Path or running from temp directories) are an early ransomware indicator
- H2 "Detecting Unauthorized External Connections" — code block: `# NIST IR-4: Identify established TCP connections to external IPs (not in 10.0.0.0/8 range)` then `Get-NetTCPConnection -State Established | Where-Object {$_.RemoteAddress -notlike '10.*'} | Select-Object LocalPort, RemoteAddress, RemotePort, OwningProcess` — explain: in an OT environment, any established connection to a non-RFC1918 address from a pipeline workstation is a critical anomaly requiring immediate investigation
- OT callout: `> [!OT]` — In OT environments, ransomware that reaches the historian VLAN can corrupt or encrypt time-series process data, affecting operational visibility for hours or days; the primary OT incident response principle is safety first — if a threat shows signs of lateral movement toward safety instrumented systems (SIS) or SCADA, isolate before collecting complete forensic evidence; partial forensic evidence from an isolated system is better than a complete forensic dataset from a system that has spread malware to the control network; coordinate all isolation decisions with the operations team before disconnecting any system that may affect pipeline control
- NERC CIP block (per D-12, CIP-008 in intro): `> **NERC CIP scope note:** NERC CIP governs electric utilities; pipeline operators follow TSA directives — referenced here as a maturity benchmark. CIP-008 incident reporting requirements mandate that cybersecurity incidents affecting critical cyber assets are reported and tracked — the TSA pipeline security directive has structurally equivalent incident notification requirements for pipeline operators.`
- Closing paragraph transitioning to Lesson 2 (ps-ir) about evidence collection

**FILE 2: ps-ir.md** (Pattern B — exercise host, D-02)
Frontmatter:
- title: Evidence Collection with PowerShell
- lessonId: ps-ir
- moduleId: incident-response
- order: 2
- complianceTags: [TSA, NIST]
- complianceControls: [TSA-IR, NIST-AU-12]
- NO quizId in frontmatter

Body sections (Pattern B structure):
- H2 "Evidence Collection Principles" — collect before you contain where operationally safe; evidence must be collected in a forensically sound manner that preserves integrity; PowerShell provides three key evidence collection capabilities: process snapshot, network connection snapshot, and Security event log export; all collections should be timestamped and saved to a dedicated evidence path (C:\Evidence\)
- H2 "Capturing a Process Snapshot" — code block: `# NIST AU-12: Capture a timestamped process snapshot as evidence — run at first sign of anomaly on PIPELINE-DC01` then `Get-Process | Sort-Object CPU -Descending | Select-Object -First 10 Name, Id, CPU, Path` — explain: the process list represents system state at the time of the snapshot; a high-CPU process with no Path or running from C:\Users\*\AppData\Local\Temp\ is a strong ransomware indicator; document the PID for correlation with network connection data
- H2 "Capturing Network Connection State" — code block: `# NIST IR-4: Capture external TCP connections at time of incident — connection to non-internal IP is an anomaly in this environment` then `Get-NetTCPConnection -State Established | Where-Object {$_.RemoteAddress -notlike '10.*'} | Select-Object LocalPort, RemoteAddress, RemotePort, OwningProcess` — explain: the OwningProcess PID links connections back to the process snapshot; an OwningProcess with no corresponding legitimate path is a command-and-control indicator
- H2 "Exporting Security Event Logs as Evidence" — code block: `# NIST AU-12: Export recent logon events to CSV for chain-of-custody evidence — run before isolation` then: first show the pipeline, then explain `-NoTypeInformation` removes the type header from CSV making it readable by investigators without PS: `Get-WinEvent -LogName Security -MaxEvents 50 | Where-Object {$_.Id -eq 4624} | Select-Object TimeCreated, Message | Export-Csv 'C:\Evidence\logons.csv' -NoTypeInformation` — explain: C:\Evidence\ should be a pre-created directory; the file becomes your chain-of-custody artifact; hash it immediately after creation: `Get-FileHash 'C:\Evidence\logons.csv' -Algorithm SHA256`
- H2 "Isolating the System" — code block: `# TSA-IR: Isolate network adapter after evidence collection is complete — coordination with OT operations required` then `Disable-NetAdapter -Name 'Ethernet' -Confirm:$false` — IMPORTANT explanation: ISOLATION IS A CONTAINMENT STEP, not an evidence step; this command is run ONLY after process snapshot, connection snapshot, and log export are complete; for OT-connected systems, notify operations before running this command as it will immediately stop any data collection, polling, or control commands flowing through the Ethernet interface; use the adapter name that matches your environment (verify with `Get-NetAdapter`)
- OT callout: `> [!OT]` — In OT environments, `Disable-NetAdapter` on the wrong interface can immediately stop SCADA data collection, historian writes, or DCS polling — causing loss of operational visibility; before running any isolation command, run `Get-NetAdapter` to identify all adapters and their connection states; on multi-homed OT systems (one IT-facing NIC and one OT-facing NIC), disable only the IT-facing adapter to preserve OT network connectivity; always coordinate with operations: "I am about to isolate this workstation from the IT network — confirm this will not affect pipeline control." NIST AU-12 audit record generation requirements still apply during an incident — every command you run during response should be logged in your incident record
- Closing paragraph noting the terminal exercise lets you practice all four evidence collection and isolation steps in the simulator before performing them on a live system

**FILE 3: ir-procedures.md** (Pattern C — quiz host, D-02, D-12 CIP-008)
Frontmatter:
- title: Containment and Recovery Procedures
- lessonId: ir-procedures
- moduleId: incident-response
- order: 3
- complianceTags: [TSA, NIST]
- complianceControls: [TSA-IR, NIST-IR-4]
- quizId: '01'   ← REQUIRED on Lesson 3 only

Body sections (Pattern C structure):
- H2 "Why Containment Procedures Matter in OT" — compliance rationale: the current TSA pipeline security directive requires documented containment procedures for cybersecurity incidents affecting pipeline systems; in OT environments, containment without a procedure risks inadvertently taking down safety instrumented systems, blocking SCADA polling, or interrupting historian data collection that is legally required for operational continuity; documented procedures ensure consistent, authorized, and reversible containment actions
- H2 "OT-Specific Containment Decision Tree" — table of 3 scenarios: (1) Workstation with ransomware indicators only — isolate immediately if not OT-critical, collect evidence first if OT-critical; (2) Ransomware spreading toward historian VLAN — isolate boundary immediately regardless of evidence state (safety first); (3) Compromised service account on domain — disable in AD + force password reset + preserve logs before removing from domain
- H2 "Checking IR Plan Readiness" — code block: `# NIST IR-4: Verify that the C:\Evidence\ evidence staging directory exists and is writable before an incident` then `Test-Path 'C:\Evidence\' ; New-Item -ItemType Directory -Path 'C:\Evidence\' -Force` — explain: pre-staging the evidence directory ensures it exists when needed; create it during quarterly IR drills rather than during an actual incident
- H2 "Documenting the Incident Timeline" — code block: `# TSA-IR: Append a timestamped entry to the incident log during response` then `Add-Content -Path 'C:\Evidence\incident-log.txt' -Value "$(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC') — $(Read-Host 'Action taken')"` — explain: every action during IR should be logged in real time; this simple approach creates a timestamped audit trail that can be incorporated into the TSA incident report
- H2 "TSA Incident Notification Requirements" — explain that the current TSA pipeline security directive requires notification of cybersecurity incidents within specific timeframes; notification must include: what systems were affected, when the incident was detected, what containment actions were taken, and the operational impact on pipeline operations; notification goes to TSA's cybersecurity division (contact information is in the operator's TSA-approved cybersecurity plan); failure to notify is itself a compliance violation
- OT callout: `> [!OT]` — In OT environments, recovery requires verification that operational data integrity was not compromised before reconnecting to the control network; run a data integrity check on historian records covering the incident window before declaring recovery complete; verify that all SCADA setpoints and configuration files on restored workstations match the known-good baseline stored in the configuration management system; NIST IR-4 incident handling requires post-incident review — document what worked, what failed, and what process changes are needed before closing the incident record
- NERC CIP block (per D-12, CIP-008 in ir-procedures): `> **NERC CIP scope note:** NERC CIP governs electric utilities; pipeline operators follow TSA directives — referenced here as a maturity benchmark. CIP-008 requires that incident response plans be tested annually and that all incidents be documented and reported — the TSA pipeline security directive imposes structurally equivalent documentation and notification obligations for pipeline operators.`
- H2 "IR Procedure Checklist" — table: Evidence collection complete before isolation (yes/no per scenario type), OT operations notified before any network isolation (mandatory), C:\Evidence\ directory pre-staged (should be done during quarterly drill), TSA notification initiated within required timeframe (per current TSA pipeline security directive), Incident log timestamped for every action (mandatory), Post-incident review scheduled (within 30 days of closure)

CONTENT RULES (apply to all three files):
- Every PS code block fence uses ```powershell
- First code block line: `# NIST {control}: {purpose}`
- No hardcoded "SD-02F" version strings in prose — use "the current TSA pipeline security directive"
- Evidence path: C:\Evidence\ (from D-06 and CONTEXT.md specifics)
- Host: PIPELINE-DC01, domain: ExampleCorp, network: 10.0.0.0/24
- Each file must contain exactly one `> [!OT]` block
  </action>

  <verify>
    <automated>
      node -e "const fs=require('fs'); ['intro','ps-ir','ir-procedures'].forEach(f=>{const c=fs.readFileSync('public/data/modules/incident-response/lessons/'+f+'.md','utf8'); if(!c.includes('[!OT]')) throw new Error(f+' missing OT callout'); if(!c.includes('lessonId:')) throw new Error(f+' missing lessonId'); console.log(f+': OK')})"
    </automated>
  </verify>

  <acceptance_criteria>
    - public/data/modules/incident-response/lessons/intro.md exists; frontmatter has `lessonId: intro`, `moduleId: incident-response`, `order: 1`, `complianceTags: [TSA, NIST]`, `complianceControls: [TSA-IR, NIST-IR-4]`; NO quizId field; body contains `> [!OT]`; body contains verbatim "NERC CIP governs electric utilities; pipeline operators follow TSA directives — referenced here as a maturity benchmark"; body mentions "CIP-008"
    - public/data/modules/incident-response/lessons/ps-ir.md exists; frontmatter has `lessonId: ps-ir`, `order: 2`, `complianceControls: [TSA-IR, NIST-AU-12]`; NO quizId field; body contains `> [!OT]`; body contains `Get-Process`, `Get-NetTCPConnection`, `Export-Csv`, `Disable-NetAdapter`; contains `C:\Evidence\`
    - public/data/modules/incident-response/lessons/ir-procedures.md exists; frontmatter has `lessonId: ir-procedures`, `order: 3`, `quizId: '01'`; body contains `> [!OT]`; body contains verbatim NERC CIP disclaimer; body mentions "CIP-008"; no `exerciseId` or `scenarioId` in frontmatter
    - None of the three files contains "SD-02F" as a standalone version string in lesson prose
    - All three files use only generic identifiers: PIPELINE-DC01, ExampleCorp, 10.0.0.0/24, C:\Evidence\
    - npm test exits 0 (167 tests green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the Incident Response quiz, exercise, and scenario JSON files</name>
  <files>
    public/data/modules/incident-response/quizzes/01.json
    public/data/modules/incident-response/exercises/01.json
    public/data/modules/incident-response/scenarios/01.json
  </files>

  <read_first>
    - public/data/modules/logging-auditing/quizzes/01.json — canonical quiz JSON schema; exact field names, answer IDs a/b/c/d, explanation field
    - public/data/modules/logging-auditing/exercises/01.json — exercise JSON schema; hintPatterns format, successOutput PS table format with \n, feedbackOnWrong directive sentence
    - public/data/modules/logging-auditing/scenarios/01.json — scenario JSON schema; narrative OT paragraph, phases with isFinal and nextPhaseId values, options array
    - .planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-CONTEXT.md — D-06 (exact 4-step exercise commands and the CRITICAL teaching note: isolation ONLY after evidence collected), D-09 (exact scenario structure: id, title, phase titles, OT-priority decision logic — Phase 1 correct is isolate immediately for OT safety)
    - .planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-PATTERNS.md — Quiz/Exercise/Scenario hard constraints; Pitfall 5 (successOutput realistic PS format); Pitfall 7 (nextPhaseId mismatch)
  </read_first>

  <action>
Create directories public/data/modules/incident-response/quizzes/, exercises/, and scenarios/ and write three JSON files.

**FILE: quizzes/01.json**
id: "incident-response-quiz-01"
moduleId: "incident-response"
title: "Incident Response Knowledge Check"
Exactly 3 questions (q-01, q-02, q-03). Each has 4 answers (a, b, c, d). Exactly 1 correct per question.

q-01 (complianceControls: ["NIST-AU-12"]):
stem: "Which PowerShell command exports Security event log entries to a CSV file for chain-of-custody evidence?"
a: "Get-EventLog -LogName Security | Out-File 'C:\\Evidence\\logons.txt'" (incorrect — Out-File creates a plain text file, not a structured CSV; investigators cannot easily parse or import it; use Export-Csv for structured evidence)
b: "Get-WinEvent -LogName Security -MaxEvents 50 | Where-Object {$_.Id -eq 4624} | Select-Object TimeCreated, Message | Export-Csv 'C:\\Evidence\\logons.csv' -NoTypeInformation" (correct — Get-WinEvent with Where-Object filters to only logon events (4624), Select-Object limits columns, Export-Csv creates a structured CSV, and -NoTypeInformation removes the PS type header that would confuse non-PowerShell investigators)
c: "Copy-Item -Path 'C:\\Windows\\System32\\winevt\\Logs\\Security.evtx' -Destination 'C:\\Evidence\\'" (incorrect — copying the raw .evtx binary file preserves the event log but requires Event Viewer or specialized tools to read; Export-Csv is more accessible for investigators and satisfies NIST AU-12 audit record generation in a portable format)
d: "wevtutil epl Security C:\\Evidence\\security.evtx" (incorrect — wevtutil epl exports the event log in binary .evtx format, not a portable CSV; it is useful for offline log analysis but does not produce the structured, human-readable CSV format required for chain-of-custody documentation)
explanation: "Get-WinEvent piped to Export-Csv with -NoTypeInformation produces a portable, human-readable evidence file. Filter to specific Event IDs (4624 for logons) and use Select-Object to include only the fields needed for the investigation. Hash the resulting CSV immediately with Get-FileHash for chain-of-custody integrity verification (NIST AU-12)."

q-02 (complianceControls: ["TSA-IR", "NIST-IR-4"]):
stem: "Your EDR alerts to possible ransomware file encryption on a DMZ workstation showing lateral movement signatures toward the historian VLAN. In an OT pipeline context, what is the highest-priority first action?"
a: "Collect a complete forensic image of the workstation disk before taking any other action" (incorrect — full disk imaging takes 30–90 minutes; in an OT context, that delay allows ransomware to reach and encrypt historian data or SCADA configuration files, potentially affecting pipeline operational safety; OT IR prioritizes containment over complete forensic evidence)
b: "Isolate the workstation immediately from the network to stop lateral movement toward OT systems" (correct — in OT environments, the threat of ransomware reaching historian, SCADA, or safety instrumented systems outweighs the forensic loss of a network isolation; isolate first, then collect evidence from the isolated system; this is the OT IR principle: operational safety priority over forensic completeness when spread to safety systems is the risk)
c: "Wait for the security team to assess the EDR alert before taking any action" (incorrect — an EDR alert with confirmed lateral movement signatures toward OT systems is not a 'wait and see' situation; in OT incident response, waiting for a formal assessment while ransomware propagates toward safety systems is unacceptable)
d: "Reset the workstation to factory settings and restore from backup immediately" (incorrect — restoring immediately destroys all forensic evidence and violates TSA incident notification requirements; the incident must be documented and TSA notified before any restoration)
explanation: "NIST IR-4 and TSA IR requirements both require documented incident response. In OT environments, the key distinguishing principle is that operational safety takes priority over forensic completeness when a threat shows lateral movement toward safety-critical OT systems. Isolate first, collect from the isolated host. The correct order for pipeline OT: Isolate → Collect → Contain → Notify → Recover."

q-03 (complianceControls: ["TSA-IR"]):
stem: "After isolating a ransomware-affected workstation, script block logging reveals an encoded PowerShell command was the initial vector. What is the TSA-required next step before restoring the system?"
a: "Restore the workstation from the most recent backup and reconnect it to the network" (incorrect — restoring before notifying TSA destroys the forensic state of the isolated workstation; the current TSA pipeline security directive requires incident notification before any remediation that might destroy evidence)
b: "Notify TSA within the required timeframe and preserve the isolated workstation as a forensic artifact until the investigation is complete" (correct — the current TSA pipeline security directive requires prompt notification of cybersecurity incidents; the notification must include the nature of the incident, affected systems, and containment actions taken; the isolated workstation must be preserved — not restored — until TSA notification has been made and the investigation allows)
c: "Run a malware scan on the isolated workstation and confirm it is clean before notifying TSA" (incorrect — running a malware scan modifies the system state and may destroy forensic artifacts; notification to TSA must occur based on the known incident facts, not contingent on completing internal cleanup; notification first, then investigation)
d: "Submit an incident report internally to IT management only — TSA notification is not required for ransomware that did not affect pipeline operations" (incorrect — the current TSA pipeline security directive requires notification regardless of whether the incident directly disrupted pipeline operations; a cybersecurity incident on a pipeline DMZ workstation with lateral movement toward OT systems meets the notification threshold)
explanation: "The current TSA pipeline security directive imposes a mandatory incident notification requirement when a cybersecurity incident is confirmed on pipeline systems. Notification must occur within the required timeframe — not after internal cleanup is complete. Preserve the isolated workstation as forensic evidence. Notify TSA with: what happened, when, which systems, containment actions, and operational impact."

**FILE: exercises/01.json**
id: "incident-response-ex-01"
moduleId: "incident-response"
title: "Evidence Collection and System Isolation"
description: "Use PowerShell to collect process, network connection, and Security event log evidence from PIPELINE-DC01, then isolate the system from the network after evidence collection is complete."
complianceControls: ["TSA-IR", "NIST-AU-12"]
context: "You are logged into PIPELINE-DC01 as a domain administrator for ExampleCorp. The EDR has alerted to suspicious file activity on this workstation. You need to collect evidence (process state, network connections, logon events) before isolating the system. C:\\Evidence\\ already exists."

4 steps using EXACTLY the D-06 commands:

step-1:
instruction: "Capture the top 10 CPU-consuming processes as the first evidence snapshot. Show Name, Id, CPU, and Path for each."
hint: "Use Get-Process piped to Sort-Object CPU with -Descending, then Select-Object -First 10 with the four fields."
hintPatterns:
  - pattern: "Get-Process\\s+-Name|Get-Process\\s+\\*" → hint: "Close — use Get-Process without a name filter to get all processes, then pipe to Sort-Object CPU -Descending and Select-Object -First 10 Name, Id, CPU, Path."
  - pattern: "tasklist|task\\s+list" → hint: "tasklist shows processes but is not a PowerShell cmdlet. Use Get-Process | Sort-Object CPU -Descending | Select-Object -First 10 Name, Id, CPU, Path for structured PS 5.1 output."
expectedCommands:
  - pattern: "Get-Process.*Sort-Object.*CPU|Get-Process.*cpu"
  - matchType: regex, caseSensitive: false
successOutput: "Name                     Id       CPU Path\n----                     --       --- ----\ncrypto_worker.exe      4512    94.23 C:\\Users\\Public\\AppData\\Temp\\crypto_worker.exe\nCPUMiner.exe           3908    87.11 C:\\ProgramData\\svchost_upd\\CPUMiner.exe\nsvchost                1024    12.50 C:\\Windows\\System32\\svchost.exe\nCSRSS                   888     8.90 C:\\Windows\\System32\\csrss.exe\nexplorer               2340     3.20 C:\\Windows\\explorer.exe\ndwm                    1892     2.10 C:\\Windows\\System32\\dwm.exe\nSearchIndexer          2648     1.90 C:\\Windows\\System32\\SearchIndexer.exe\nspoolsv                1544     0.80 C:\\Windows\\System32\\spoolsv.exe\nRdpCoreTS              3120     0.50 C:\\Windows\\System32\\RdpCoreTS.dll\ntaskhostw              2144     0.30 C:\\Windows\\System32\\taskhostw.exe"
feedbackOnWrong: "Run: Get-Process | Sort-Object CPU -Descending | Select-Object -First 10 Name, Id, CPU, Path"

step-2:
instruction: "Capture all established TCP connections to remote addresses outside the 10.x.x.x range. Show LocalPort, RemoteAddress, RemotePort, and OwningProcess."
hint: "Use Get-NetTCPConnection -State Established piped to Where-Object filtering RemoteAddress not starting with '10.', then Select-Object the four fields."
hintPatterns:
  - pattern: "netstat" → hint: "netstat -ano shows similar data but is not a PowerShell cmdlet. Use Get-NetTCPConnection -State Established | Where-Object {$_.RemoteAddress -notlike '10.*'} for PS 5.1 structured output."
  - pattern: "Get-NetTCPConnection\\s+-State\\s+Listen|Get-NetTCPConnection\\s*$" → hint: "Close — use -State Established (not Listen) to see active connections, and add Where-Object {$_.RemoteAddress -notlike '10.*'} to filter for external IPs only."
expectedCommands:
  - pattern: "Get-NetTCPConnection.*Established.*notlike|Get-NetTCPConnection.*notlike.*Established|Get-NetTCPConnection.*10\\.\\*"
  - matchType: regex, caseSensitive: false
successOutput: "LocalPort RemoteAddress    RemotePort OwningProcess\n--------- -------------    ---------- -------------\n    49232 203.0.113.47            443          4512\n    49288 198.51.100.22           8080         4512\n    49301 192.0.2.15              4444         3908"
feedbackOnWrong: "Run: Get-NetTCPConnection -State Established | Where-Object {$_.RemoteAddress -notlike '10.*'} | Select-Object LocalPort, RemoteAddress, RemotePort, OwningProcess"

step-3:
instruction: "Export the last 50 Security event log logon events (Event ID 4624) to a CSV file at C:\\Evidence\\logons.csv. Use -NoTypeInformation to omit the PS type header."
hint: "Use Get-WinEvent with -LogName Security and -MaxEvents 50, pipe to Where-Object filtering Id -eq 4624, pipe to Select-Object TimeCreated, Message, then Export-Csv with the path and -NoTypeInformation flag."
hintPatterns:
  - pattern: "Get-EventLog.*Security" → hint: "Get-EventLog is deprecated in newer PS versions. Use Get-WinEvent -LogName Security -MaxEvents 50 | Where-Object {$_.Id -eq 4624} | Select-Object TimeCreated, Message | Export-Csv 'C:\\Evidence\\logons.csv' -NoTypeInformation"
  - pattern: "Export-Csv(?!.*NoTypeInformation)|Out-File.*\\.csv" → hint: "Close — add -NoTypeInformation flag to Export-Csv to omit the PS type header row from the CSV file, making it readable by non-PowerShell investigators."
expectedCommands:
  - pattern: "Export-Csv.*Evidence.*logons|Get-WinEvent.*4624.*Export-Csv"
  - matchType: regex, caseSensitive: false
successOutput: "\"TimeCreated\",\"Message\"\n\"05/16/2026 03:41:22\",\"An account was successfully logged on...\"\n\"05/16/2026 03:39:08\",\"An account was successfully logged on...\"\n\"05/16/2026 02:15:44\",\"An account was successfully logged on...\"\n\nFile written to C:\\Evidence\\logons.csv (3 records exported)"
feedbackOnWrong: "Run: Get-WinEvent -LogName Security -MaxEvents 50 | Where-Object {$_.Id -eq 4624} | Select-Object TimeCreated, Message | Export-Csv 'C:\\Evidence\\logons.csv' -NoTypeInformation"

step-4:
instruction: "Evidence collection is complete. Isolate this system by disabling the Ethernet network adapter. This severs the connection to prevent further spread. Confirm: this is run AFTER evidence collection."
hint: "Use Disable-NetAdapter with -Name 'Ethernet' and -Confirm:$false to skip the confirmation prompt."
hintPatterns:
  - pattern: "Disable-NetAdapter(?!.*-Confirm)|Disable-NetAdapter.*-Confirm:true" → hint: "Add -Confirm:$false to suppress the interactive confirmation prompt: Disable-NetAdapter -Name 'Ethernet' -Confirm:$false"
  - pattern: "netsh.*interface.*disable|ipconfig.*release" → hint: "netsh and ipconfig can disable network interfaces but are not PowerShell cmdlets. Use Disable-NetAdapter -Name 'Ethernet' -Confirm:$false for the PS 5.1 approach."
expectedCommands:
  - pattern: "Disable-NetAdapter.*Ethernet|Disable-NetAdapter.*-Name"
  - matchType: regex, caseSensitive: false
successOutput: "WARNING: Disabling network adapter 'Ethernet' will disconnect this session if connected remotely.\n\nName     InterfaceDescription                    ifIndex Status\n----     --------------------                    ------- ------\nEthernet Intel(R) 82574L Gigabit Network Connection   4 Disabled"
feedbackOnWrong: "Run: Disable-NetAdapter -Name 'Ethernet' -Confirm:$false — note: this disconnects the network adapter. In the simulator, it is safe to run; on a live system, ensure evidence collection is complete first."

**FILE: scenarios/01.json**
Use EXACTLY the D-09 scenario design:
id: "incident-response-scenario-01"
moduleId: "incident-response"
title: "Ransomware Indicator on Pipeline DMZ Workstation"
complianceControls: ["TSA-IR", "NIST-IR-4"]
narrative: "At 14:22 UTC, an EDR alert fires on a workstation in the ExampleCorp pipeline DMZ. The alert signature is 'Possible Ransomware: Unusual file encryption activity detected.' A secondary alert follows within 90 seconds: 'Lateral movement attempt detected — target: 10.0.0.0/24 historian VLAN.' PIPELINE-DC01 and the historian server are on the 10.0.0.0/24 segment. The DMZ workstation is a Windows engineering workstation used by a remote OT vendor for periodic system access.\n\nIn OT environments: the historian VLAN (10.0.0.0/24) contains real-time process data from SCADA and DCS systems. Ransomware that reaches the historian can encrypt time-series data and historian configuration, causing loss of operational visibility. Loss of historian data does not stop the pipeline, but it disables anomaly detection and compliance logging simultaneously. Pipeline safety instrumented systems (SIS) on the same segment face a higher risk: ransomware that reaches a SIS workstation can affect safety function availability."

Phase 1 ("Triage") — isFinal: false, both options nextPhaseId: "phase-2":
title: "Triage"
prompt: "The EDR shows file encryption activity on the DMZ workstation and lateral movement attempts toward the historian VLAN (10.0.0.0/24). What is your first action?"
opt-a (correct): "Immediately isolate the DMZ workstation from the network using Disable-NetAdapter to stop lateral movement toward the historian VLAN."
  outcome: "Correct. In OT incident response, the threat of ransomware reaching the historian VLAN — and potentially the safety instrumented systems on 10.0.0.0/24 — outweighs the forensic loss from immediate isolation. You can collect evidence from the isolated workstation after containment. The correct OT IR priority: operational safety first, forensic completeness second when spread to safety-critical systems is the active threat."
opt-b (incorrect): "Collect a complete forensic disk image of the workstation before taking any containment action to preserve maximum evidence."
  outcome: "A full forensic disk image takes 30–90 minutes. During that time, active lateral movement toward the historian VLAN and SIS systems continues. In an IT-only environment, this might be acceptable. In a pipeline OT environment, allowing active ransomware to remain connected to the historian VLAN while you collect evidence is an unacceptable operational risk. Isolate first, then collect evidence from the isolated system."

Phase 2 ("Reporting") — isFinal: true, both options nextPhaseId: null:
title: "Reporting"
prompt: "The workstation is isolated. Script block logging in PowerShell/Operational on the isolated host reveals an encoded PowerShell command was the initial attack vector — the attacker's script disabled Windows Defender before executing the ransomware payload. What is the correct next step?"
opt-a (correct): "Notify TSA within the required reporting timeframe with the known incident details, and preserve the isolated workstation as a forensic artifact without restoring or modifying it until the investigation concludes."
  outcome: "Correct. The current TSA pipeline security directive requires prompt notification of cybersecurity incidents affecting pipeline systems. The notification must include: affected systems, detection time, the nature of the incident (ransomware with encoded PS vector), containment actions taken (network isolation), and operational impact. The isolated workstation must be preserved — restoring it before TSA notification and investigation completion destroys the forensic evidence of the encoded PS command vector."
opt-b (incorrect): "Restore the workstation from the most recent backup immediately to minimize downtime, then submit an internal incident report to IT management."
  outcome: "Restoring the workstation immediately destroys the forensic evidence — including the PowerShell script block logs that identified the attack vector. The current TSA pipeline security directive requires incident notification before remediation that would destroy evidence. An internal IT incident report does not satisfy the TSA reporting requirement. The correct sequence: Notify TSA first. Preserve the isolated system. Restore only after TSA notification has been made and the investigation allows."
  </action>

  <verify>
    <automated>
      node -e "
const fs=require('fs');
const quiz=JSON.parse(fs.readFileSync('public/data/modules/incident-response/quizzes/01.json','utf8'));
if(quiz.questions.length!==3) throw new Error('quiz: expected 3 questions, got '+quiz.questions.length);
quiz.questions.forEach((q,i)=>{
  if(q.answers.length!==4) throw new Error('q-0'+(i+1)+': expected 4 answers');
  const correct=q.answers.filter(a=>a.correct);
  if(correct.length!==1) throw new Error('q-0'+(i+1)+': expected exactly 1 correct answer');
});
const ex=JSON.parse(fs.readFileSync('public/data/modules/incident-response/exercises/01.json','utf8'));
if(ex.steps.length!==4) throw new Error('exercise: expected 4 steps, got '+ex.steps.length);
const sc=JSON.parse(fs.readFileSync('public/data/modules/incident-response/scenarios/01.json','utf8'));
if(sc.id!=='incident-response-scenario-01') throw new Error('scenario id wrong: '+sc.id);
const p1=sc.phases.find(p=>p.id==='phase-1');
const p2=sc.phases.find(p=>p.id==='phase-2');
if(p1.isFinal!==false) throw new Error('phase-1 must have isFinal:false');
if(p2.isFinal!==true) throw new Error('phase-2 must have isFinal:true');
p1.options.forEach(o=>{if(o.nextPhaseId!=='phase-2') throw new Error('phase-1 option '+o.id+' nextPhaseId must be phase-2');});
p2.options.forEach(o=>{if(o.nextPhaseId!==null) throw new Error('phase-2 option '+o.id+' nextPhaseId must be null');});
console.log('All schema assertions passed');
"
    </automated>
  </verify>

  <acceptance_criteria>
    - public/data/modules/incident-response/quizzes/01.json: id is "incident-response-quiz-01", has exactly 3 questions, each question has exactly 4 answers (a/b/c/d), exactly 1 correct:true per question, each correct answer feedback starts with "Correct.", complianceControls uses NIST-AU-12, NIST-IR-4, or TSA-IR (not generic "NIST")
    - public/data/modules/incident-response/exercises/01.json: id is "incident-response-ex-01", has exactly 4 steps, step-1 expectedCommands pattern matches Get-Process and Sort-Object CPU, step-2 matches Get-NetTCPConnection with notlike or 10.*, step-3 matches Export-Csv and Evidence and logons, step-4 matches Disable-NetAdapter, all steps have matchType "regex" and caseSensitive false, successOutput strings use \n for newlines, step-3 successOutput contains "C:\\Evidence\\logons.csv"
    - public/data/modules/incident-response/scenarios/01.json: id is "incident-response-scenario-01", exactly 2 phases, phase-1 isFinal:false both options nextPhaseId:"phase-2", phase-2 isFinal:true both options nextPhaseId:null, exactly 1 correct:true per phase, phase-1 CORRECT option is isolate immediately (not collect forensics first — per D-09 OT safety priority), narrative contains "In OT environments"
    - node -e validation script exits 0 with "All schema assertions passed"
    - npm test exits 0 (167 tests green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages CDN → browser | Static incident-response content files served over HTTPS read-only |
| browser fetch → engines | quiz-engine.js, terminal-engine.js, scenario-view.js apply esc() to all JSON string fields before DOM insertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | Static incident-response content files | accept | Version-controlled in git; served read-only via GitHub Pages CDN; no write path |
| T-07-10 | Information Disclosure | Exercise context mentioning C:\Evidence\, logons.csv | accept | C:\Evidence\ is a generic, fictional evidence path used for training illustration; no PII, no real system data |
| T-07-11 | Injection (XSS) | Quiz/exercise/scenario JSON string fields | accept | esc() applied at render time by quiz-engine, terminal-engine, scenario-view; content does not embed HTML tags |
| T-07-12 | Spoofing | Incident response guidance framed as regulatory requirement | mitigate | Task actions explicitly require verbatim NERC CIP disclaimer in intro.md and ir-procedures.md; TSA notification requirements described using "the current TSA pipeline security directive" (no hardcoded version string that would become stale) |
</threat_model>

<verification>
After both tasks complete:

1. Run `npm test` — all 167 tests must pass
2. Run the node schema validation script from Task 2's verify block
3. Grep check: `grep -r "\[!OT\]" public/data/modules/incident-response/lessons/` — must return 3 matches
4. Grep check: `grep -r "NERC CIP governs electric utilities" public/data/modules/incident-response/lessons/` — must return at least 2 matches (intro: CIP-008, ir-procedures: CIP-008)
5. Grep check: `grep -rn "scenarioId\|exerciseId" public/data/modules/incident-response/lessons/` — must return 0 matches
6. Verify JSON parses: `node -e "require('./public/data/modules/incident-response/quizzes/01.json'); require('./public/data/modules/incident-response/exercises/01.json'); require('./public/data/modules/incident-response/scenarios/01.json'); console.log('JSON valid')"`
7. Verify scenario Phase 1 correct option is isolation (not forensic collection) — this is the key OT-specific teaching point per D-09
</verification>

<success_criteria>
- 6 files exist under public/data/modules/incident-response/
- All three lesson files have correct frontmatter (lessonId, moduleId, order, complianceTags, complianceControls)
- ir-procedures.md has quizId: '01' in frontmatter; intro.md and ps-ir.md do not
- All lesson files contain exactly one > [!OT] callout block
- intro.md and ir-procedures.md both contain the verbatim NERC CIP disclaimer with CIP-008
- Exercise step-3 uses C:\Evidence\logons.csv path (from D-06)
- Exercise step-4 uses Disable-NetAdapter with OT safety teaching note
- Quiz has 3 questions, 4 answers each, 1 correct per question
- Scenario id is incident-response-scenario-01, phase-1 correct option is isolate-immediately (OT safety priority per D-09), phase-2 correct option is notify TSA + preserve workstation
- npm test exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-module-content-mod-02-mod-03-mod-04/07-03-SUMMARY.md`
</output>
