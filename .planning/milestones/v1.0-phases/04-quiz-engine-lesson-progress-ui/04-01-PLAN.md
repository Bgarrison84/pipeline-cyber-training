---
phase: 04-quiz-engine-lesson-progress-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/utils/icons.js
  - src/modules-config.js
  - public/data/modules/logging-auditing/lessons/audit-policies.md
  - public/data/modules/logging-auditing/quizzes/01.json
  - tests/quiz-engine.test.js
  - tests/module-view.test.js
  - tests/sidebar.test.js
autonomous: true
requirements:
  - ASSESS-01
  - SHELL-03

must_haves:
  truths:
    - "CheckCircle and XCircle are exported from icons.js and included in the createIcons() call"
    - "The audit-policies lesson entry in MODULES has quizId: '01'"
    - "audit-policies.md frontmatter declares quizId: '01'"
    - "quiz 01.json has no stale 'status' field"
    - "tests/quiz-engine.test.js exists with failing (RED) stubs covering ASSESS-01 behaviors"
    - "tests/module-view.test.js exists with failing (RED) stubs covering SHELL-03 behaviors"
    - "tests/sidebar.test.js has new describe blocks with stubs for progress bar injection and refreshSidebarProgress"
  artifacts:
    - path: "src/utils/icons.js"
      provides: "CheckCircle and XCircle available to activateIcons()"
      contains: "CheckCircle, XCircle"
    - path: "src/modules-config.js"
      provides: "quizId field on audit-policies lesson entry"
      contains: "quizId: '01'"
    - path: "public/data/modules/logging-auditing/lessons/audit-policies.md"
      provides: "quizId frontmatter declaration"
      contains: "quizId: '01'"
    - path: "tests/quiz-engine.test.js"
      provides: "RED test stubs for quiz engine"
      exports: []
    - path: "tests/module-view.test.js"
      provides: "RED test stubs for module-view progress"
      exports: []
  key_links:
    - from: "src/utils/icons.js"
      to: "quiz-engine.js (Wave 1)"
      via: "activateIcons() includes CheckCircle/XCircle"
      pattern: "CheckCircle.*XCircle"
    - from: "src/modules-config.js"
      to: "computeModuleProgress() (Wave 1)"
      via: "lesson.quizId field drives quiz-vs-visited logic in D-07 formula"
      pattern: "quizId.*01"
---

<objective>
Wave 0 prerequisites for Phase 4. This plan fixes two verified gaps that would cause silent failures in later waves, adds quizId frontmatter to the one lesson that has a quiz, cleans up the stale quiz JSON field, and creates RED (failing) test scaffolds so that Wave 1 implementation tasks can follow TDD cycles.

Purpose: Without these prerequisites, Wave 1 quiz icons would silently fail to render (CheckCircle/XCircle missing from icons.js), and the progress formula would miscount completions (quizId missing from modules-config.js). The test scaffolds ensure feedback latency stays under 10 seconds per the Nyquist contract.

Output: Updated src/utils/icons.js, src/modules-config.js, audit-policies.md, 01.json, plus three test files (two new, one extended) that are all in RED state awaiting Wave 1 implementation.
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\04-quiz-engine-lesson-progress-ui\04-VALIDATION.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\ROADMAP.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\04-quiz-engine-lesson-progress-ui\04-CONTEXT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\04-quiz-engine-lesson-progress-ui\04-RESEARCH.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\04-quiz-engine-lesson-progress-ui\04-PATTERNS.md

<interfaces>
<!-- Key interfaces Wave 0 must be aware of — extracted from codebase. -->

From src/utils/icons.js (current — MODIFY to add CheckCircle, XCircle):
```javascript
import {
  createIcons,
  BookOpen, Shield, Users, AlertTriangle, Wrench,
  ChevronLeft, Copy, Check, AlertCircle,
} from 'lucide';

export function activateIcons() {
  createIcons({
    icons: { BookOpen, Shield, Users, AlertTriangle, Wrench, ChevronLeft, Copy, Check, AlertCircle },
  });
}
```

From src/modules-config.js (current shape of logging-auditing lessons — MODIFY audit-policies entry):
```javascript
lessons: [
  { id: 'intro',          title: 'Introduction to Windows Event Logs' },
  { id: 'ps-logging',     title: 'Enabling PowerShell Script Block Logging' },
  { id: 'audit-policies', title: 'Configuring Audit Policies via Group Policy' },
],
```

From tests/sidebar-progress.test.js (vi.mock hoisting pattern — reference for new test files):
```javascript
// vi.mock() calls must appear BEFORE imports that depend on the mocked modules.
vi.mock('../src/progress-store.js', () => ({ progressStore: { ... } }))
vi.mock('../src/utils/icons.js', () => ({ activateIcons: vi.fn() }))
vi.mock('../src/modules-config.js', () => ({ MODULES: [...] }))
// THEN import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix icons.js + modules-config.js + audit-policies.md + clean 01.json</name>
  <files>
    src/utils/icons.js
    src/modules-config.js
    public/data/modules/logging-auditing/lessons/audit-policies.md
    public/data/modules/logging-auditing/quizzes/01.json
  </files>
  <read_first>
    - src/utils/icons.js — current import list and createIcons() call
    - src/modules-config.js — current logging-auditing lessons array shape (lines 15-19)
    - public/data/modules/logging-auditing/lessons/audit-policies.md — current frontmatter block (lines 1-8)
    - public/data/modules/logging-auditing/quizzes/01.json — full file (contains stale "status": "placeholder" field)
  </read_first>
  <behavior>
    - icons.js: CheckCircle and XCircle are importable from 'lucide' and included in createIcons()
    - modules-config.js: only the audit-policies lesson entry in logging-auditing gets quizId: '01'; all other lesson entries across all modules remain unchanged
    - audit-policies.md: frontmatter gains quizId: '01' as a new line after the complianceControls line; all other frontmatter lines unchanged
    - 01.json: the top-level "status": "placeholder" field is removed; all question/answer data is unchanged
  </behavior>
  <action>
    1. In src/utils/icons.js: add CheckCircle and XCircle to the named import from 'lucide' and to the icons object passed to createIcons(). Place them on the same line as the other imports — do not restructure the file. New import line: `ChevronLeft, Copy, Check, AlertCircle, CheckCircle, XCircle,`. New icons object entry: `CheckCircle, XCircle` appended after AlertCircle.

    2. In src/modules-config.js: change only the audit-policies entry in the logging-auditing module lessons array from `{ id: 'audit-policies', title: 'Configuring Audit Policies via Group Policy' }` to `{ id: 'audit-policies', title: 'Configuring Audit Policies via Group Policy', quizId: '01' }`. Touch no other lesson entries.

    3. In public/data/modules/logging-auditing/lessons/audit-policies.md: add `quizId: '01'` as a new YAML frontmatter line immediately after the complianceControls line. The parseFrontmatter() function in content-loader.js passes all YAML fields to meta — no parser changes needed.

    4. In public/data/modules/logging-auditing/quizzes/01.json: remove the `"status": "placeholder"` field from the top-level object. The field is not read by any code; its removal is a cleanup only. Preserve all questions, answers, feedback, and explanation text exactly.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/icons.js imports CheckCircle and XCircle from 'lucide' (grep: `grep -c "CheckCircle" src/utils/icons.js` returns 2 — one import, one in icons object)
    - src/modules-config.js audit-policies entry has quizId: '01' (grep: `grep -c "quizId" src/modules-config.js` returns 1)
    - No other lesson entry in modules-config.js has a quizId field
    - audit-policies.md frontmatter contains the line `quizId: '01'`
    - 01.json does not contain the string "placeholder"
    - npm test exits 0 (existing tests remain green; no new test files yet in this task)
  </acceptance_criteria>
  <done>Four files updated. All existing tests remain green. CheckCircle and XCircle are registered with Lucide. quizId field is present on the audit-policies lesson config entry and on the lesson frontmatter.</done>
</task>

<task type="auto">
  <name>Task 2: Create RED test scaffolds — quiz-engine.test.js, module-view.test.js, sidebar.test.js extensions</name>
  <files>
    tests/quiz-engine.test.js
    tests/module-view.test.js
    tests/sidebar.test.js
  </files>
  <read_first>
    - tests/sidebar-progress.test.js — vi.mock hoisting pattern, beforeEach DOM setup, fetch mock with vi.stubGlobal, afterEach cleanup (vi.unstubAllGlobals, vi.clearAllMocks)
    - tests/lesson-view.test.js — synchronous string-return test pattern for renderModule()
    - tests/sidebar.test.js — existing file structure to understand where to append new describe blocks (must add vi.mock declarations at top of file before existing imports)
    - public/data/modules/logging-auditing/quizzes/01.json — quiz data shape to use in fetch mock
  </read_first>
  <action>
    Create tests/quiz-engine.test.js as a new file. The file covers ASSESS-01 and SHELL-03. All test bodies that test src/quiz-engine.js (which does not exist yet) must use it.todo() or throw new Error('RED') so they fail until Wave 1 creates quiz-engine.js. Tests that only call computeModuleProgress or renderQuiz should fail with module-not-found errors until those exports exist.

    Structure for tests/quiz-engine.test.js:
    - vi.mock declarations FIRST (before vitest imports): mock '../src/progress-store.js', '../src/utils/icons.js', '../src/modules-config.js' using the exact patterns from sidebar-progress.test.js. The MODULES mock should include only the logging-auditing module with lessons: intro (no quizId), audit-policies (quizId: '01').
    - vi.stubGlobal('fetch', ...) in beforeEach returning a resolved quiz JSON matching the 01.json shape (one question, four answers, explanation).
    - vi.unstubAllGlobals() + vi.clearAllMocks() in afterEach.
    - DOM setup in beforeEach: document.body.innerHTML with a div#app containing div.lesson-wrapper > div.lesson-column.
    - describe blocks covering:
      a) renderQuiz — first-visit mode: injects .quiz-section into .lesson-column; question stem appears in DOM; answer buttons (.quiz-answer-btn) are present; activateIcons() is called once after injection.
      b) renderQuiz — answer click behavior: clicking a .quiz-answer-btn sets data-answered="true" on the parent .quiz-question-card; all answer buttons on that question get a disabled-looking attribute or class; the clicked answer's feedback text appears in the DOM; the question explanation appears in the DOM.
      c) renderQuiz — score save: when all questions are answered, progressStore.saveQuiz() is called with (moduleId, quizId, {score: number, total: number}); progressStore.markLessonCompleted() is called.
      d) renderQuiz — revisit mode: when progressStore.getQuizScore returns {score:1, total:1, attemptedAt:'2026-05-14T00:00:00.000Z'}, the rendered DOM contains the text 'Your last attempt:' and '1/1'; no click handlers are attached (clicking answer buttons does not call saveQuiz).
      e) computeModuleProgress — returns {pct:0, complete:false} when all lessons are unvisited and no quiz score exists; returns {pct:100, complete:true} when the quiz-having lesson has a score; returns {pct:50, complete:false} when 1 of 2 lessons contributes (quiz-less intro is visited, quiz-having audit-policies has no score yet).

    Create tests/module-view.test.js as a new file. Covers SHELL-03 for the module-view.
    - vi.mock declarations FIRST: mock '../src/progress-store.js', '../src/quiz-engine.js' (computeModuleProgress returns {pct:0, complete:false, numerator:0, denominator:2}), '../src/modules-config.js' (logging-auditing module with two lessons: intro no quizId, audit-policies quizId:'01'), '../src/badge.js' (renderBadge returns '<span>TSA</span>'), '../src/utils/escape.js' (esc returns its argument unchanged for test simplicity — vi.fn().mockImplementation(s => s)).
    - import renderModule from '../src/views/module-view.js'.
    - describe blocks covering:
      a) renderModule({moduleId:'logging-auditing'}) returns a string containing a progress bar element (look for 'sidebar-progress-bar' or a width style pattern like 'width:0%').
      b) renderModule returns a string containing each lesson title ('Introduction to Windows Event Logs', 'Configuring Audit Policies via Group Policy').
      c) With getQuizScore returning {score:1,total:1,attemptedAt:...}, lesson audit-policies row contains 'quiz-passed'.
      d) With getLessonProgress returning {visited:true,completed:true} for intro and getQuizScore returning null for audit-policies, audit-policies row contains 'unvisited' and intro row contains 'visited'.
      e) renderModule({moduleId:'does-not-exist'}) returns a string containing 'Module not found'.
      f) computeModuleProgress mock is called with the logging-auditing module object when renderModule renders.

    Extend tests/sidebar.test.js by adding vi.mock declarations at the very top of the file (before the existing `import { describe, it, expect, beforeEach } from 'vitest'` line) and appending new describe blocks at the end.

    vi.mock declarations to add at top of sidebar.test.js (hoisted before imports):
    - mock '../src/progress-store.js' (same shape as sidebar-progress.test.js mock)
    - mock '../src/content-loader.js' (checkLessonAvailability returns true, other stubs)
    - mock '../src/utils/icons.js' (activateIcons: vi.fn())
    - mock '../src/quiz-engine.js' (computeModuleProgress: vi.fn().mockReturnValue({pct:0, complete:false}))

    New describe blocks to append to sidebar.test.js:
    - 'sidebar progress bars — initSidebar() injection': after initSidebar() resolves, document contains at least one element with class 'sidebar-progress-bar'; a .sidebar-progress-bar inner div has style containing 'width:0%' (or similar 0 progress); initSidebar() can be called without throwing.
    - 'sidebar progress bars — refreshSidebarProgress()': importing refreshSidebarProgress from sidebar.js and calling it with a valid moduleId does not throw; after setting computeModuleProgress to return {pct:50, complete:false} and calling refreshSidebarProgress('logging-auditing'), the .sidebar-progress-bar div for that module has width '50%'.

    All new test bodies that import from src/quiz-engine.js (which does not exist yet) will fail at import time or throw — this is the intended RED state.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/quiz-engine.test.js exists and contains describe blocks with the test names listed above
    - tests/module-view.test.js exists and contains describe blocks with the test names listed above
    - tests/sidebar.test.js now has vi.mock declarations at its top and at least 2 new describe blocks appended
    - Running npm test shows test failures only for tests that import from src/quiz-engine.js (module not found) — the three pre-existing sidebar.test.js describe blocks still pass
    - The existing test suite (compliance-refs, content-loader, router, lesson-view, main-init, sidebar-progress, router-resume, progress-store) continues to pass without regressions
  </acceptance_criteria>
  <done>Three test files updated/created. Pre-existing tests are green. New quiz-engine and module-view tests are RED (src/quiz-engine.js does not exist yet). The Nyquist contract for Phase 4 is satisfied — every behavior in the VALIDATION.md table has a corresponding test stub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → src modules | Test files mock modules to isolate units; no trust boundary concerns |
| config files → runtime | icons.js and modules-config.js are authored static config; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Tampering | public/data/modules/logging-auditing/quizzes/01.json | accept | Static authored file served from public/; no user input; removal of stale "status" field is a cosmetic cleanup |
| T-04-W0-02 | Tampering | src/modules-config.js quizId field | accept | Authored config only; quizId value '01' is a static string not derived from any URL or user input |
</threat_model>

<verification>
After completing both tasks:

1. `npm test` exits 0 — all pre-existing tests green.
2. New test files exist: `tests/quiz-engine.test.js`, `tests/module-view.test.js`.
3. `tests/sidebar.test.js` has vi.mock declarations at top and new describe blocks at bottom.
4. New tests that reference `src/quiz-engine.js` are RED (expected — Wave 1 creates that file).
5. `grep -c "CheckCircle" src/utils/icons.js` returns 2.
6. `grep -c "quizId" src/modules-config.js` returns 1.
7. `grep "quizId" public/data/modules/logging-auditing/lessons/audit-policies.md` outputs `quizId: '01'`.
8. `grep "placeholder" public/data/modules/logging-auditing/quizzes/01.json` returns nothing.
</verification>

<success_criteria>
- CheckCircle and XCircle are registered in icons.js — answer state icons will render in Wave 1.
- quizId: '01' is on the audit-policies lesson entry in modules-config.js — computeModuleProgress() will count quiz completion correctly in Wave 1.
- audit-policies.md declares quizId: '01' in frontmatter — renderLesson() will invoke quiz engine for this lesson in Wave 1.
- 01.json has no stale "status" field.
- Test scaffolds exist and are RED — Wave 1 makes them GREEN by implementing src/quiz-engine.js.
</success_criteria>

<output>
After completion, create `.planning/phases/04-quiz-engine-lesson-progress-ui/04-01-SUMMARY.md`
</output>
