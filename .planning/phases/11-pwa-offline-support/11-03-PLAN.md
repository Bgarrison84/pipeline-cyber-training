---
phase: 11-pwa-offline-support
plan: 03
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - vite.config.js
autonomous: true
requirements:
  - PWA-01
  - PWA-02

must_haves:
  truths:
    - "vite.config.js imports VitePWA from 'vite-plugin-pwa' and includes it in the plugins array"
    - "npm run build produces dist/sw.js and dist/manifest.webmanifest"
    - "dist/sw.js content includes .md and .json URL patterns (confirming globPatterns coverage)"
    - "No navigateFallback appears anywhere in vite.config.js (hash router conflict)"
    - "scope and start_url are exactly '/pipeline-cyber-training/' (not './' or relative)"
  artifacts:
    - path: "vite.config.js"
      provides: "VitePWA plugin config with correct scope, globPatterns, registerType"
      contains: "VitePWA"
    - path: "dist/sw.js"
      provides: "Generated Workbox service worker with precache manifest"
    - path: "dist/manifest.webmanifest"
      provides: "Web app manifest for PWA installability"
  key_links:
    - from: "vite.config.js VitePWA({ workbox.globPatterns })"
      to: "dist/sw.js precache manifest entries"
      via: "Workbox generateSW traverses dist/ at build time"
      pattern: "\\.md|\\.json"
    - from: "vite.config.js manifest.scope"
      to: "browser SW registration scope"
      via: "manifest.webmanifest scope field"
      pattern: "/pipeline-cyber-training/"
---

<objective>
Add the VitePWA() plugin to vite.config.js with all required configuration: globPatterns covering md/json, absolute scope/start_url, autoUpdate strategy, and the three icon entries. Run the build to confirm sw.js and manifest.webmanifest are emitted correctly.

Purpose: This is the single integration point for Workbox precaching. Getting globPatterns, scope, and start_url correct here determines whether offline mode works at all. This plan is blocked on Plan 11-01 completing (icon PNGs must exist in public/ before the build).

Output: vite.config.js with VitePWA plugin; confirmed build output containing sw.js and manifest.webmanifest with correct content.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-pwa-offline-support/11-01-SUMMARY.md
@.planning/phases/11-pwa-offline-support/11-CONTEXT.md
@.planning/phases/11-pwa-offline-support/11-RESEARCH.md
@.planning/phases/11-pwa-offline-support/11-PATTERNS.md
</context>

<interfaces>
From vite.config.js (current — 8 lines, read before editing):
```javascript
import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  base: '/pipeline-cyber-training/',
  plugins: [tailwindcss()],
})
```
The base value '/pipeline-cyber-training/' is the source of truth for scope and start_url.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add VitePWA plugin to vite.config.js</name>
  <files>vite.config.js</files>
  <read_first>
    - vite.config.js — read the current 8-line file; confirm base is '/pipeline-cyber-training/' and plugins is [tailwindcss()]; preserve both
    - .planning/phases/11-pwa-offline-support/11-PATTERNS.md — "vite.config.js" section (lines 26–88): the exact modification pattern including key constraints; complete target file structure shown
    - .planning/phases/11-pwa-offline-support/11-RESEARCH.md — Pattern 1 (lines 199–255): VitePWA plugin configuration with all required fields and cited sources; also read Anti-Patterns section (lines 425–433) for what NOT to add
    - public/ directory listing — confirm pwa-192x192.png, pwa-512x512.png, apple-touch-icon.png all exist before proceeding (they must be present for build to include them)
  </read_first>
  <action>
    Modify vite.config.js to add VitePWA import and plugin. Preserve all existing content; only additions:

    Add import at top: import { VitePWA } from 'vite-plugin-pwa'

    Expand plugins array from [tailwindcss()] to [tailwindcss(), VitePWA({...})] with this exact configuration:

    registerType: 'autoUpdate' — per D-13; autoUpdate forces skipWaiting:true and clientsClaim:true automatically without explicit workbox config

    injectRegister: 'script-defer' — injects <script defer src="registerSW.js"> into built index.html; requires NO import in main.js; explicitly set to avoid the unreliable 'auto' default (RESEARCH.md Pitfall 6)

    workbox object:
    - globPatterns: ['**/*.{js,css,html,md,json,png,ico,svg}'] — MUST include md and json (per D-12); these are absent from Workbox defaults; fetchLesson() has a silent catch block — blank offline lessons with no error if omitted
    - cleanupOutdatedCaches: true — removes stale precache entries from previous SW versions
    - DO NOT add navigateFallback — this app uses hash-based routing (#/module/:id/lesson/:n); navigateFallback is for HTML5 history API SPAs and would intercept hash navigation incorrectly (RESEARCH.md Pitfall 4)

    manifest object:
    - name: 'OT Security Lab'
    - short_name: 'OT Training'
    - description: 'Pipeline cybersecurity training — TSA SD-02 series compliance'
    - theme_color: '#111827'
    - background_color: '#1a1a1a'
    - display: 'standalone'
    - scope: '/pipeline-cyber-training/' — MUST be this exact absolute path per D-11; NOT './' (RESEARCH.md Pitfall 2 — DOMException on registration)
    - start_url: '/pipeline-cyber-training/' — same reason; must match base exactly
    - icons array with three entries: src 'pwa-192x192.png' sizes '192x192' type 'image/png'; src 'pwa-512x512.png' sizes '512x512' type 'image/png' purpose 'any maskable'; src 'apple-touch-icon.png' sizes '180x180' type 'image/png'

    After editing, run: npm run build
    Confirm build succeeds without errors.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.js imports VitePWA from 'vite-plugin-pwa' (line near top)
    - vite.config.js plugins array contains both tailwindcss() and VitePWA({...})
    - vite.config.js VitePWA config has registerType: 'autoUpdate'
    - vite.config.js VitePWA config has injectRegister: 'script-defer'
    - vite.config.js workbox.globPatterns value includes 'md' and 'json'
    - vite.config.js manifest.scope is exactly '/pipeline-cyber-training/'
    - vite.config.js manifest.start_url is exactly '/pipeline-cyber-training/'
    - vite.config.js does NOT contain 'navigateFallback' anywhere
    - npm run build exits 0 (no build errors)
    - dist/sw.js file exists after build
    - dist/manifest.webmanifest file exists after build
    - dist/sw.js contains the string '.md' or 'md' (confirming Markdown files appear in precache manifest)
    - dist/manifest.webmanifest contains '"scope":"/pipeline-cyber-training/"' or equivalent JSON
    - npm test exits 0 (205 tests still GREEN after vite.config.js change — config is build-only)
  </acceptance_criteria>
  <done>vite.config.js updated with VitePWA; npm run build produces dist/sw.js and dist/manifest.webmanifest; manifest scope is '/pipeline-cyber-training/'; npm test GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workbox SW → cached content | SW serves cached lesson/quiz/scenario files to browser; content is public training material |
| SW scope → GitHub Pages subpath | SW scope restricted to /pipeline-cyber-training/ — cannot intercept other deployments on same origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | Workbox precache manifest revision hashes | mitigate | Workbox generateSW includes content hashes in precache manifest; cleanupOutdatedCaches:true removes stale entries; attackers cannot serve poisoned cached content without matching the build hash |
| T-11-04 | Elevation of Privilege | SW scope configuration | mitigate | scope explicitly set to '/pipeline-cyber-training/' (not '/') — restricts SW to project subpath; other GitHub Pages deployments on github.io are not intercepted |
| T-11-05 | Information Disclosure | SW caches lesson content | accept | All cached content is public training material (Markdown lessons, JSON quizzes, compliance refs); no credentials, PII, or org-internal data in public/data/ |
| T-11-06 | Tampering | GitHub Pages HTTPS enforcement | accept | GitHub Pages enforces HTTPS; service workers require secure context — HTTP downgrade impossible on this host |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan; vite-plugin-pwa installed and audited in Plan 11-01 |
</threat_model>

<verification>
After the task:
1. vite.config.js contains VitePWA() in plugins array
2. globPatterns includes md and json extensions
3. scope and start_url are '/pipeline-cyber-training/'
4. navigateFallback is absent
5. npm run build exits 0
6. dist/sw.js exists and contains .md file references
7. dist/manifest.webmanifest exists with correct scope
8. npm test: 205 GREEN (vite.config.js change is build-only, no test impact)
</verification>

<success_criteria>
- vite.config.js: VitePWA plugin configured with autoUpdate, script-defer, globPatterns including md/json, scope '/pipeline-cyber-training/', no navigateFallback
- npm run build: exits 0; dist/sw.js and dist/manifest.webmanifest present
- dist/sw.js: contains .md extension (Markdown files precached)
- npm test: 205/205 GREEN
</success_criteria>

<output>
Create `.planning/phases/11-pwa-offline-support/11-03-SUMMARY.md` when done
</output>
