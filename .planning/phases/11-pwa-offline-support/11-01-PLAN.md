---
phase: 11-pwa-offline-support
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - scripts/generate-icons.js
  - public/pwa-192x192.png
  - public/pwa-512x512.png
  - public/apple-touch-icon.png
autonomous: true
requirements:
  - PWA-01
  - PWA-02

must_haves:
  truths:
    - "Three PNG icon files exist in public/ before any build runs"
    - "vite-plugin-pwa and pureimage appear as devDependencies in package.json"
    - "npm test exits 0 after package install (all 205 prior tests still pass)"
  artifacts:
    - path: "public/pwa-192x192.png"
      provides: "PWA icon for browser manifest and homescreen install"
      min_lines: 1
    - path: "public/pwa-512x512.png"
      provides: "PWA icon (any maskable purpose) for splash screens"
      min_lines: 1
    - path: "public/apple-touch-icon.png"
      provides: "iOS Safari add-to-homescreen icon (180x180)"
      min_lines: 1
    - path: "scripts/generate-icons.js"
      provides: "Reproducible dev-time icon generation; not part of build"
      min_lines: 20
  key_links:
    - from: "scripts/generate-icons.js"
      to: "public/pwa-192x192.png, public/pwa-512x512.png, public/apple-touch-icon.png"
      via: "pureimage PImage.encodePNGToStream"
      pattern: "pwa-192x192\\.png"
    - from: "package.json devDependencies"
      to: "vite.config.js VitePWA import"
      via: "npm install -D"
      pattern: "vite-plugin-pwa"
---

<objective>
Install the two new devDependencies (vite-plugin-pwa, pureimage) and generate the three PWA icon PNG files that must exist in public/ before any Workbox precache build runs.

Purpose: Workbox traverses dist/ at build time to generate the precache manifest. If the icon PNGs are absent when `npm run build` runs, Workbox silently omits them — no build error, no offline icon, and the PWA installability Lighthouse check fails. This Wave 0 plan resolves the blocker before vite.config.js touches VitePWA.

Output: package.json updated with two new devDependencies; scripts/generate-icons.js committed; three PNG files committed to public/.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-pwa-offline-support/11-CONTEXT.md
@.planning/phases/11-pwa-offline-support/11-RESEARCH.md
@.planning/phases/11-pwa-offline-support/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install devDependencies (vite-plugin-pwa, pureimage)</name>
  <files>package.json</files>
  <read_first>
    - package.json — read current devDependencies section to confirm vite-plugin-pwa and pureimage are absent before installing
  </read_first>
  <action>
    Run: npm install -D vite-plugin-pwa@1.3.0 pureimage@0.4.18

    This adds both packages to devDependencies in package.json. workbox-build@7.4.1 and workbox-window@7.4.1 are peer deps of vite-plugin-pwa and will be installed automatically by npm — do NOT install them separately (version mismatch risk).

    After install, run npm test to confirm all 205 existing tests still pass. A devDependency install must not break the existing suite.

    Do not modify package.json scripts — no icon generation step goes in the build pipeline. The script is dev-time only (run once, commit PNGs).
  </action>
  <verify>
    <automated>npm test -- --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains "vite-plugin-pwa": "^1.3.0" (or exact)
    - package.json devDependencies contains "pureimage": "^0.4.18" (or exact)
    - npm test exits 0 with 205 tests passing (no regressions from install)
    - workbox-build and workbox-window are NOT listed as direct dependencies (they are pulled in as peer deps)
  </acceptance_criteria>
  <done>vite-plugin-pwa and pureimage in package.json devDependencies; full test suite GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Write generate-icons.js and commit icon PNGs</name>
  <files>scripts/generate-icons.js, public/pwa-192x192.png, public/pwa-512x512.png, public/apple-touch-icon.png</files>
  <read_first>
    - .planning/phases/11-pwa-offline-support/11-RESEARCH.md — Pattern 4 (lines 374–423) for pureimage API: PImage.make(), img.getContext('2d'), fillStyle, fillRect, beginPath, fill, encodePNGToStream; note the WARNING about text rendering requiring font registration — use shapes only, no ctx.fillText
    - .planning/phases/11-pwa-offline-support/11-PATTERNS.md — "No Analog Found" section and "scripts/generate-icons.js" section confirming ESM syntax (package.json has "type":"module"), shapes-only design, output paths
    - src/style.css — CSS variable values: --color-accent is #f97316 (orange), --color-bg-secondary is #2a2a2a; icon background is #111827 (--color-bg-base for darker feel per D-14)
  </read_first>
  <action>
    Create the scripts/ directory if it does not exist, then create scripts/generate-icons.js.

    The script is ESM (import syntax — package.json "type":"module"). It generates three PNGs using pureimage with a shapes-only design (NO text rendering — pureimage requires font registration for ctx.fillText; omitting font causes silent failure or garbled output). Use:
    - Background fill: #111827 (dark navy — matches D-14)
    - Shield shape in #f97316 (orange accent — matches D-14)
    - Shield: a polygon drawn with ctx.beginPath/moveTo/lineTo/closePath/fill — wide at top, narrowed at bottom point

    API calls (confirmed in RESEARCH.md Pattern 4):
    - import * as PImage from 'pureimage'
    - import * as fs from 'fs'
    - const img = PImage.make(size, size)
    - const ctx = img.getContext('2d')
    - ctx.fillStyle / ctx.fillRect / ctx.beginPath / ctx.moveTo / ctx.lineTo / ctx.closePath / ctx.fill
    - await PImage.encodePNGToStream(img, fs.createWriteStream(outPath))

    Generate three files:
    - public/pwa-192x192.png (192x192)
    - public/pwa-512x512.png (512x512)
    - public/apple-touch-icon.png (180x180)

    Run the script: node scripts/generate-icons.js

    Verify all three files are created and non-zero bytes. Then run npm test again to confirm no test regressions from the new files.

    These PNG files must be committed (git add public/pwa-192x192.png public/pwa-512x512.png public/apple-touch-icon.png scripts/generate-icons.js). The generated PNGs are tracked in git — they are static assets, not build artifacts.
  </action>
  <verify>
    <automated>node scripts/generate-icons.js && ls -la public/pwa-192x192.png public/pwa-512x512.png public/apple-touch-icon.png</automated>
  </verify>
  <acceptance_criteria>
    - public/pwa-192x192.png exists and is > 1000 bytes (a valid PNG, not an empty file)
    - public/pwa-512x512.png exists and is > 1000 bytes
    - public/apple-touch-icon.png exists and is > 1000 bytes (180x180)
    - scripts/generate-icons.js uses ESM import syntax (first line is "import * as PImage from 'pureimage'")
    - scripts/generate-icons.js contains no ctx.fillText call (shapes-only — text requires font registration)
    - All three PNG paths contain 'pwa-' or 'apple-touch' prefix matching D-15 naming convention
    - npm test exits 0 after icon generation (no regressions)
    - git status shows all three PNGs and the script are tracked (not untracked after commit)
  </acceptance_criteria>
  <done>Three PNG icon files committed to public/; scripts/generate-icons.js committed; npm test GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | Third-party packages installed from npm; package legitimacy verified before install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | npm install (vite-plugin-pwa, pureimage) | mitigate | Package Legitimacy Audit in RESEARCH.md confirms both packages [OK] — vite-plugin-pwa (6yr, github.com/vite-pwa/), pureimage (11yr, github.com/joshmarinacci) |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Both packages passed slopcheck v0.6.1 on 2026-05-18; no postinstall scripts detected; no [ASSUMED] or [SUS] packages — no blocking checkpoint required |
</threat_model>

<verification>
After both tasks:
1. package.json devDependencies includes vite-plugin-pwa and pureimage
2. All three icon PNGs exist in public/ and are valid (non-zero, readable by file command)
3. npm test exits 0 (all 205 tests GREEN — devDep install must not break suite)
4. git log shows commit with icons and generate-icons.js
</verification>

<success_criteria>
- vite-plugin-pwa@1.3.0 and pureimage@0.4.18 in package.json devDependencies
- public/pwa-192x192.png, public/pwa-512x512.png, public/apple-touch-icon.png all committed and non-empty
- scripts/generate-icons.js committed (ESM, shapes-only, no ctx.fillText)
- npm test: 205/205 GREEN
</success_criteria>

<output>
Create `.planning/phases/11-pwa-offline-support/11-01-SUMMARY.md` when done
</output>
