---
phase: 10-fork-config-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/FORK-GUIDE.md
autonomous: true
requirements:
  - FORK-01

must_haves:
  truths:
    - "docs/FORK-GUIDE.md exists and is readable"
    - "Guide covers forking the repo via GitHub web UI without any git CLI"
    - "Guide covers editing fork.config.json via GitHub web UI editor"
    - "Guide covers enabling GitHub Pages via Settings → Pages"
    - "Config field reference table covers all three fields (orgName, logoPath, activeModules)"
    - "A learner can add a logo by dropping a file into the repo using the GitHub web UI"
  artifacts:
    - path: "docs/FORK-GUIDE.md"
      provides: "Step-by-step GitHub Pages deployment guide for IT admins"
      contains: "## Config Field Reference"
  key_links:
    - from: "docs/FORK-GUIDE.md"
      to: "public/fork.config.json"
      via: "guide references the file path and field names"
      pattern: "fork\\.config\\.json"
---

<objective>
Create docs/FORK-GUIDE.md — a step-by-step GitHub Pages deployment guide for IT admins who have no development toolchain.

Purpose: Fulfills FORK-01. The guide enables any IT admin at another pipeline company to deploy a branded copy of this training platform by only using the GitHub web UI. Zero git CLI. Zero Node.js.
Output: docs/FORK-GUIDE.md with numbered steps and a config field reference table.
</objective>

<execution_context>
@C:\Users\psyco\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-fork-config-system/10-CONTEXT.md

<interfaces>
<!-- Decisions governing this document (from CONTEXT.md): -->
D-14: GitHub Pages only via GitHub web UI. Zero git CLI required. Primary audience: IT admins with no development toolchain.
D-15: Format — step-by-step numbered list with exact UI paths (e.g., "In GitHub, click Settings → Pages → Source: Deploy from branch").
D-16: Includes a config field reference table: Field | Type | Example | Effect — for all three fields (orgName, logoPath, activeModules).

Deferred (do NOT include):
- Git CLI workflow section
- Non-GitHub-Pages deployment options (Azure, IIS, nginx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/FORK-GUIDE.md</name>
  <files>docs/FORK-GUIDE.md</files>
  <read_first>
    .planning/phases/10-fork-config-system/10-CONTEXT.md — D-14, D-15, D-16 govern format, scope, and content. Read the Deferred section to confirm what to exclude.
    public/fork.config.json — (will exist after Plan 01) The guide references this exact file. Once Plan 01 runs, check the committed content. If Plan 01 has not run yet, write the guide based on the known OkieOps schema: orgName, logoPath, activeModules.
    src/modules-config.js — Read the module id values so the guide can give a concrete activeModules example.
  </read_first>
  <action>
    Create docs/FORK-GUIDE.md. The audience is an IT admin at a pipeline company with a GitHub account but no development experience. Every step uses the GitHub web UI only.

    Structure (in this order):

    ## Overview
    One paragraph: what this guide does (deploy a branded copy of the pipeline cybersecurity training platform to GitHub Pages by editing one JSON file in the GitHub web UI). Mention no code changes or software installation is required.

    ## Prerequisites
    Numbered list:
    1. A GitHub account (free tier works)
    2. Access to the source repository on GitHub

    ## Step 1 — Fork the Repository
    Exact UI path numbered steps:
    1. Go to the repository page on GitHub.
    2. Click the Fork button (top-right of the page).
    3. Under "Owner", select your GitHub account or organization.
    4. Click Create fork.
    5. Wait for the fork to complete — GitHub redirects you to your new copy of the repository.

    ## Step 2 — Enable GitHub Pages
    Exact UI path:
    1. In your forked repository, click Settings (top navigation tab).
    2. In the left sidebar, click Pages.
    3. Under "Build and deployment" → Source, select Deploy from a branch.
    4. Under Branch, select main and folder / (root). Click Save.
    5. GitHub Pages begins building your site. After 1–3 minutes, a URL appears at the top of the Pages settings page (format: https://YOUR-USERNAME.github.io/pipeline-cyber-training/). This is your site URL.

    ## Step 3 — Customize Your Organization's Config
    Explain that public/fork.config.json controls org name, logo, and which modules are shown.

    Sub-section: Edit the config file via GitHub web UI:
    1. In your forked repository, navigate to public/fork.config.json.
    2. Click the pencil (Edit) icon.
    3. Replace the values for orgName and activeModules as needed (see Config Field Reference below).
    4. Click Commit changes. In the dialog, enter a short commit message (e.g., "Set org name and active modules"). Click Commit changes.
    5. GitHub Pages automatically rebuilds your site within 1–3 minutes. Refresh your site URL to see the changes.

    ## Step 4 — Add Your Organization Logo (Optional)
    Explain that logoPath in fork.config.json points to an image file in your repository's public/ folder.

    Steps:
    1. Prepare a PNG or SVG logo file. Recommended size: at least 64px tall.
    2. In your forked repository, navigate to the public/ folder.
    3. Click Add file → Upload files.
    4. Drag your logo file into the upload area (example file name: MyOrg.png).
    5. Click Commit changes.
    6. Edit public/fork.config.json (see Step 3) and set logoPath to your filename, e.g., "logoPath": "MyOrg.png".
    7. Commit the config change and wait for the site to rebuild.

    ## Step 5 — Verify Your Deployment
    1. Visit your GitHub Pages URL (found in Settings → Pages).
    2. Confirm your org name appears in the top-left header.
    3. If you added a logo, confirm the logo appears beside the org name.
    4. Confirm only the modules you listed in activeModules are visible in the sidebar and home page.
    5. Complete one lesson to confirm the training content is functional.

    ## Config Field Reference
    Markdown table with columns: Field | Type | Example | Effect

    Rows:
    - orgName | string | "OkieOps" | Appears in the top-left header and browser tab title
    - logoPath | string or null | "OkieOps.png" | Path to an image file in public/. Set to null for text-only header. Image is displayed at 32px height beside the org name.
    - activeModules | array of strings | ["logging-auditing", "network-hardening"] | Which modules are visible in the sidebar and home page. Omitted modules are completely hidden. Available IDs: logging-auditing, network-hardening, account-access, incident-response, patch-management

    ## Troubleshooting
    Short section with 3-4 common issues:
    - Site shows 404 after enabling Pages: wait up to 5 minutes for initial build; check Settings → Pages for build status
    - Logo not appearing: confirm the image file is in public/ (not the repo root); confirm logoPath in fork.config.json matches the filename exactly (case-sensitive)
    - Module not appearing in sidebar: confirm the module ID in activeModules exactly matches one of the five valid IDs listed in the Config Field Reference
    - Changes not reflected: GitHub Pages rebuild takes 1–3 minutes; hard-refresh your browser (Ctrl+Shift+R / Cmd+Shift+R)

    Format rules:
    - Use numbered lists for steps, not bullets
    - Every UI click must have an exact label in quotes or bold: "Click **Settings** → **Pages** → **Source: Deploy from branch**"
    - No git CLI commands anywhere in the document
    - No Node.js, npm, or build tool instructions
    - No mention of NERC CIP as mandatory (it is out of scope entirely for this guide)
  </action>
  <verify>
    <automated>grep -c "## Config Field Reference" docs/FORK-GUIDE.md && grep -c "fork.config.json" docs/FORK-GUIDE.md && grep -c "activeModules" docs/FORK-GUIDE.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/FORK-GUIDE.md exists
    - File contains "## Config Field Reference" heading
    - File contains a markdown table with columns Field, Type, Example, Effect
    - File contains all three field names: orgName, logoPath, activeModules
    - File contains the five valid module ID values: logging-auditing, network-hardening, account-access, incident-response, patch-management
    - File references "fork.config.json" by name at least twice
    - File contains GitHub Pages UI path: "Settings" and "Pages" appear as step text
    - No git CLI commands (no "git clone", "git push", "git commit" as commands)
    - No Node.js or npm references
    - File has a Troubleshooting section
  </acceptance_criteria>
  <done>docs/FORK-GUIDE.md exists with step-by-step GitHub web UI instructions, config field reference table, and troubleshooting section. An IT admin with a GitHub account can deploy a branded fork by following it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → docs/FORK-GUIDE.md | Static Markdown document; no executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | FORK-GUIDE.md | accept | Document is intentionally public; contains no secrets, credentials, or sensitive paths |
| T-10-02-02 | Tampering | FORK-GUIDE.md content accuracy | mitigate | Reference exact GitHub UI labels as they existed at authoring time; note that GitHub may update UI; instruct users to check GitHub Docs if labels differ |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
docs/FORK-GUIDE.md passes all acceptance_criteria checks above.
Word count reasonable for a step-by-step guide: wc -w docs/FORK-GUIDE.md should be 400–1200 words.
</verification>

<success_criteria>
- docs/FORK-GUIDE.md exists with all five required sections
- Config field reference table covers all three fields with Type, Example, and Effect columns
- No CLI instructions; GitHub web UI only throughout
</success_criteria>

<output>
Create .planning/phases/10-fork-config-system/10-02-SUMMARY.md when done.
</output>
