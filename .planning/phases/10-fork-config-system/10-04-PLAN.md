---
phase: 10-fork-config-system
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/sidebar.js
  - src/views/home-view.js
  - src/views/completion-summary-view.js
autonomous: true
requirements:
  - FORK-03

must_haves:
  truths:
    - "sidebar.js renders only modules in getForkConfig().activeModules — inactive modules have no sidebar entry"
    - "home-view.js renders only active module cards — inactive modules have no home page card"
    - "completion-summary-view.js overall percentage denominator equals the count of active modules"
    - "computeModuleProgress() in quiz-engine.js is NOT filtered — it is called with individual full module objects as before"
    - "A 2-active-module fork with both modules complete shows 100% overall progress"
  artifacts:
    - path: "src/sidebar.js"
      provides: "Sidebar filtered to active modules"
      contains: "getForkConfig"
    - path: "src/views/home-view.js"
      provides: "Home page filtered to active module cards"
      contains: "getForkConfig"
    - path: "src/views/completion-summary-view.js"
      provides: "Completion summary with active-module-count denominator"
      contains: "getForkConfig"
  key_links:
    - from: "src/sidebar.js"
      to: "src/fork-config.js"
      via: "import { getForkConfig } from './fork-config.js'"
      pattern: "getForkConfig"
    - from: "src/views/home-view.js"
      to: "src/fork-config.js"
      via: "import { getForkConfig } from '../fork-config.js'"
      pattern: "getForkConfig"
    - from: "src/views/completion-summary-view.js"
      to: "src/fork-config.js"
      via: "import { getForkConfig } from '../fork-config.js'"
      pattern: "getForkConfig"
---

<objective>
Apply the activeModules filter at the render layer: sidebar.js, home-view.js, and completion-summary-view.js. The filter hides inactive modules completely (no greyed-out cards, no disabled sidebar entries). The overall completion percentage denominator adjusts to reflect only active modules.

Purpose: Fulfills D-10, D-11, D-12. Filter is render-only — computeModuleProgress() in quiz-engine.js is never touched (per D-11 constraint).
Output: Updated sidebar.js (module list filtered), home-view.js (card list filtered), completion-summary-view.js (table + percentage filtered to active modules).
</objective>

<execution_context>
@C:\Users\psyco\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-fork-config-system/10-CONTEXT.md
@.planning/phases/10-fork-config-system/10-01-SUMMARY.md

<interfaces>
<!-- From src/fork-config.js (created in Plan 01): -->
export function getForkConfig(): { orgName: string, logoPath: string|null, activeModules: string[] }
-- Returns DEFAULT_FORK_CONFIG if called before loadForkConfig() has settled.
-- Always returns an object with activeModules as an array (never undefined/null).

<!-- From src/sidebar.js — current render loop (to be filtered): -->
sidebarModules.innerHTML = MODULES.map(mod => `...`).join('');
MODULES.forEach(mod => { ... computeModuleProgress(mod) ... });
-- Both MODULES.map() and MODULES.forEach() must use the filtered list.

<!-- From src/views/home-view.js — current render (to be filtered): -->
const cards = MODULES.map(mod => `...`).join('');
-- The MODULES.map() must use the filtered list.

<!-- From src/views/completion-summary-view.js — current render (to be modified): -->
const hasProgress = MODULES.some(mod => computeModuleProgress(mod).numerator > 0);
const tableHtml = buildProgressTableHtml(MODULES);     // line 189
const badgesHtml = buildControlsBadgesHtml(MODULES);   // line 190
-- These three MODULES references must use the active-module-filtered list.
-- computeModuleProgress(mod) calls inside buildProgressTableHtml/buildControlsBadgesHtml use the
   individual mod objects — these are already the filtered list and are NOT the computeModuleProgress
   function itself, so they remain correct.

<!-- D-11 constraint (CRITICAL): -->
computeModuleProgress() in quiz-engine.js must NOT be modified. It always operates on the full
MODULES array. Do not add any filtering inside quiz-engine.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter sidebar.js and home-view.js to active modules</name>
  <files>src/sidebar.js, src/views/home-view.js</files>
  <read_first>
    src/sidebar.js — Read the full current file. Identify every use of MODULES (map, forEach, find). The filter must apply before the first MODULES.map() and before the MODULES.forEach() progress bar loop. The refreshSidebarProgress() function uses MODULES.find() — this should remain unfiltered (it looks up a specific module by ID and is called after a lesson completes, so the module will always be in the full list).
    src/views/home-view.js — Read the full current file. Identify the single MODULES.map() call. The filter applies here.
    src/fork-config.js — Confirm getForkConfig() export and return shape { activeModules: string[] }.
  </read_first>
  <action>
    SIDEBAR.JS CHANGES:

    1. Add import at top: import { getForkConfig } from './fork-config.js';

    2. At the beginning of initSidebar(), derive the filtered module list:
       const { activeModules } = getForkConfig();
       const visibleModules = MODULES.filter(mod => activeModules.includes(mod.id));

    3. Replace every use of MODULES inside initSidebar() with visibleModules:
       - The allChecks flatMap: use visibleModules.flatMap(...)
       - The sidebarModules.innerHTML assignment: use visibleModules.map(...)
       - The progress bar loop: use visibleModules.forEach(...)
       Note: The checkLessonAvailability calls, Lucide icon activation, hover listeners, toggle logic, and footer wiring are all unchanged.

    4. Do NOT change refreshSidebarProgress() — it takes a moduleId parameter and uses MODULES.find(m => m.id === moduleId). This is correct: after a lesson completes, the module is always in the full MODULES array. Filtering here would prevent progress refresh for modules that happen to be in all 5 IDs.

    5. Do NOT change setActiveModule() or setActiveLesson() — these operate on rendered DOM, not MODULES.

    HOME-VIEW.JS CHANGES:

    1. Add import at top: import { getForkConfig } from '../fork-config.js';

    2. At the beginning of renderHome(), derive filtered list:
       const { activeModules } = getForkConfig();
       const visibleModules = MODULES.filter(mod => activeModules.includes(mod.id));

    3. Replace MODULES.map(mod => ...) with visibleModules.map(mod => ...) for the cards array.

    4. Leave everything else in renderHome() unchanged (the outer section HTML, the description paragraph, the cards container).
  </action>
  <verify>
    <automated>grep -n "getForkConfig\|visibleModules\|activeModules" src/sidebar.js src/views/home-view.js</automated>
  </verify>
  <acceptance_criteria>
    - src/sidebar.js imports getForkConfig from './fork-config.js'
    - src/sidebar.js derives visibleModules = MODULES.filter(mod => activeModules.includes(mod.id)) inside initSidebar()
    - src/sidebar.js allChecks flatMap uses visibleModules (not MODULES)
    - src/sidebar.js sidebarModules.innerHTML uses visibleModules.map() (not MODULES.map())
    - src/sidebar.js progress bar forEach uses visibleModules.forEach() (not MODULES.forEach())
    - src/sidebar.js refreshSidebarProgress() still uses MODULES.find() — unchanged
    - src/views/home-view.js imports getForkConfig from '../fork-config.js'
    - src/views/home-view.js derives visibleModules inside renderHome()
    - src/views/home-view.js cards array uses visibleModules.map() (not MODULES.map())
    - quiz-engine.js is NOT modified in this task
  </acceptance_criteria>
  <done>sidebar.js and home-view.js only render modules present in getForkConfig().activeModules. Inactive modules have no sidebar entry and no home page card.</done>
</task>

<task type="auto">
  <name>Task 2: Filter completion-summary-view.js to active modules for overall percentage</name>
  <files>src/views/completion-summary-view.js</files>
  <read_first>
    src/views/completion-summary-view.js — Read the full current file. Identify the three MODULES references in renderCompletionSummary(): hasProgress (line ~156), buildProgressTableHtml(MODULES) (line ~189), buildControlsBadgesHtml(MODULES) (line ~190). These three must use the filtered list. The internal helpers buildProgressTableHtml and buildControlsBadgesHtml accept a modules parameter — they already operate on whatever array is passed in; no changes needed inside those helpers.
    src/fork-config.js — Confirm getForkConfig() export.
    src/quiz-engine.js — Confirm computeModuleProgress() signature: computeModuleProgress(mod) takes a single module object. Do NOT modify this function.
  </read_first>
  <action>
    Modify src/views/completion-summary-view.js to use active modules for the completion summary:

    1. Add import at top: import { getForkConfig } from '../fork-config.js';

    2. At the beginning of renderCompletionSummary() (after the null-guard and storageOk check), derive the active module list:
       const { activeModules } = getForkConfig();
       const activeModuleList = MODULES.filter(mod => activeModules.includes(mod.id));

    3. Replace the three MODULES references inside renderCompletionSummary() with activeModuleList:
       - const hasProgress = activeModuleList.some(mod => computeModuleProgress(mod).numerator > 0);
       - const tableHtml = buildProgressTableHtml(activeModuleList);
       - const badgesHtml = buildControlsBadgesHtml(activeModuleList);

    4. The internal helpers buildProgressTableHtml(modules) and buildControlsBadgesHtml(modules) already accept a modules parameter. Do NOT modify them — they will automatically work with the filtered list.

    5. Do NOT modify computeModuleProgress() in quiz-engine.js. The calls to computeModuleProgress(mod) inside buildProgressTableHtml and buildControlsBadgesHtml remain correct because they operate on individual mod objects from the (now filtered) list.

    6. The empty-state link "Go to Module 1 → logging-auditing" is hardcoded. Leave it as-is — it targets logging-auditing which is always module 1 and will always be available in at least single-module forks.

    Result: A 2-module fork that has completed both active modules will show 2/2 lessons in the table. computeModuleProgress() still reads progress from localStorage correctly because it's called with individual module objects, not the full array.
  </action>
  <verify>
    <automated>grep -n "getForkConfig\|activeModuleList\|activeModules" src/views/completion-summary-view.js</automated>
  </verify>
  <acceptance_criteria>
    - src/views/completion-summary-view.js imports getForkConfig from '../fork-config.js'
    - renderCompletionSummary() derives activeModuleList = MODULES.filter(mod => activeModules.includes(mod.id))
    - hasProgress check uses activeModuleList.some() (not MODULES.some())
    - buildProgressTableHtml called with activeModuleList (not MODULES)
    - buildControlsBadgesHtml called with activeModuleList (not MODULES)
    - buildProgressTableHtml function signature and internals are unchanged
    - buildControlsBadgesHtml function signature and internals are unchanged
    - quiz-engine.js is NOT modified
    - computeModuleProgress() in quiz-engine.js is NOT modified
  </acceptance_criteria>
  <done>completion-summary-view.js overall percentage and table rows reflect only active modules. A 2-module fork with both complete shows 100% (not 40%). quiz-engine.computeModuleProgress() is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getForkConfig().activeModules → render layer | activeModules strings used in Array.includes() and Array.filter() — no DOM injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | activeModules filter bypass | accept | Filter is UX-only (D-10 explicitly states modules are hidden, not access-controlled); content files are public static assets |
| T-10-04-02 | Denial of Service | empty activeModules array | mitigate | If activeModules is [] (empty array), visibleModules = [] and the sidebar renders empty — this is acceptable behavior; getForkConfig() always returns an array (never undefined) due to Plan 01 validation |
| T-10-04-03 | Tampering | completion-summary denominator manipulation | accept | A user can open DevTools and modify localStorage; training log is self-reported, not audited; no regulatory filing value |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
1. grep -n "getForkConfig" src/sidebar.js src/views/home-view.js src/views/completion-summary-view.js — all three files show matches
2. grep "MODULES\.map\|MODULES\.some\|MODULES\.forEach" src/sidebar.js src/views/home-view.js src/views/completion-summary-view.js — should return empty or only show unmodified helpers
3. grep "getForkConfig\|filter\|activeModules" src/quiz-engine.js — should return empty (quiz-engine not modified)
4. npm test -- --reporter=verbose 2>&1 | tail -20 — all tests pass
</verification>

<success_criteria>
- sidebar.js: only active modules appear in sidebar (visibleModules filter applied)
- home-view.js: only active module cards appear on home page
- completion-summary-view.js: table and percentage denominator uses active modules only
- quiz-engine.js: unchanged — computeModuleProgress() always operates on full module list
- All existing tests pass (177 green)
</success_criteria>

<output>
Create .planning/phases/10-fork-config-system/10-04-SUMMARY.md when done.
</output>
