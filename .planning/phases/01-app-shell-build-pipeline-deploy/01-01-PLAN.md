---
phase: 01-app-shell-build-pipeline-deploy
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - src/style.css
  - src/modules-config.js
  - data/compliance-refs.json
  - vitest.config.js
  - tests/router.test.js
  - tests/sidebar.test.js
  - tests/compliance-refs.test.js
  - public/.nojekyll
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - DATA-01

must_haves:
  truths:
    - "Running `npm run dev` starts the Vite dev server without engine errors"
    - "`npm run build` produces a `dist/` directory with `dist/.nojekyll` present"
    - "`data/compliance-refs.json` contains TSA and NIST directive data with shortName fields"
    - "`src/style.css` declares all design tokens in the `@theme` block"
    - "`src/modules-config.js` exports MODULES array with all 5 module definitions"
    - "Vitest test stubs exist and can be discovered by `npx vitest run`"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with vite, tailwindcss, @tailwindcss/vite, vitest, @vitest/ui, happy-dom"
      contains: "\"build\": \"vite build\""
    - path: "vite.config.js"
      provides: "Vite build config with base path and Tailwind plugin"
      contains: "base: '/pipeline-cyber-training/'"
    - path: "src/style.css"
      provides: "Tailwind v4 @theme design token declarations"
      contains: "--color-accent: #f97316"
    - path: "data/compliance-refs.json"
      provides: "Single source of truth for TSA and NIST version strings"
      contains: "\"shortName\": \"TSA SD-02F\""
    - path: "src/modules-config.js"
      provides: "Static module metadata for Phase 1 placeholder views"
      contains: "export const MODULES"
    - path: "vitest.config.js"
      provides: "Vitest test runner config with happy-dom environment"
    - path: "tests/router.test.js"
      provides: "Failing test stubs for SHELL-01, SHELL-02"
    - path: "tests/sidebar.test.js"
      provides: "Failing test stubs for SHELL-01"
    - path: "tests/compliance-refs.test.js"
      provides: "Failing test stubs for DATA-01"
    - path: "public/.nojekyll"
      provides: "Disables Jekyll on GitHub Pages deployed artifact"
  key_links:
    - from: "vite.config.js"
      to: "dist/"
      via: "npm run build"
      pattern: "base: '/pipeline-cyber-training/'"
    - from: "src/style.css"
      to: "compiled CSS"
      via: "@tailwindcss/vite plugin"
      pattern: "@import \"tailwindcss\""
    - from: "data/compliance-refs.json"
      to: "src/main.js loadComplianceRefs()"
      via: "fetch(import.meta.env.BASE_URL + 'data/compliance-refs.json')"
      pattern: "directives.TSA.shortName"
---

<objective>
Scaffold the Vite project, install all dependencies, create the design token CSS, compliance data file, module config, and Vitest test stubs. After this plan, `npm run dev` starts without errors, `npm run build` produces a dist/ with .nojekyll, and Vitest can discover (but not necessarily pass) all three test files.

Purpose: Every subsequent plan depends on this scaffold. The Node upgrade is a hard blocker — local Node 20.13.1 is below Vite 8's minimum. The test stubs (Wave 0 gap) must exist before any implementation task runs.

Output: Working Vite project directory, all contracts established, test infrastructure installed.
</objective>

<execution_context>
@C:\Users\psyco\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-build-pipeline-deploy/01-CONTEXT.md
@.planning/phases/01-app-shell-build-pipeline-deploy/01-RESEARCH.md
@.planning/phases/01-app-shell-build-pipeline-deploy/01-PATTERNS.md
@.planning/phases/01-app-shell-build-pipeline-deploy/01-VALIDATION.md
</context>

## Phase Goal

**As a** pipeline IT/OT admin, **I want to** navigate between all five module placeholders via a sidebar and see the app's structure, **so that** I can understand the training platform's scope and access it at a stable URL.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Upgrade Node.js to 22 LTS</name>
  <read_first>
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-RESEARCH.md (Pitfall 1: Node Version Mismatch section; Environment Availability table)
  </read_first>
  <what-built>Nothing yet — this is a prerequisite action. Local Node 20.13.1 is below Vite 8's engine requirement of ^20.19.0 || >=22.12.0. Without upgrading, `npm create vite@latest` and `npm run dev` will fail with an engine incompatibility error.</what-built>
  <how-to-verify>
    1. Run `node --version` — confirm output is v22.x.x or v20.19.x or higher
    2. Upgrade options (choose one):
       - Windows installer: download Node 22 LTS from https://nodejs.org/en/download — run .msi installer
       - winget: `winget install OpenJS.NodeJS.LTS`
    3. Open a NEW terminal window after install (PATH must refresh)
    4. Re-run `node --version` in the new terminal to confirm
  </how-to-verify>
  <resume-signal>Type "node upgraded" after confirming `node --version` returns 22.x.x or 20.19.x+</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold Vite project, install dependencies, create test stubs</name>
  <read_first>
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-RESEARCH.md (Standard Stack section, Pitfall 1, Pitfall 2, Validation Architecture section, Wave 0 Gaps)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-PATTERNS.md (vite.config.js pattern, vitest.config.js pattern, tests/router.test.js pattern, tests/sidebar.test.js section, tests/compliance-refs.test.js section)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <behavior>
    - Test: `matchRoute('#/')` returns `{ view: 'home', params: {} }` — initially a stub that imports a non-existent function (test will fail RED until Plan 02)
    - Test: `matchRoute('#/module/logging-auditing')` returns `{ view: 'module', params: { moduleId: 'logging-auditing' } }` — stub only
    - Test: `matchRoute('#/unknown')` returns object with `view === 'not-found'` — stub only
    - Test: compliance-refs.json has `directives.TSA.shortName` as a non-empty string — this test CAN pass once the JSON is created in Task 3
    - Test: compliance-refs.json has `directives.NIST.shortName` as a non-empty string — same
    - Test: compliance-refs.json `schemaVersion` equals 1
  </behavior>
  <action>
    Run `npm create vite@latest pipeline-cyber-training -- --template vanilla` in the PARENT of the working directory (or scaffold directly into the current directory using `npm create vite@latest . -- --template vanilla` if the directory is empty). The scaffold produces: `package.json`, `vite.config.js` (minimal, base not yet set), `index.html`, `src/main.js`, `src/style.css`, `src/counter.js`, `src/javascript.svg`, `public/vite.svg`.

    After scaffold, install additional dependencies:
    - `npm install tailwindcss @tailwindcss/vite` (adds Tailwind v4 packages; versions: tailwindcss@4.3.0, @tailwindcss/vite@4.3.0)
    - `npm install -D vitest @vitest/ui happy-dom` (adds test infrastructure)

    Update `vite.config.js`: import `tailwindcss` from `'@tailwindcss/vite'`; set `base: '/pipeline-cyber-training/'`; add `tailwindcss()` to `plugins` array. Do NOT create `tailwind.config.js` or `postcss.config.js` — these are not used with Tailwind v4.

    Create `vitest.config.js` using the pattern from PATTERNS.md: `environment: 'happy-dom'`, `include: ['tests/**/*.test.js']`. Import `defineConfig` from `'vitest/config'`.

    Create `tests/router.test.js` with 4 test cases per PATTERNS.md (import `matchRoute` from `'../src/router.js'` — this import will fail until Plan 02 creates router.js; the file must exist now as a stub with the correct import and test bodies).

    Create `tests/sidebar.test.js` with 3 test stubs: (1) sidebar renders exactly 5 module items with `data-module-id` attributes matching MODULES ids, (2) each module item's `data-module-id` is in the set `['logging-auditing', 'network-hardening', 'account-access', 'incident-response', 'patch-management']`, (3) collapse toggle sets `aria-label="Expand navigation"` when clicked. Import `MODULES` from `'../src/modules-config.js'`.

    Create `tests/compliance-refs.test.js` with 4 test cases: read and JSON.parse `./data/compliance-refs.json` (using `fs.readFileSync`), assert `schemaVersion === 1`, assert `directives.TSA.shortName` is a non-empty string, assert `directives.NIST.shortName` is a non-empty string. This test CAN be green once Task 3 creates the JSON file — use Node's `fs` module via `import { readFileSync } from 'fs'`.

    Create `public/.nojekyll` as an empty file. This must be in `public/`, not the project root — Vite copies `public/` to `dist/` at build time.

    Add `"test": "vitest run"` and `"test:ui": "vitest --ui"` scripts to `package.json`.

    Commit `package-lock.json` — required for `npm ci` in GitHub Actions.
  </action>
  <verify>
    <automated>node --version | grep -E "^v(2[0-9]\.|22\.)" && npm run build 2>&1 | grep -v "error" | head -5</automated>
  </verify>
  <done>
    - `npm run build` exits 0 and produces `dist/` directory
    - `dist/.nojekyll` exists (Vite copied from `public/`)
    - `vite.config.js` contains `base: '/pipeline-cyber-training/'` and `plugins: [tailwindcss()]`
    - `vitest.config.js` exists with `environment: 'happy-dom'`
    - `tests/router.test.js`, `tests/sidebar.test.js`, `tests/compliance-refs.test.js` all exist
    - `package-lock.json` exists in repo root
    - No `tailwind.config.js` or `postcss.config.js` files exist
  </done>
</task>

<task type="auto">
  <name>Task 3: Create design tokens, module config, and compliance data</name>
  <read_first>
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-PATTERNS.md (src/style.css pattern, src/modules-config.js pattern, data/compliance-refs.json pattern)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-UI-SPEC.md (Tailwind v4 Token Declaration section, Color section, Typography section, Spacing Scale section)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-RESEARCH.md (Pattern 1: Vite + Tailwind v4 Configuration, Pattern 4: compliance-refs.json Shape, Pattern 6: Module Data Shape, Anti-Patterns to Avoid)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-CONTEXT.md (D-11, D-12, D-16)
  </read_first>
  <action>
    Replace the scaffolded `src/style.css` entirely. The new file has exactly two sections: `@import "tailwindcss";` on the first line, then the `@theme` block with ALL tokens from PATTERNS.md src/style.css pattern. The `@theme` block must include every token listed: color tokens (--color-bg-base through --color-badge-nist-text), typography tokens (--font-sans, --font-mono, --text-mono, --text-body, --text-heading, --text-display), spacing tokens (--spacing-xs through --spacing-3xl), and layout dimension tokens (--sidebar-width-expanded: 256px, --sidebar-width-collapsed: 48px, --topbar-height: 48px). Also add the sidebar collapse CSS rule and sidebar-label transition CSS as plain CSS after the `@theme` block:
    - `#shell.sidebar-collapsed { grid-template-columns: var(--sidebar-width-collapsed) 1fr; }` 
    - `#sidebar { transition: width 200ms ease; overflow: hidden; }`
    - `.sidebar-label { transition: opacity 150ms ease 50ms, width 150ms ease 50ms; }`
    - `.sidebar-lessons { max-height: 0; overflow: hidden; transition: max-height 200ms ease; }`
    - Focus ring rule: `*:focus-visible { outline: 3px solid #f97316; outline-offset: 2px; }`
    Do NOT create tailwind.config.js. Do NOT create postcss.config.js.

    Create `src/modules-config.js` using the exact MODULES array from PATTERNS.md: all 5 modules with id, title, icon (Lucide name), description, order, estimatedMinutes, lessons array with id+title per lesson, and complianceTags: ['TSA', 'NIST']. The five module ids are: 'logging-auditing', 'network-hardening', 'account-access', 'incident-response', 'patch-management'. Export as `export const MODULES = [...]`.

    Create `data/compliance-refs.json` using the exact shape from PATTERNS.md Pattern 4: schemaVersion 1, lastVerified "2026-05-11", directives.TSA (name, shortName "TSA SD-02F", effectiveDate "2025-05-03", expiryDate "2026-05-02", sourceUrl), directives.NIST (name, shortName "NIST SP 800-82 Rev 3", publishedDate "2023-09", sourceUrl). Never hardcode "SD-02F" or "SP 800-82 Rev 3" in any src/ file — this JSON is the only allowed location for those strings.

    After creating compliance-refs.json, run the compliance-refs test to confirm it goes green: `npx vitest run tests/compliance-refs.test.js`
  </action>
  <verify>
    <automated>npx vitest run tests/compliance-refs.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - `src/style.css` begins with `@import "tailwindcss";` followed by `@theme {` block containing all tokens from UI-SPEC
    - `src/style.css` does NOT contain any hardcoded hex values in class attributes — tokens only
    - `src/modules-config.js` exports `MODULES` array with exactly 5 entries; each has id, title, icon, description, order, estimatedMinutes, lessons[], complianceTags[]
    - `data/compliance-refs.json` passes all 4 tests in `tests/compliance-refs.test.js` (green)
    - `grep -r "SD-02" src/` returns no matches (version string is only in data/)
    - `npm run build` still exits 0 after these file replacements
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static file → browser | JSON and JS files served from GitHub Pages; no user-controlled input in Wave 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package-lock.json / supply chain | mitigate | Commit package-lock.json; CI uses `npm ci` (not `npm install`) for deterministic installs |
| T-01-02 | Tampering | compliance-refs.json version strings | mitigate | Single file is the only allowed location for version strings; grep gate in verification (`grep -r "SD-02" src/` must return no matches) |
| T-01-03 | Information Disclosure | vite.config.js base path | accept | Base path `/pipeline-cyber-training/` is public knowledge (GitHub Pages URL is public); no secrets in config |
</threat_model>

<verification>
After Plan 01 completes:
1. `node --version` returns v22.x.x or v20.19.x+
2. `npm run build` exits 0; `dist/.nojekyll` exists
3. `vite.config.js` contains `base: '/pipeline-cyber-training/'`
4. `npx vitest run tests/compliance-refs.test.js` exits 0 (4 tests green)
5. `grep -r "SD-02" src/` returns no output (exit code 1 — no matches)
6. No `tailwind.config.js` or `postcss.config.js` in repo root
7. `tests/router.test.js` and `tests/sidebar.test.js` exist (may fail — stubs expected)
</verification>

<success_criteria>
- Vite scaffold is committed with Node 22 compatible package.json
- `npm run build` produces `dist/` with `dist/.nojekyll`
- Design tokens are the single source of color/type/spacing — no hardcoded values in src/
- `data/compliance-refs.json` is the single source for TSA/NIST version strings
- Vitest test infrastructure is installed; compliance-refs tests are green; router/sidebar tests are red stubs (expected at this stage)
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-build-pipeline-deploy/01-01-SUMMARY.md`
</output>
