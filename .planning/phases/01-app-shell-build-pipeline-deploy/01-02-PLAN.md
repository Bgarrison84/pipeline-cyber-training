---
phase: 01-app-shell-build-pipeline-deploy
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01-PLAN.md
files_modified:
  - index.html
  - src/main.js
  - src/router.js
  - src/sidebar.js
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02

must_haves:
  truths:
    - "Visiting `http://localhost:5173/pipeline-cyber-training/` in a browser renders the OT Security Lab app shell (top bar + sidebar + #app area)"
    - "`window.location.hash = '#/'` triggers home view render into #app"
    - "`window.location.hash = '#/module/logging-auditing'` triggers module view render into #app"
    - "An unmatched hash (e.g. `#/garbage`) renders the 404 view, no uncaught JS exception"
    - "The sidebar toggle button collapses the sidebar to 48px and shows only module icons"
    - "`npx vitest run tests/router.test.js` exits 0 (all 4 matchRoute tests green)"
  artifacts:
    - path: "src/router.js"
      provides: "Hash router with matchRoute, handleRoute, extractParams"
      exports: ["matchRoute", "handleRoute"]
    - path: "src/main.js"
      provides: "App entry point — fetches compliance-refs, calls handleRoute, exports renderBadge"
      exports: ["loadComplianceRefs", "getComplianceRefs", "renderBadge"]
    - path: "src/sidebar.js"
      provides: "Sidebar collapse toggle and active module accordion"
      exports: ["setActiveModule", "initSidebar"]
    - path: "index.html"
      provides: "App shell HTML — #shell grid, #top-bar, #sidebar, #app"
      contains: "id=\"app\""
  key_links:
    - from: "index.html"
      to: "src/main.js"
      via: "<script type=\"module\" src=\"/src/main.js\">"
      pattern: "type=\"module\""
    - from: "src/main.js"
      to: "src/router.js handleRoute"
      via: "import { handleRoute } from './router.js'"
      pattern: "handleRoute()"
    - from: "src/router.js"
      to: "document.getElementById('app')"
      via: "app.innerHTML = renderer(params)"
      pattern: "getElementById('app')"
    - from: "src/main.js"
      to: "data/compliance-refs.json"
      via: "fetch(import.meta.env.BASE_URL + 'data/compliance-refs.json')"
      pattern: "import.meta.env.BASE_URL"
---

<objective>
Build the hash router, app entry point with compliance-refs fetch, and sidebar toggle. After this plan, the dev server at `localhost:5173` serves a navigable app shell. The sidebar renders 5 module links; clicking one changes the hash; the router dispatches to the correct view handler. Vitest router tests go green.

Purpose: This is the routing nervous system — everything else mounts onto it. The sidebar is the primary navigation mechanism (D-05). The compliance-refs fetch chain is established here so all subsequent views can call `renderBadge()` without async setup.

Output: `src/router.js`, `src/main.js`, `src/sidebar.js`, `index.html`
</objective>

<execution_context>
@C:\Users\psyco\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/01-app-shell-build-pipeline-deploy/01-CONTEXT.md
@.planning/phases/01-app-shell-build-pipeline-deploy/01-RESEARCH.md
@.planning/phases/01-app-shell-build-pipeline-deploy/01-PATTERNS.md
@.planning/phases/01-app-shell-build-pipeline-deploy/01-UI-SPEC.md
@.planning/phases/01-app-shell-build-pipeline-deploy/01-01-SUMMARY.md

<interfaces>
From src/modules-config.js (created in Plan 01):
Each MODULES entry shape:
  id: string          — e.g. 'logging-auditing'
  title: string       — e.g. 'Logging & Auditing'
  icon: string        — Lucide icon name, e.g. 'BookOpen'
  description: string
  order: number
  estimatedMinutes: number
  lessons: Array<{ id: string, title: string }>
  complianceTags: string[]  — ['TSA', 'NIST']

Five module ids: 'logging-auditing', 'network-hardening', 'account-access', 'incident-response', 'patch-management'

From data/compliance-refs.json (created in Plan 01):
{
  "schemaVersion": 1,
  "directives": {
    "TSA": { "shortName": "TSA SD-02F", ... },
    "NIST": { "shortName": "NIST SP 800-82 Rev 3", ... }
  }
}

From vitest.config.js (created in Plan 01):
environment: 'happy-dom', include: ['tests/**/*.test.js']

Tests that must go GREEN in this plan (from tests/router.test.js):
- matchRoute('#/') → { view: 'home', params: {} }
- matchRoute('#/module/logging-auditing') → { view: 'module', params: { moduleId: 'logging-auditing' } }
- matchRoute('#/unknown').view === 'not-found'
- matchRoute('').view === 'home'  (empty hash treated as root)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router — matchRoute, extractParams, handleRoute, updateSidebarActiveState</name>
  <read_first>
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-PATTERNS.md (src/router.js pattern — full implementation)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-RESEARCH.md (Pattern 3: Hash Router Implementation, Security Domain section — XSS hygiene for hash content)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-CONTEXT.md (D-03 — hash routing locked; no history.pushState)
    - tests/router.test.js (the test stubs that must go green after this task)
  </read_first>
  <behavior>
    - matchRoute('#/') returns { view: 'home', params: {} }
    - matchRoute('#/module/logging-auditing') returns { view: 'module', params: { moduleId: 'logging-auditing' } }
    - matchRoute('#/module/patch-management') returns { view: 'module', params: { moduleId: 'patch-management' } }
    - matchRoute('#/unknown') returns { view: 'not-found', params: {} }
    - matchRoute('') (empty string) returns { view: 'home', params: {} }
    - matchRoute('#/module/a/b/c') returns { view: 'not-found', params: {} } — extra segments do not match
    - decodeURIComponent is applied to param values (e.g. '%2F' in a segment is decoded)
    - handleRoute() reads window.location.hash, calls matchRoute, writes renderer(params) result to document.getElementById('app').innerHTML, then calls updateSidebarActiveState
  </behavior>
  <action>
    Create `src/router.js` implementing the full pattern from PATTERNS.md. The file must:

    1. Import view renderers: `renderHome` from './views/home-view.js', `renderModule` from './views/module-view.js', `renderNotFound` from './views/not-found-view.js'. Note: these files do not exist yet (created in Plan 03). To avoid import errors during testing, guard the view imports or use dynamic import — OR structure the file so that `matchRoute` and `extractParams` are independently testable without DOM/view dependencies. Recommended approach: export `matchRoute` and `extractParams` from the top of the file before any view imports. The test file only imports `matchRoute`.

    2. `extractParams(hash, pattern)`: tokenize both strings by splitting on `/` after removing leading `#`, filter empty segments. If lengths differ, return null. Iterate: if pattern segment starts with `:`, capture as param key; else require exact match. Return params object or null on mismatch.

    3. `matchRoute(hash)`: treat empty/null hash as `'#/'`. Iterate `routes` array. For each route, call `extractParams`. First non-null match returns `{ view: route.view, params }`. If no match, return `{ view: 'not-found', params: {} }`.

    4. `routes` array: `{ pattern: '#/', view: 'home' }` and `{ pattern: '#/module/:moduleId', view: 'module' }`. Comment: Phase 2 adds `{ pattern: '#/lesson/:moduleId/:lessonId', view: 'lesson' }`.

    5. `viewRenderers` object mapping view names to renderer functions.

    6. `handleRoute()`: read `window.location.hash`, call `matchRoute`, get `app = document.getElementById('app')`, call renderer, assign `app.innerHTML = renderer(params)`. After render, call `lucide.createIcons()` if `typeof lucide !== 'undefined'`. Call `updateSidebarActiveState(params.moduleId)` from sidebar.js (import as named import).

    7. `updateSidebarActiveState(activeModuleId)` (can be inline or imported from sidebar.js): query all `[data-module-id]` elements, toggle `sidebar-module--active` class and `aria-current` attribute based on match.

    8. Register events: `window.addEventListener('hashchange', handleRoute)` and `window.addEventListener('load', handleRoute)`.

    9. Export: `export { matchRoute, handleRoute }`.

    XSS rule: never write `params.moduleId` or any hash-derived string directly via `innerHTML`. The `moduleId` param is used only as a lookup key against `MODULES` — the module title from MODULES (a hardcoded JS object) is what reaches the DOM.

    After creating the file, run the router tests to confirm they go green.
  </action>
  <verify>
    <automated>npx vitest run tests/router.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - `npx vitest run tests/router.test.js` exits 0 with all 4 tests green
    - `src/router.js` exports both `matchRoute` and `handleRoute`
    - `src/router.js` does not contain `eval(` or `new Function(`
    - `src/router.js` does not use `history.pushState`
    - Hash-derived strings (params.moduleId) are used only as lookup keys, not injected into innerHTML directly
  </done>
</task>

<task type="auto">
  <name>Task 2: App entry point (main.js) + index.html shell structure</name>
  <read_first>
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-PATTERNS.md (src/main.js pattern, index.html pattern — full structure including top bar and sidebar HTML, Lucide Icon Activation Pattern, import.meta.env.BASE_URL Pattern)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-UI-SPEC.md (Component 1: App Shell, Component 2: Top Bar, Component 3: Sidebar — all structural details; Copywriting Contract; Accessibility Contract; Interaction Contracts — Sidebar Toggle, Module Expand/Collapse)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-RESEARCH.md (Pattern 2: CSS Grid App Shell Layout; Code Examples — Compliance Badge Rendering; Anti-Patterns to Avoid)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-CONTEXT.md (D-05, D-06, D-07, D-08, D-09)
    - src/modules-config.js (read the actual file created in Plan 01 to verify MODULES array shape)
  </read_first>
  <action>
    Create `src/main.js` using the exact pattern from PATTERNS.md:
    - Import `handleRoute` from './router.js', import './style.css'
    - Module-level `let complianceRefs = null`
    - `export async function loadComplianceRefs()`: fetch from `import.meta.env.BASE_URL + 'data/compliance-refs.json'`; if res.ok, set module-level complianceRefs; return complianceRefs or null
    - `export function getComplianceRefs()`: returns complianceRefs
    - `export function renderBadge(directiveKey)`: fallback object `{ TSA: 'TSA SD-02F', NIST: 'NIST SP 800-82 Rev 3' }`; reads shortName from complianceRefs?.directives?.[directiveKey]?.shortName ?? fallbacks[directiveKey]; returns `<span>` with Tailwind v4 class attributes using `bg-[var(--color-badge-tsa-bg)]` / `text-[var(--color-badge-tsa-text)]` for TSA and equivalent NIST classes; span contains the shortName string
    - `async function init()`: await loadComplianceRefs(), then call handleRoute()
    - Call `init()` at module level
    - CRITICAL: The shortName string from complianceRefs is set as the span's text via the template literal — this is acceptable because the data comes from a static JSON file (not user input). Do NOT set it via textContent separately.

    Replace the scaffolded `index.html` entirely. The new index.html must:
    - `<!doctype html><html lang="en">` — semantic lang attribute
    - `<head>`: charset UTF-8, viewport, `<title>OT Security Lab</title>`, Google Fonts preconnect links for fonts.googleapis.com and fonts.gstatic.com (crossorigin on gstatic), Google Fonts stylesheet for Inter (weights 400,600) and JetBrains Mono, Lucide CDN script tag: `<script src="https://unpkg.com/lucide@latest"></script>`, stylesheet link: `<link rel="stylesheet" href="/src/style.css" />`
    - `<body class="bg-[var(--color-bg-base)] text-[var(--color-text-primary)] h-screen overflow-hidden">`
    - `<div id="shell" class="grid h-screen" style="grid-template-columns: var(--sidebar-width-expanded) 1fr; grid-template-rows: var(--topbar-height) 1fr;">`
    - `<header id="top-bar" class="col-span-2 flex items-center justify-between px-[var(--spacing-lg)]" style="background: var(--color-bg-secondary); border-bottom: 1px solid var(--color-border); position: sticky; top: 0; z-index: 50; height: var(--topbar-height);">` containing: left div with "OT Security Lab" span (style: color var(--color-accent), font-size var(--text-display), font-weight 600), right div with "Compliance Index" anchor (href="#/compliance", style: color var(--color-text-muted)) and "0 of 5 modules complete" span (style: font-size var(--text-body), font-weight 600, text-transform uppercase, letter-spacing 0.08em)
    - `<nav id="sidebar" aria-label="Module navigation" style="background: var(--color-bg-secondary); border-right: 1px solid var(--color-border); display: flex; flex-direction: column; overflow: hidden; width: var(--sidebar-width-expanded);">` containing: `<ul id="module-list" style="flex: 1; overflow-y: auto; list-style: none; margin: 0; padding: 0;">` — module items injected by sidebar.js — and sidebar toggle button at bottom: `<button id="sidebar-toggle" aria-label="Collapse navigation" style="width: 100%; height: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center; background: transparent; border: none; border-top: 1px solid var(--color-border); cursor: pointer; color: var(--color-text-muted);">` with a `<i data-lucide="chevron-left" style="width:20px;height:20px;"></i>` inside
    - `<main id="app" role="main" style="overflow-y: auto; padding: 0;"><!-- router injects views here --></main>`
    - `<script type="module" src="/src/main.js"></script>` before closing `</body>`
    - Delete the scaffolded `src/counter.js` and `src/javascript.svg` and `public/vite.svg` files (they are Vite scaffold artifacts not needed here)
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build` exits 0
    - `index.html` contains `id="app"`, `id="top-bar"`, `id="sidebar"`, `id="shell"`, `id="sidebar-toggle"`
    - `index.html` `<title>` is "OT Security Lab" (per D-09 and Copywriting Contract)
    - `index.html` `<body>` uses `bg-[var(--color-bg-base)]` class (no hardcoded hex)
    - `src/main.js` exports `loadComplianceRefs`, `getComplianceRefs`, `renderBadge`
    - `src/main.js` uses `import.meta.env.BASE_URL` for the compliance-refs fetch URL (not a hardcoded string)
    - `src/main.js` does NOT contain the literal strings "SD-02F" or "SP 800-82 Rev 3" — these come from the JSON
    - Lucide CDN script tag uses `https://unpkg.com/lucide@latest` (per RESEARCH.md)
  </done>
</task>

<task type="auto">
  <name>Task 3: Sidebar JS — module list render, collapse toggle, accordion</name>
  <read_first>
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-PATTERNS.md (src/sidebar.js pattern — collapse toggle and accordion patterns; Lucide Icon Activation Pattern; XSS Hygiene Pattern)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-UI-SPEC.md (Component 3: Sidebar — all visual specs including active state colors, lesson item opacity, icon names, chevron behavior; Interaction Contracts — Sidebar Toggle, Module Expand/Collapse; Accessibility Contract)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-RESEARCH.md (Code Examples: Sidebar Collapse Toggle; Pitfall 5: Sidebar Width Transition)
    - .planning/phases/01-app-shell-build-pipeline-deploy/01-CONTEXT.md (D-06, D-07, D-14)
    - src/modules-config.js (the actual file to verify MODULES array)
    - tests/sidebar.test.js (the test stubs that must go green after this task)
  </read_first>
  <action>
    Create `src/sidebar.js`. This file is responsible for: (1) rendering all 5 module items into `#module-list`, (2) the collapse toggle behavior on `#sidebar-toggle`, and (3) the accordion expand/collapse on module click.

    The file must import `MODULES` from './modules-config.js'.

    Function `renderSidebarModules()`: iterate MODULES, for each module generate an `<li>` element with `class="sidebar-module"` and `data-module-id="{mod.id}"`. Inside the li: an `<a>` tag with `href="#/module/{mod.id}"` and `aria-label="{mod.title}"` for screen-reader support in collapsed mode. The anchor contains: a Lucide icon `<i data-lucide="{mod.icon}" style="width:20px;height:20px;flex-shrink:0;">`, a `<span class="sidebar-label">` with the module title, and a chevron `<i data-lucide="chevron-right" style="width:16px;height:16px;color:var(--color-text-muted);" class="sidebar-chevron">`. Below the anchor, a `<ul class="sidebar-lessons">` containing lesson items — each lesson rendered as `<li><span class="sidebar-label" aria-disabled="true" aria-label="{lesson.title} — available in Phase 2" style="pointer-events:none;opacity:0.4;font-size:var(--text-body);padding: var(--spacing-sm) var(--spacing-md) var(--spacing-sm) 48px;display:block;">{lesson.title}</span></li>`. Each lesson `<li>` gets `pointer-events: none`. Insert the generated HTML into `document.getElementById('module-list').innerHTML` via the list join. After setting innerHTML, call `lucide.createIcons()` if available.

    Active module styling: when `setActiveModule(moduleId)` is called, find the matching `.sidebar-module` element (by `el.dataset.moduleId === moduleId`), add inline style `border-left: 3px solid var(--color-accent); background: rgba(249, 115, 22, 0.08);` and set text color to accent. Set `aria-current="page"` on its anchor. Expand its `.sidebar-lessons` via `lessonList.style.maxHeight = lessonList.scrollHeight + 'px'`. For all other modules, remove the active styles and set maxHeight to '0'.

    Collapse toggle: bind `click` to `#sidebar-toggle`. Toggle `sidebar-collapsed` class on `#shell` (not on the sidebar element itself — per RESEARCH.md Pitfall 5). Toggle `aria-label` on the button between "Collapse navigation" and "Expand navigation". Toggle all `.sidebar-label` elements: `opacity: 0; width: 0; overflow: hidden` when collapsed, restore when expanded. Toggle the toggle button's chevron icon direction by toggling a class or swapping `data-lucide` value between `chevron-left` and `chevron-right`.

    Export `initSidebar()` — called from main.js after DOM is ready (after `handleRoute()`). Export `setActiveModule(moduleId)` — called from router.js after each navigation.

    Update `src/main.js` to call `initSidebar()` from sidebar.js after `handleRoute()` in `init()`. Add `import { initSidebar } from './sidebar.js'` to main.js.

    Note: router.js `updateSidebarActiveState` should delegate to `setActiveModule` from sidebar.js. Update router.js import to use `setActiveModule` from sidebar.js.
  </action>
  <verify>
    <automated>npx vitest run tests/sidebar.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - `npx vitest run tests/sidebar.test.js` exits 0 (all sidebar tests green)
    - `src/sidebar.js` exports `initSidebar` and `setActiveModule`
    - Sidebar module items have `data-module-id` attribute for each of the 5 modules
    - Lesson items rendered as `<span>` with `aria-disabled="true"` and `pointer-events:none` — not as `<a>` or `<button>`
    - Toggle button has `aria-label` that switches between "Collapse navigation" and "Expand navigation"
    - `npm run build` still exits 0
    - `npx vitest run` exits 0 (all three test files green — router, sidebar, compliance-refs)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| location.hash → router | User controls the hash; router must not eval or innerHTML hash-derived content |
| compliance-refs.json → renderBadge → DOM | Static JSON; trust boundary is same-origin fetch; shortName string reaches innerHTML via template literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / XSS | router.js — hash content in DOM | mitigate | params.moduleId is used only as lookup key against MODULES array; the module title from MODULES (hardcoded JS object) reaches innerHTML — not the raw hash segment |
| T-02-02 | Spoofing (open redirect) | router.js — matchRoute | mitigate | matchRoute returns not-found for all unmatched hashes; no eval(), no new Function(), no redirect logic based on hash content |
| T-02-03 | Tampering / XSS | sidebar.js — module titles in innerHTML | accept | Module titles come from MODULES (hardcoded JS object in modules-config.js), not from user input; no XSS vector |
| T-02-04 | Information Disclosure | compliance-refs.json fetch | accept | Same-origin fetch on GitHub Pages; file contains only public compliance document references, no secrets |
</threat_model>

<verification>
After Plan 02 completes:
1. `npx vitest run` exits 0 — all tests in router.test.js, sidebar.test.js, compliance-refs.test.js green
2. `npm run build` exits 0 — no build errors
3. `npm run dev` starts without errors (browser opens and renders OT Security Lab app shell)
4. Navigating to `#/module/logging-auditing` in browser renders "Logging & Auditing" module placeholder area in #app (even though views are stubs — the routing dispatch works)
5. `src/router.js` does not contain `eval(`, `new Function(`, or `pushState`
6. `src/main.js` does not contain literal strings "SD-02F" or "SP 800-82 Rev 3"
</verification>

<success_criteria>
- Hash router is fully operational — matchRoute, handleRoute, param extraction all work
- App shell HTML structure is in place — #shell, #top-bar, #sidebar, #app all exist
- Sidebar renders all 5 modules with correct data-module-id attributes and disabled lesson items
- Collapse toggle works: sidebar animates between 256px and 48px
- All Vitest tests pass: router (4), sidebar (3), compliance-refs (4) = 11 tests green
- No XSS vectors: hash content never reaches DOM via innerHTML
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-build-pipeline-deploy/01-02-SUMMARY.md`
</output>
