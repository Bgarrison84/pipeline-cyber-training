---
phase: 09-compliance-currency-content-depth
plan: 06
type: execute
wave: 3
depends_on:
  - "09-03"
  - "09-04"
files_modified:
  - public/data/modules/network-hardening/lessons/04-ot-network-segmentation.md
  - public/data/modules/network-hardening/lessons/05-firewall-audit.md
  - public/data/modules/network-hardening/quizzes/02.json
  - public/data/modules/network-hardening/scenarios/02.json
  - src/modules-config.js
autonomous: true
requirements:
  - CONT-05
  - CONT-06
  - CONT-07
must_haves:
  truths:
    - "Two new lessons appear in the network-hardening sidebar and are reachable via direct URL"
    - "Lesson 05-firewall-audit.md renders a quiz section backed by quizzes/02.json"
    - "New scenario 02.json passes validateScenario() with branching OT/IT paths"
    - "Both new lessons contain at least one > [!OT] callout block"
    - "Both new lessons cite a TSA SD-02 series control and a NIST SP 800-82 chapter"
  artifacts:
    - path: "public/data/modules/network-hardening/lessons/04-ot-network-segmentation.md"
      provides: "Advanced OT network segmentation lesson with OT callout"
      contains: "> [!OT]"
    - path: "public/data/modules/network-hardening/lessons/05-firewall-audit.md"
      provides: "Firewall audit lesson with quiz wiring (quizId: '02')"
      contains: "quizId: '02'"
    - path: "public/data/modules/network-hardening/quizzes/02.json"
      provides: "Advanced network quiz; id=network-hardening-quiz-02"
      contains: '"id": "network-hardening-quiz-02"'
    - path: "public/data/modules/network-hardening/scenarios/02.json"
      provides: "Multi-branch OT/IT scenario; id=network-hardening-scenario-02"
      contains: '"id": "network-hardening-scenario-02"'
    - path: "src/modules-config.js"
      provides: "network-hardening MODULES entry with 5 lessons"
  key_links:
    - from: "src/modules-config.js"
      to: "public/data/modules/network-hardening/lessons/04-ot-network-segmentation.md"
      via: "MODULES network-hardening.lessons array entry id: 'ot-network-segmentation'"
      pattern: "id: 'ot-network-segmentation'"
    - from: "src/modules-config.js"
      to: "public/data/modules/network-hardening/quizzes/02.json"
      via: "MODULES entry quizId: '02' on firewall-audit lesson"
      pattern: "quizId: '02'"
---

<objective>
Author all new Phase 9 content for the Network Hardening module: two new lessons,
one new quiz file (02.json), one new scenario (02.json), and update modules-config.js.
Runs in parallel with Plan 09-05 (logging-auditing) — no shared file conflicts after
both add to modules-config.js (different module blocks).

Purpose: CONT-05/06/07 for network-hardening module.

Output:
  - 2 new lesson .md files (order 4 + 5) with OT callouts and compliance citations
  - quizzes/02.json with 3 questions (id: network-hardening-quiz-02)
  - scenarios/02.json with OT/IT branching (id: network-hardening-scenario-02)
  - src/modules-config.js network-hardening lessons array updated to 5 entries
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-PLAN-EXECUTE.md
@C:\Users\psyco\Claude CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-03-SUMMARY.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-04-SUMMARY.md
</context>

<interfaces>
From src/modules-config.js (current network-hardening lessons array — lines 29-33):
```javascript
lessons: [
  { id: 'intro',           title: 'Network Hardening Overview',              scenarioId: '01' },
  { id: 'ps-firewall',     title: 'Managing Firewall Rules with PowerShell', exerciseId: '01' },
  { id: 'firewall-policy', title: 'Windows Firewall Policy for OT Networks', quizId: '01' },
],
```

New entries to append:
```javascript
{ id: 'ot-network-segmentation', title: 'OT Network Segmentation and DMZ Architecture' },
{ id: 'firewall-audit',          title: 'Firewall Rule Auditing with PowerShell', quizId: '02' },
```

Lesson frontmatter template (from 09-PATTERNS.md):
```yaml
---
title: {title}
lessonId: {id}
moduleId: network-hardening
order: {4 or 5}
complianceTags: [TSA, NIST]
complianceControls: [TSA-NetworkSeg, NIST-SC-7]
quizId: '02'      # only on firewall-audit lesson
lastReviewed: ''
reviewer: ''
---
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author lesson 04-ot-network-segmentation.md and lesson 05-firewall-audit.md</name>
  <files>
    public/data/modules/network-hardening/lessons/04-ot-network-segmentation.md,
    public/data/modules/network-hardening/lessons/05-firewall-audit.md
  </files>
  <read_first>
    - public/data/modules/network-hardening/lessons/firewall-policy.md (full file — frontmatter schema + lesson structure for this module)
    - public/data/modules/network-hardening/lessons/intro.md (OT callout pattern for this module)
  </read_first>
  <action>
    Create 04-ot-network-segmentation.md:
    Frontmatter: title="OT Network Segmentation and DMZ Architecture", lessonId='ot-network-segmentation',
    moduleId='network-hardening', order=4, complianceTags=[TSA, NIST],
    complianceControls=[TSA-NetworkSeg, NIST-SC-7, NIST-SC-3], lastReviewed='', reviewer=''

    Content outline (≥400 words):
    - H2: Why OT Requires Separate Network Zones
      - Purdue model zones (Level 0-3) and the pipeline OT context
      - Reference "current TSA pipeline security directive" network segmentation requirement
      - Reference NIST SP 800-82 Rev 3 Chapter 5 (ICS network architecture)
    - H2: DMZ Architecture for IT/OT Data Exchange
      - One-way data diodes vs. jump host models
      - PowerShell: Test-NetConnection -ComputerName PIPELINE-DC01 -Port 5985
    - OT callout block (mandatory):
      > [!OT]
      > In pipeline OT environments, the control network (Level 2) must be isolated from
      > the corporate IT network by a DMZ containing historian servers and data diodes.
      > Direct IT-to-OT connections are prohibited under NIST SP 800-82 Rev 3 Section 5.4.
      > Any exception requires written risk acceptance and compensating controls.
    - H2: Verifying Segmentation via PowerShell
      - Get-NetFirewallRule | Where-Object Enabled -eq True
      - Test-NetConnection verification examples
    - At least one ```powershell code block

    Create 05-firewall-audit.md:
    Frontmatter: title="Firewall Rule Auditing with PowerShell", lessonId='firewall-audit',
    moduleId='network-hardening', order=5, complianceTags=[TSA, NIST],
    complianceControls=[TSA-NetworkSeg, NIST-CM-7, NIST-SC-7], quizId='02',
    lastReviewed='', reviewer=''

    Content outline (≥400 words):
    - H2: Why Firewall Rule Auditing Matters for Compliance
      - Reference "current TSA pipeline security directive" and NIST SP 800-82 Rev 3 Chapter 5
      - Risk of rule accumulation: legacy "any-any" rules from decommissioned systems
    - H2: Enumerating Firewall Rules with PowerShell
      - Get-NetFirewallRule with Select-Object
      - Filter enabled rules by direction and action
      - Export to CSV for evidence: Export-Csv -Path C:\Audit\firewall-rules.csv
    - OT callout block (mandatory):
      > [!OT]
      > OT workstations (HMI, SCADA servers) often run Windows Firewall in public profile
      > because they are not domain-joined. Run Get-NetFirewallProfile to confirm which
      > profile is active before auditing rules. NIST SP 800-82 Rev 3 Section 5.4.3
      > requires documenting firewall rule exceptions for OT devices.
    - H2: Identifying Rule Violations
      - Flags: RemoteAddress=Any AND Action=Allow with no local scope restriction
    - At least one ```powershell code block

    Content rules: No "TSA SD-02F", use "current TSA pipeline security directive".
    Env identifiers: PIPELINE-DC01, PIPELINE-HMI01, ExampleCorp.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - public/data/modules/network-hardening/lessons/04-ot-network-segmentation.md exists
    - public/data/modules/network-hardening/lessons/05-firewall-audit.md exists
    - Both files contain "> [!OT]" callout block
    - Both files reference "current TSA pipeline security directive" (not "TSA SD-02F")
    - Both files reference "NIST SP 800-82"
    - 04-ot-network-segmentation.md frontmatter: lessonId: ot-network-segmentation, order: 4, lastReviewed: '', reviewer: ''
    - 05-firewall-audit.md frontmatter: lessonId: firewall-audit, order: 5, quizId: '02', lastReviewed: '', reviewer: ''
    - npm test exits 0
  </acceptance_criteria>
  <done>Two new network-hardening lessons exist with correct frontmatter, OT callouts, and compliance citations</done>
</task>

<task type="auto">
  <name>Task 2: Author quizzes/02.json and scenarios/02.json; update modules-config.js</name>
  <files>
    public/data/modules/network-hardening/quizzes/02.json,
    public/data/modules/network-hardening/scenarios/02.json,
    src/modules-config.js
  </files>
  <read_first>
    - public/data/modules/network-hardening/quizzes/01.json (full file — quiz schema to replicate)
    - public/data/modules/network-hardening/scenarios/01.json (full file — 2-phase scenario schema)
    - src/modules-config.js (full file — read the CURRENT state which may already have logging-auditing updates from plan 09-05 running in parallel; do NOT overwrite those changes)
  </read_first>
  <action>
    IMPORTANT: Plans 09-05 and 09-06 both modify modules-config.js. If running after 09-05
    has already appended logging-auditing lessons, the file will have those new entries.
    Read the CURRENT file state before editing to avoid overwriting the other plan's changes.
    Only edit the network-hardening lessons array — leave all other module arrays untouched.

    QUIZZES/02.JSON: Create with schema:
    {
      "id": "network-hardening-quiz-02",
      "moduleId": "network-hardening",
      "title": "Network Hardening — Advanced Knowledge Check",
      "questions": [
        q-01: Purdue model zone isolation — which zone contains HMI workstations?
              complianceControls: ["NIST-SC-7"]
              4 answers, Level 2 is correct
        q-02: What PowerShell cmdlet exports firewall rules to a CSV file?
              complianceControls: ["NIST-CM-7", "TSA-NetworkSeg"]
              4 answers, Export-Csv after Get-NetFirewallRule is correct
        q-03: Under the current TSA pipeline security directive, what is required when
              an IT-to-OT direct connection cannot be avoided?
              complianceControls: ["TSA-NetworkSeg", "NIST-RA-3"]
              4 answers, written risk acceptance + compensating controls is correct
      ]
    }
    No "SD-02F" in any strings. Use "current TSA pipeline security directive".

    SCENARIOS/02.JSON: Create file:
    {
      "id": "network-hardening-scenario-02",
      "moduleId": "network-hardening",
      "title": "Emergency Remote Access to OT Segment",
      "complianceControls": ["TSA-NetworkSeg", "NIST-AC-17"],
      "narrative": "An operator at ExampleCorp reports a SCADA system alarm from home
        during off-hours. The IT/OT team is asked to enable remote access to the OT
        segment for troubleshooting.\n\nIn OT environments: Remote access to OT control
        systems must go through a jump host or VPN gateway in the IT/OT DMZ — direct
        RDP from IT into OT is prohibited by the current TSA pipeline security directive.",
      phases: [
        phase-1: decision, isFinal:false
          opt-a (wrong): Open RDP directly from IT network to SCADA server → nextPhaseId: phase-2-it
          opt-b (correct, OT-aware): Route through DMZ jump host → nextPhaseId: phase-2-ot
        phase-2-ot: isFinal:true — jump host route contains audit trail + session recording;
                    aligns with NIST SP 800-82 Rev 3 Section 5.4; completes successfully
        phase-2-it: isFinal:true — direct RDP creates unlogged lateral path; violates
                    network segmentation requirement; must add compensating control
      ]
    }
    Verify all non-null nextPhaseId values match real phase ids before completing.

    MODULES-CONFIG.JS: Locate the network-hardening module's lessons array.
    Append two entries after 'firewall-policy':
      { id: 'ot-network-segmentation', title: 'OT Network Segmentation and DMZ Architecture' },
      { id: 'firewall-audit',          title: 'Firewall Rule Auditing with PowerShell', quizId: '02' },
    Leave all other module blocks unchanged.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - public/data/modules/network-hardening/quizzes/02.json exists and is valid JSON
    - 02.json "id" equals "network-hardening-quiz-02"
    - 02.json has 3 questions with exactly 1 correct answer each
    - 02.json contains no "SD-02F" string
    - public/data/modules/network-hardening/scenarios/02.json exists and is valid JSON
    - scenarios/02.json "id" equals "network-hardening-scenario-02"
    - scenarios/02.json phases contain phase-2-ot (isFinal: true) and phase-2-it (isFinal: true)
    - All non-null nextPhaseId values in scenarios/02.json resolve to real phase ids
    - src/modules-config.js network-hardening lessons array has 5 entries
    - src/modules-config.js contains: { id: 'firewall-audit', ..., quizId: '02' }
    - src/modules-config.js logging-auditing entries are not corrupted (if 09-05 ran first)
    - npm test exits 0
  </acceptance_criteria>
  <done>network-hardening module has quiz 02.json (3 questions), scenario 02.json (OT/IT branching), modules-config.js updated to 5 lessons; tests GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN static files → browser | New .md and .json served from GitHub Pages |
| scenarios/02.json → scenario-engine | validateScenario() validates all nextPhaseId refs before rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Tampering | modules-config.js parallel edit | mitigate | Both plan 09-05 and 09-06 edit modules-config.js. If sequential, the later plan MUST read the current file state first. Executor must not overwrite changes from the first plan. Verified by checking all 5 modules retain correct lesson counts after both plans complete. |
| T-09-14 | Spoofing | scenarios/02.json nextPhaseId | mitigate | validateScenario() enforces nextPhaseId integrity before render. |
</threat_model>

<verification>
After both tasks:
- Navigate to #/lesson/network-hardening/ot-network-segmentation — lesson renders
- Navigate to #/lesson/network-hardening/firewall-audit — lesson renders with quiz section
- npm test exits 0
- src/modules-config.js shows 5 lessons for network-hardening AND preserved lessons for other modules
</verification>

<success_criteria>
Plan 06 complete when:
- network-hardening module shows 5 lessons in sidebar
- Firewall audit lesson renders quiz section backed by 02.json
- Advanced scenario 02 renders with OT/IT branching
- npm test exits 0
- modules-config.js changes from plan 09-05 are preserved
</success_criteria>

<output>
After completion, create .planning/phases/09-compliance-currency-content-depth/09-06-SUMMARY.md
</output>
