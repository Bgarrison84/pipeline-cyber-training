---
phase: 09-compliance-currency-content-depth
plan: 05
type: execute
wave: 3
depends_on:
  - "09-03"
  - "09-04"
files_modified:
  - public/data/modules/logging-auditing/lessons/04-ot-logging-advanced.md
  - public/data/modules/logging-auditing/lessons/05-siem-integration.md
  - public/data/modules/logging-auditing/quizzes/02.json
  - public/data/modules/logging-auditing/scenarios/02.json
  - src/modules-config.js
autonomous: true
requirements:
  - CONT-05
  - CONT-06
  - CONT-07
must_haves:
  truths:
    - "Two new lessons appear in the logging-auditing sidebar and are reachable via direct URL"
    - "Lesson 05-siem-integration.md renders a quiz section backed by quizzes/02.json"
    - "New scenario 02.json passes validateScenario() with branching OT/IT paths"
    - "Both new lessons contain at least one > [!OT] callout block"
    - "Both new lessons cite a TSA SD-02 series control and a NIST SP 800-82 chapter"
  artifacts:
    - path: "public/data/modules/logging-auditing/lessons/04-ot-logging-advanced.md"
      provides: "Advanced OT logging lesson with OT callout and compliance citations"
      contains: "> [!OT]"
    - path: "public/data/modules/logging-auditing/lessons/05-siem-integration.md"
      provides: "SIEM integration lesson with quiz wiring (quizId: '02')"
      contains: "quizId: '02'"
    - path: "public/data/modules/logging-auditing/quizzes/02.json"
      provides: "Advanced quiz with 3+ questions; id=logging-auditing-quiz-02"
      contains: '"id": "logging-auditing-quiz-02"'
    - path: "public/data/modules/logging-auditing/scenarios/02.json"
      provides: "Multi-branch OT/IT scenario; id=logging-auditing-scenario-02"
      contains: '"id": "logging-auditing-scenario-02"'
    - path: "src/modules-config.js"
      provides: "logging-auditing MODULES entry with 5 lessons including new lessons at order 4 and 5"
  key_links:
    - from: "src/modules-config.js"
      to: "public/data/modules/logging-auditing/lessons/04-ot-logging-advanced.md"
      via: "MODULES logging-auditing.lessons array entry id: 'ot-logging-advanced'"
      pattern: "id: 'ot-logging-advanced'"
    - from: "src/modules-config.js"
      to: "public/data/modules/logging-auditing/quizzes/02.json"
      via: "MODULES entry quizId: '02' + lesson frontmatter quizId: '02'"
      pattern: "quizId: '02'"
    - from: "src/modules-config.js"
      to: "public/data/modules/logging-auditing/scenarios/02.json"
      via: "MODULES entry scenarioId: '02' on intro-style scenario lesson"
      pattern: "scenarioId: '02'"
---

<objective>
Author all new Phase 9 content for the Logging & Auditing module: two new lessons,
one new quiz file (02.json), one new scenario (02.json), and update modules-config.js
to register all new content simultaneously.

Purpose: CONT-05/06/07 require ≥2 new lessons, ≥1 new advanced OT scenario, and a new
quiz bank per module. The atomic modules-config.js update ensures every new file is
immediately reachable via sidebar, URL, and progress tracking.

Output:
  - 2 new lesson .md files (order 4 + 5) with OT callouts and compliance citations
  - quizzes/02.json with 3+ questions (id: logging-auditing-quiz-02)
  - scenarios/02.json with OT/IT branching (id: logging-auditing-scenario-02)
  - src/modules-config.js logging-auditing lessons array updated to 5 entries
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-PLAN-EXECUTE.md
@C:\Users\psyco\Claude CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-03-SUMMARY.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-04-SUMMARY.md
</context>

<interfaces>
From src/modules-config.js (current logging-auditing lessons array — lines 15-19):
```javascript
lessons: [
  { id: 'intro',          title: 'Introduction to Windows Event Logs', scenarioId: '01' },
  { id: 'ps-logging',     title: 'Enabling PowerShell Script Block Logging', exerciseId: '01' },
  { id: 'audit-policies', title: 'Configuring Audit Policies via Group Policy', quizId: '01' },
],
```

New entries to append (both must be added atomically with lesson files):
```javascript
{ id: 'ot-logging-advanced', title: 'Advanced OT Log Collection and Retention', },
{ id: 'siem-integration',    title: 'SIEM Integration for Pipeline OT Environments', quizId: '02' },
```

Lesson frontmatter pattern (from audit-policies.md / 09-PATTERNS.md):
```yaml
---
title: {title}
lessonId: {id matching MODULES entry}
moduleId: logging-auditing
order: {4 or 5}
complianceTags: [TSA, NIST]
complianceControls: [TSA-Monitoring, NIST-AU-9]
quizId: '02'      # only on siem-integration lesson
lastReviewed: ''
reviewer: ''
---
```

Quiz 02.json schema from 09-PATTERNS.md (id: "logging-auditing-quiz-02"):
```json
{
  "id": "logging-auditing-quiz-02",
  "moduleId": "logging-auditing",
  "title": "Logging & Auditing — Advanced Knowledge Check",
  "questions": [ ... 3+ questions ... ]
}
```

Scenario branching pattern (OT/IT split via two isFinal phases) from 09-PATTERNS.md.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author lesson 04-ot-logging-advanced.md and lesson 05-siem-integration.md</name>
  <files>
    public/data/modules/logging-auditing/lessons/04-ot-logging-advanced.md,
    public/data/modules/logging-auditing/lessons/05-siem-integration.md
  </files>
  <read_first>
    - public/data/modules/logging-auditing/lessons/audit-policies.md (full file — frontmatter schema + OT callout pattern + lesson structure to replicate)
    - public/data/modules/logging-auditing/lessons/intro.md (OT callout + NERC CIP disclaimer pattern)
  </read_first>
  <action>
    Create 04-ot-logging-advanced.md with:
    Frontmatter: title="Advanced OT Log Collection and Retention", lessonId='ot-logging-advanced',
    moduleId='logging-auditing', order=4, complianceTags=[TSA, NIST],
    complianceControls=[TSA-Monitoring, NIST-AU-9, NIST-AU-11], lastReviewed='', reviewer=''

    Content outline (≥400 words, structured prose with headers):
    - H2: Why OT Log Collection Differs from IT
      - Discuss WEF (Windows Event Forwarding) limitations on air-gapped OT segments
      - Reference "current TSA pipeline security directive" monitoring mandate (not "SD-02F")
      - Reference NIST SP 800-82 Rev 3 Chapter 6 (OT security architecture)
    - H2: Collector Architecture for Isolated OT Segments
      - WEC (Windows Event Collector) on a DMZ host with one-way data diode pattern
      - PowerShell example: Get-WinEvent -ComputerName PIPELINE-HMI01 -LogName Security
    - OT callout block (mandatory):
      > [!OT]
      > In HMI stations and SCADA servers on air-gapped OT networks, Windows Event Forwarding
      > subscriptions cannot traverse the DMZ without explicit firewall rules on ports 5985/5986.
      > Use a collector host in the OT DMZ and forward consolidated logs to the enterprise SIEM.
      > NIST SP 800-82 Rev 3 Section 6.2 specifies event collection architecture for ICS.
    - H2: Retention Policy for OT Logs
      - Minimum retention per current TSA directive (generic phrasing)
      - PowerShell: Set-LogProperties to configure maximum log size
    - Include at least one ```powershell code block

    Create 05-siem-integration.md with:
    Frontmatter: title="SIEM Integration for Pipeline OT Environments", lessonId='siem-integration',
    moduleId='logging-auditing', order=5, complianceTags=[TSA, NIST],
    complianceControls=[TSA-Monitoring, NIST-SI-4, NIST-AU-12], quizId='02',
    lastReviewed='', reviewer=''

    Content outline (≥400 words):
    - H2: What SIEM Receives from OT
      - syslog vs. WEF vs. API forwarding — why WEF preferred for Windows OT
      - Reference "current TSA pipeline security directive" continuous monitoring requirement
      - Reference NIST SP 800-82 Rev 3 Chapter 6 continuous monitoring guidance
    - H2: Normalizing OT Events
      - Common event IDs from OT context: 4624, 4625, 4688, 4104
      - PowerShell: Get-WinEvent filtering pipeline (Where-Object Level -le 2)
    - OT callout block (mandatory):
      > [!OT]
      > On HMI and SCADA workstations with older Windows versions (2012R2 or earlier),
      > PowerShell Remoting may not be available. Use agentless WMI event subscription
      > as a fallback. NIST SP 800-82 Rev 3 Section 6.3 addresses legacy OT platform constraints.
    - H2: Alert Tuning for Pipeline Environments
      - Whitelisting known OT process hashes to reduce false positives
    - Include at least one ```powershell code block

    Content rules (enforced):
    - No "TSA SD-02F" or any version string — use "current TSA pipeline security directive"
    - No "SD-02G" or any assumed successor
    - Environment identifiers: PIPELINE-DC01, PIPELINE-HMI01, 10.0.0.0/24, ExampleCorp
    - NERC CIP mentions include: "NERC CIP governs electric utilities; pipeline operators follow TSA directives — referenced here as a maturity benchmark"
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - public/data/modules/logging-auditing/lessons/04-ot-logging-advanced.md exists
    - public/data/modules/logging-auditing/lessons/05-siem-integration.md exists
    - Both files contain "> [!OT]" callout block
    - Both files contain "current TSA pipeline security directive" (not "TSA SD-02F")
    - Both files contain "NIST SP 800-82" citation
    - 04-ot-logging-advanced.md frontmatter: lessonId: ot-logging-advanced, order: 4, lastReviewed: '', reviewer: ''
    - 05-siem-integration.md frontmatter: lessonId: siem-integration, order: 5, quizId: '02', lastReviewed: '', reviewer: ''
    - Neither file contains "SD-02F", "SD-02G", or any specific version string
    - npm test exits 0
  </acceptance_criteria>
  <done>Two new logging-auditing lessons exist with correct frontmatter, OT callouts, and compliance citations</done>
</task>

<task type="auto">
  <name>Task 2: Author quizzes/02.json and scenarios/02.json; update modules-config.js</name>
  <files>
    public/data/modules/logging-auditing/quizzes/02.json,
    public/data/modules/logging-auditing/scenarios/02.json,
    src/modules-config.js
  </files>
  <read_first>
    - public/data/modules/logging-auditing/quizzes/01.json (full file — exact schema to replicate)
    - public/data/modules/logging-auditing/scenarios/01.json (full file — 2-phase scenario schema)
    - src/modules-config.js (full file — read before editing; understand current lessons array)
  </read_first>
  <action>
    QUIZZES/02.JSON: Create file with schema:
    {
      "id": "logging-auditing-quiz-02",
      "moduleId": "logging-auditing",
      "title": "Logging & Auditing — Advanced Knowledge Check",
      "questions": [
        q-01: About WEF subscription types (push vs. pull) — complianceControls: ["NIST-AU-12"]
          4 answers, 1 correct, per-answer feedback, explanation
        q-02: About SIEM log normalization for OT pipeline environments
          — complianceControls: ["TSA-Monitoring", "NIST-SI-4"]
          4 answers, 1 correct, per-answer feedback, explanation
        q-03: About minimum log retention under the current TSA directive
          — complianceControls: ["TSA-Monitoring", "NIST-AU-11"]
          4 answers, 1 correct, per-answer feedback, explanation
      ]
    }
    Use "current TSA pipeline security directive" — NOT "TSA SD-02F" — in all stems,
    feedback, and explanations. Each question follows the exact q-0N id pattern.
    Exactly one answer per question has "correct": true. Every answer has 4 fields: id, text, correct, feedback.

    SCENARIOS/02.JSON: Create file with OT/IT branching structure:
    {
      "id": "logging-auditing-scenario-02",
      "moduleId": "logging-auditing",
      "title": "Log Retention Decision in an Air-Gapped OT Segment",
      "complianceControls": ["TSA-Monitoring", "NIST-AU-11"],
      "narrative": "You are the IT/OT admin at ExampleCorp. Your SIEM shows a gap in
        OT log collection from the PIPELINE-HMI01 segment.\n\nIn OT environments:
        HMI and SCADA systems may have limited disk capacity for local log storage and
        limited bandwidth to forward logs to central collection.",
      phases: [
        phase-1: decision, isFinal:false, options point to phase-2-ot (OT-aware choice) and phase-2-it (IT standard)
        phase-2-ot: isFinal:true — addresses air-gap constraints with DMZ collector approach
        phase-2-it: isFinal:true — standard enterprise SIEM forwarding, correct for IT but risky for air-gapped OT
      ]
    }
    Set "correct": true on the option that selects the OT-aware path. Set all terminal
    options' nextPhaseId: null. Verify all non-null nextPhaseId values match real phase ids
    before completing this task.

    MODULES-CONFIG.JS: In the logging-auditing module's lessons array, append two new entries
    after 'audit-policies':
      { id: 'ot-logging-advanced', title: 'Advanced OT Log Collection and Retention' },
      { id: 'siem-integration',    title: 'SIEM Integration for Pipeline OT Environments', quizId: '02' },

    The 'ot-logging-advanced' lesson is a pure reading lesson (completes on visit) — no quizId,
    exerciseId, or scenarioId. The 'siem-integration' lesson gets quizId: '02'.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - public/data/modules/logging-auditing/quizzes/02.json exists and is valid JSON
    - 02.json "id" field equals "logging-auditing-quiz-02"
    - 02.json contains exactly 3 questions (q-01, q-02, q-03)
    - Each question has exactly 4 answers and exactly 1 answer with "correct": true
    - 02.json contains no "SD-02F" string anywhere
    - public/data/modules/logging-auditing/scenarios/02.json exists and is valid JSON
    - scenarios/02.json "id" field equals "logging-auditing-scenario-02"
    - scenarios/02.json phases array contains phase-2-ot and phase-2-it (two separate final phases)
    - scenarios/02.json: every non-null nextPhaseId resolves to a real phase id in the phases array
    - src/modules-config.js logging-auditing lessons array contains 5 entries (3 original + 2 new)
    - src/modules-config.js contains: { id: 'siem-integration', ..., quizId: '02' }
    - npm test exits 0 (quiz-engine test for quizId '02' URL is now GREEN)
  </acceptance_criteria>
  <done>logging-auditing module has quiz 02.json (3 questions), scenario 02.json (OT/IT branching), and modules-config.js updated to 5 lessons; quiz-engine quizId 02 test GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN static files → browser | New .md and .json content files served from GitHub Pages |
| scenario JSON → scenario-engine | validateScenario() validates structure before render; no user input flows back into JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | quizzes/02.json answer "correct" fields | accept | Static JSON served from CDN; no server-side validation needed. Users cannot modify the canonical answer key via browser. |
| T-09-11 | Spoofing | scenarios/02.json nextPhaseId resolution | mitigate | validateScenario() checks all nextPhaseId values resolve before rendering — a typo causes graceful error state rather than silent wrong-path routing. Verify all nextPhaseId values before commit. |
| T-09-12 | Information Disclosure | lesson .md content | accept | Lesson content is public educational material. No PII or credentials. |
</threat_model>

<verification>
After both tasks:
- Navigate to #/lesson/logging-auditing/ot-logging-advanced — lesson renders
- Navigate to #/lesson/logging-auditing/siem-integration — lesson renders with quiz section
- Navigate to #/scenario/logging-auditing/2 (or the route pattern for scenario 02) — scenario renders
- npm test exits 0
- grep confirms no "SD-02F" in either new lesson or quiz file
</verification>

<success_criteria>
Plan 05 complete when:
- logging-auditing module shows 5 lessons in sidebar
- SIEM lesson renders quiz section backed by 02.json
- Advanced scenario 02 renders with OT/IT branching
- npm test exits 0 including the quizId '02' quiz-engine test
</success_criteria>

<output>
After completion, create .planning/phases/09-compliance-currency-content-depth/09-05-SUMMARY.md
</output>
