---
phase: 09-compliance-currency-content-depth
plan: 07
type: execute
wave: 4
depends_on:
  - "09-05"
  - "09-06"
files_modified:
  - public/data/modules/account-access/lessons/04-privileged-access-ot.md
  - public/data/modules/account-access/lessons/05-ad-audit.md
  - public/data/modules/account-access/quizzes/02.json
  - public/data/modules/account-access/scenarios/02.json
  - src/modules-config.js
autonomous: true
requirements:
  - CONT-05
  - CONT-06
  - CONT-07
must_haves:
  truths:
    - "Two new lessons appear in the account-access sidebar and are reachable via direct URL"
    - "Lesson 05-ad-audit.md renders a quiz section backed by quizzes/02.json"
    - "New scenario 02.json passes validateScenario() with OT/IT branching"
    - "Both new lessons contain at least one > [!OT] callout block"
    - "Both new lessons cite a TSA control and a NIST SP 800-82 chapter"
  artifacts:
    - path: "public/data/modules/account-access/lessons/04-privileged-access-ot.md"
      provides: "Privileged access OT lesson with OT callout"
      contains: "> [!OT]"
    - path: "public/data/modules/account-access/lessons/05-ad-audit.md"
      provides: "AD audit lesson with quiz wiring (quizId: '02')"
      contains: "quizId: '02'"
    - path: "public/data/modules/account-access/quizzes/02.json"
      provides: "Advanced access quiz; id=account-access-quiz-02"
      contains: '"id": "account-access-quiz-02"'
    - path: "public/data/modules/account-access/scenarios/02.json"
      provides: "OT/IT branching scenario; id=account-access-scenario-02"
      contains: '"id": "account-access-scenario-02"'
    - path: "src/modules-config.js"
      provides: "account-access MODULES entry with 5 lessons"
  key_links:
    - from: "src/modules-config.js"
      to: "public/data/modules/account-access/quizzes/02.json"
      via: "MODULES entry quizId: '02' on ad-audit lesson"
      pattern: "quizId: '02'"
---

<objective>
Author all new Phase 9 content for the Account & Access Management module: two new lessons,
one new quiz file (02.json), one new scenario (02.json), and update modules-config.js.
Runs in parallel with Plan 09-08 (incident-response) — different module blocks in modules-config.js.

Purpose: CONT-05/06/07 for account-access module.

Output:
  - 2 new lesson .md files (order 4 + 5)
  - quizzes/02.json (3 questions, id: account-access-quiz-02)
  - scenarios/02.json (OT/IT branching, id: account-access-scenario-02)
  - modules-config.js account-access lessons updated to 5 entries
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-PLAN-EXECUTE.md
@C:\Users\psyco\Claude CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-05-SUMMARY.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-06-SUMMARY.md
</context>

<interfaces>
From src/modules-config.js (current account-access lessons array — lines 43-47):
```javascript
lessons: [
  { id: 'intro',         title: 'Account and Access Control Overview',        scenarioId: '01' },
  { id: 'ps-ad',         title: 'Active Directory Queries with PowerShell',   exerciseId: '01' },
  { id: 'access-policy', title: 'Least Privilege and Service Account Policy', quizId: '01' },
],
```

New entries to append:
```javascript
{ id: 'privileged-access-ot', title: 'Privileged Access Management for OT Environments' },
{ id: 'ad-audit',             title: 'Auditing Active Directory with PowerShell', quizId: '02' },
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author lesson 04-privileged-access-ot.md and lesson 05-ad-audit.md</name>
  <files>
    public/data/modules/account-access/lessons/04-privileged-access-ot.md,
    public/data/modules/account-access/lessons/05-ad-audit.md
  </files>
  <read_first>
    - public/data/modules/account-access/lessons/access-policy.md (full file — frontmatter schema for this module)
    - public/data/modules/account-access/lessons/intro.md (OT callout pattern in this module)
  </read_first>
  <action>
    Create 04-privileged-access-ot.md:
    Frontmatter: title="Privileged Access Management for OT Environments",
    lessonId='privileged-access-ot', moduleId='account-access', order=4,
    complianceTags=[TSA, NIST], complianceControls=[TSA-Access, NIST-AC-2, NIST-AC-6],
    lastReviewed='', reviewer=''

    Content outline (≥400 words):
    - H2: Why Privileged Access is Higher Risk in OT
      - Shared/generic accounts on HMI and SCADA workstations (common anti-pattern)
      - Reference "current TSA pipeline security directive" personnel access controls
      - Reference NIST SP 800-82 Rev 3 Chapter 6 (ICS account management)
    - H2: PAM Approaches for Pipeline OT
      - Just-in-time (JIT) access vs. standing privileged accounts
      - PowerShell: Get-ADGroupMember 'Domain Admins' to enumerate privileged accounts
      - PowerShell: Get-ADUser -Filter {Enabled -eq $True} | Select-Object SamAccountName,LastLogonDate
    - OT callout block (mandatory):
      > [!OT]
      > In OT environments, HMI and SCADA workstations are often not domain-joined.
      > Local administrator accounts on these systems must be managed via LAPS (Local
      > Administrator Password Solution) or equivalent. Standing shared admin passwords
      > violate NIST SP 800-82 Rev 3 Section 6.3.3 least-privilege requirements.
    - H2: Service Account Hygiene for OT Systems
      - Service accounts that run OT software (historian, OPC server) must use managed service accounts (MSA)
    - At least one ```powershell code block

    Create 05-ad-audit.md:
    Frontmatter: title="Auditing Active Directory with PowerShell",
    lessonId='ad-audit', moduleId='account-access', order=5,
    complianceTags=[TSA, NIST], complianceControls=[TSA-Access, NIST-AC-2, NIST-AU-2],
    quizId='02', lastReviewed='', reviewer=''

    Content outline (≥400 words):
    - H2: What to Audit in Active Directory
      - Stale accounts, password age, group membership changes
      - Reference "current TSA pipeline security directive" and NIST SP 800-82 Rev 3 Chapter 6
    - H2: Querying AD for Compliance Evidence
      - PowerShell: Search-ADAccount -AccountDisabled | Select-Object SamAccountName,DistinguishedName
      - PowerShell: Get-ADUser -Filter {PasswordNeverExpires -eq $True} | Select SamAccountName
      - Export-Csv pattern for audit evidence: -Path C:\Audit\stale-accounts.csv
    - OT callout block (mandatory):
      > [!OT]
      > OT service accounts (historian service, OPC DA server, DCS communication accounts)
      > must appear in the AD audit — they are high-value targets because they often have
      > elevated local permissions on OT workstations. Flag any account with PasswordNeverExpires
      > that also appears in local Administrators. NIST SP 800-82 Rev 3 Section 6.3 applies.
    - H2: Automating Monthly AD Audit Reports
    - At least one ```powershell code block

    Content rules: No "TSA SD-02F". Env: PIPELINE-DC01, ExampleCorp, 10.0.0.0/24.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - public/data/modules/account-access/lessons/04-privileged-access-ot.md exists
    - public/data/modules/account-access/lessons/05-ad-audit.md exists
    - Both files contain "> [!OT]" callout block
    - Both files contain "current TSA pipeline security directive"
    - Both files contain "NIST SP 800-82"
    - 04-privileged-access-ot.md: lessonId: privileged-access-ot, order: 4, lastReviewed: '', reviewer: ''
    - 05-ad-audit.md: lessonId: ad-audit, order: 5, quizId: '02', lastReviewed: '', reviewer: ''
    - npm test exits 0
  </acceptance_criteria>
  <done>Two new account-access lessons exist with correct frontmatter, OT callouts, and compliance citations</done>
</task>

<task type="auto">
  <name>Task 2: Author quizzes/02.json and scenarios/02.json; update modules-config.js</name>
  <files>
    public/data/modules/account-access/quizzes/02.json,
    public/data/modules/account-access/scenarios/02.json,
    src/modules-config.js
  </files>
  <read_first>
    - public/data/modules/account-access/quizzes/01.json (full file — quiz schema)
    - public/data/modules/account-access/scenarios/01.json (full file — scenario schema)
    - src/modules-config.js (full file — read CURRENT state; plans 09-05 and 09-06 may have already added entries for logging-auditing and network-hardening)
  </read_first>
  <action>
    QUIZZES/02.JSON:
    {
      "id": "account-access-quiz-02",
      "moduleId": "account-access",
      "title": "Account & Access Management — Advanced Knowledge Check",
      "questions": [
        q-01: What PowerShell cmdlet enumerates all disabled AD accounts?
              Correct: Search-ADAccount -AccountDisabled
              complianceControls: ["NIST-AC-2"]
        q-02: Under the current TSA pipeline security directive, which account type must
              NOT have standing administrative access to OT workstations?
              Correct: Shared/generic administrator accounts (requires named individual accounts)
              complianceControls: ["TSA-Access", "NIST-AC-6"]
        q-03: What is the recommended approach for managing local admin passwords on
              non-domain-joined OT workstations?
              Correct: LAPS (Local Administrator Password Solution) or equivalent
              complianceControls: ["NIST-AC-2", "NIST-IA-5"]
      ]
    }
    No "SD-02F" strings. 4 answers per question, exactly 1 correct.

    SCENARIOS/02.JSON:
    {
      "id": "account-access-scenario-02",
      "moduleId": "account-access",
      "title": "Contractor Access Request for OT System Maintenance",
      "complianceControls": ["TSA-Access", "NIST-AC-2", "NIST-AC-17"],
      "narrative": "A third-party contractor at ExampleCorp needs temporary admin access
        to a SCADA historian server to perform a software upgrade.\n\nIn OT environments:
        Contractor accounts on OT systems must be time-limited, individually named, and
        logged per the current TSA pipeline security directive.",
      phases: [
        phase-1: decision, isFinal: false
          opt-a (wrong): Give contractor existing shared 'Administrator' account
                          → nextPhaseId: phase-2-it
          opt-b (correct, OT-aware): Create named time-limited contractor account with
                                      JIT elevation → nextPhaseId: phase-2-ot
        phase-2-ot: isFinal:true — named account enables audit trail, aligns with NIST AC-2
        phase-2-it: isFinal:true — shared account violates TSA access control requirement;
                    audit trail lost
      ]
    }
    Verify all non-null nextPhaseId values.

    MODULES-CONFIG.JS: Read the CURRENT file (do not overwrite other modules' changes).
    Locate account-access lessons array. Append:
      { id: 'privileged-access-ot', title: 'Privileged Access Management for OT Environments' },
      { id: 'ad-audit',             title: 'Auditing Active Directory with PowerShell', quizId: '02' },
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - public/data/modules/account-access/quizzes/02.json exists and is valid JSON
    - 02.json "id" equals "account-access-quiz-02"
    - 02.json has 3 questions, each with exactly 4 answers and 1 correct
    - 02.json contains no "SD-02F" string
    - public/data/modules/account-access/scenarios/02.json exists and is valid JSON
    - scenarios/02.json "id" equals "account-access-scenario-02"
    - scenarios/02.json phases contain phase-2-ot and phase-2-it with isFinal:true
    - All non-null nextPhaseId values resolve to real phase ids
    - src/modules-config.js account-access lessons array has 5 entries
    - src/modules-config.js contains: { id: 'ad-audit', ..., quizId: '02' }
    - Previously added entries for logging-auditing and network-hardening are preserved
    - npm test exits 0
  </acceptance_criteria>
  <done>account-access module complete with quiz 02.json, scenario 02.json (OT/IT branching), and modules-config.js updated; tests GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN static files → browser | New .md and .json content files served from GitHub Pages |
| modules-config.js parallel edit | Wave 4 runs 09-07 and 09-08 in parallel; both edit modules-config.js (different module blocks) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Tampering | modules-config.js parallel edit | mitigate | Executor must read current file state before editing. Edit only account-access lessons array. Verify other module arrays (logging-auditing, network-hardening, incident-response) are untouched after edit. |
| T-09-16 | Information Disclosure | AD audit lesson content | accept | Lesson uses generic identifiers (ExampleCorp, PIPELINE-DC01). No real org data or real AD credentials referenced. |
</threat_model>

<verification>
After both tasks:
- Navigate to #/lesson/account-access/privileged-access-ot — lesson renders
- Navigate to #/lesson/account-access/ad-audit — lesson renders with quiz section
- npm test exits 0
- src/modules-config.js shows 5 lessons for account-access and other modules preserved
</verification>

<success_criteria>
Plan 07 complete when:
- account-access module shows 5 lessons in sidebar
- AD audit lesson renders quiz section backed by 02.json
- Advanced scenario 02 renders with OT/IT branching
- npm test exits 0
</success_criteria>

<output>
After completion, create .planning/phases/09-compliance-currency-content-depth/09-07-SUMMARY.md
</output>
