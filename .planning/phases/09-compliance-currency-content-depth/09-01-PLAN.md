---
phase: 09-compliance-currency-content-depth
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/badge-expired.test.js
  - tests/compliance-refs.test.js
  - tests/scenario-view.test.js
  - tests/quiz-engine.test.js
autonomous: true
requirements:
  - SME-01
  - SME-03
  - CONT-06
  - CONT-07
must_haves:
  truths:
    - "npm test exits non-zero (RED) because badge.js has no expired branch yet"
    - "compliance-refs.test.js asserts TSA status === 'expired' (fails until Wave 1 data change)"
    - "scenario-view.test.js has validateScenario() assertions on new scenario fixture shapes"
    - "quiz-engine.test.js has quizId '02' URL resolution assertions"
  artifacts:
    - path: "tests/badge-expired.test.js"
      provides: "Failing tests for renderBadge expired state"
      min_lines: 30
    - path: "tests/compliance-refs.test.js"
      provides: "Extended tests asserting status + expiryDate fields on TSA entry"
    - path: "tests/scenario-view.test.js"
      provides: "Extended tests calling validateScenario() with new multi-branch fixture"
    - path: "tests/quiz-engine.test.js"
      provides: "Extended tests for quizId 02 URL resolution"
  key_links:
    - from: "tests/badge-expired.test.js"
      to: "src/badge.js"
      via: "import { setComplianceRefs, renderBadge } from '../src/badge.js'"
      pattern: "setComplianceRefs.*status.*expired"
---

<objective>
Author all Wave 0 failing tests that guard Phase 9 behaviors. These tests must be RED
before any implementation begins. Subsequent plans implement code and data to make them GREEN.

Purpose: Establish the test gates that prove compliance currency, expired badge rendering,
scenario schema integrity, and quiz ID resolution are all correct before marking any
requirement done.

Output:
  - tests/badge-expired.test.js (new file)
  - tests/compliance-refs.test.js (extended — 2 new assertions)
  - tests/scenario-view.test.js (extended — validateScenario with multi-branch fixture)
  - tests/quiz-engine.test.js (extended — quizId '02' fetch URL assertion)
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-PLAN-EXECUTE.md
@C:\Users\psyco\Claude CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\ROADMAP.md

Phase 9 test infrastructure. All tests must go RED before implementation starts.
</context>

<interfaces>
From tests/compliance-refs.test.js (existing pattern to extend):
```javascript
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'fs'
import { fileURLToPath } from 'url'
import { dirname, resolve } from 'path'

const raw = readFileSync(resolve(__dirname, '../public/data/compliance-refs.json'), 'utf-8')
const refs = JSON.parse(raw)

describe('compliance-refs.json', () => {
  it('has schemaVersion equal to 1', () => { expect(refs.schemaVersion).toBe(1) })
  it('has directives.TSA.shortName as a non-empty string', () => { ... })
})
```

From src/badge.js (current, no expired branch — import directly for tests):
```javascript
export function setComplianceRefs(refs) { _complianceRefs = refs; }
export function renderBadge(directiveKey) { /* no expired branch yet */ }
```

From tests/scenario-view.test.js (existing validateScenario mock structure — lines 11-35):
```javascript
vi.mock('../src/progress-store.js', () => ({ progressStore: { ... } }))
vi.mock('../src/badge.js', () => ({ renderBadge: vi.fn().mockReturnValue('') }))
import { renderScenario } from '../src/views/scenario-view.js'
```

From tests/quiz-engine.test.js (existing fetch mock structure — extend with quizId 02 case):
```javascript
vi.mock('../src/modules-config.js', () => ({ MODULES: [ { lessons: [{ quizId: '01' }] } ] }))
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Author badge-expired.test.js (new file, RED)</name>
  <files>tests/badge-expired.test.js</files>
  <read_first>
    - tests/compliance-refs.test.js (full file — Vitest import pattern, readFileSync pattern)
    - tests/scenario-view.test.js lines 1-35 (vi.mock() hoisting structure)
    - src/badge.js (full file — setComplianceRefs + renderBadge signatures)
  </read_first>
  <behavior>
    - Test 1: renderBadge('TSA') with status='expired' in refs returns HTML containing 'text-decoration: line-through'
    - Test 2: renderBadge('TSA') with status='expired' returns HTML containing '[EXPIRED]'
    - Test 3: renderBadge('TSA') with status='expired' returns HTML containing 'title=' attribute
    - Test 4: renderBadge('TSA') with status='expired' returns HTML containing 'aria-label='
    - Test 5: renderBadge('NIST') with no status field does NOT contain '[EXPIRED]'
    - Test 6: renderBadge('NIST') with no status field contains 'NIST SP 800-82 Rev 3'
    All tests must FAIL (RED) at time of authoring because badge.js has no expired branch.
  </behavior>
  <action>
    Create tests/badge-expired.test.js. No vi.mock() needed — import badge.js directly and
    call setComplianceRefs() in beforeEach.

    beforeEach block: call setComplianceRefs with:
    {
      directives: {
        TSA: { shortName: 'TSA SD-02F', status: 'expired' },
        NIST: { shortName: 'NIST SP 800-82 Rev 3' }
      }
    }

    Write six it() blocks per the behavior list above. Use toContain() for presence checks
    and not.toContain() for absence checks. Include afterEach calling setComplianceRefs(null)
    to reset module state.

    Use the exact import header:
    import { describe, it, expect, beforeEach, afterEach } from 'vitest'
    import { setComplianceRefs, renderBadge } from '../src/badge.js'

    Run npm test after writing — confirm these 6 new tests are RED (FAIL) and all
    previously-passing 167 tests remain GREEN.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/badge-expired.test.js exists with exactly 6 it() blocks in 1 describe()
    - npm test output shows the 6 new badge tests as FAIL (RED) with "expected ... to contain"
    - All 167 pre-existing tests remain GREEN (no regressions)
    - File imports setComplianceRefs and renderBadge from '../src/badge.js'
    - beforeEach calls setComplianceRefs with TSA entry having status: 'expired'
  </acceptance_criteria>
  <done>6 new badge-expired tests are RED; 167 existing tests remain GREEN; npm test shows total failures = 6</done>
</task>

<task type="tdd">
  <name>Task 2: Extend compliance-refs.test.js and quiz-engine.test.js with Phase 9 assertions (RED)</name>
  <files>tests/compliance-refs.test.js, tests/quiz-engine.test.js, tests/scenario-view.test.js</files>
  <read_first>
    - tests/compliance-refs.test.js (full file — read before appending)
    - tests/quiz-engine.test.js lines 1-119 (mock structure + existing tests)
    - tests/scenario-view.test.js lines 1-100 (full mock + fixture structure)
    - public/data/compliance-refs.json (full file — current schema, missing status field)
  </read_first>
  <behavior>
    compliance-refs.test.js new assertions:
    - Test A: refs.directives.TSA.status === 'expired' (fails until Wave 1)
    - Test B: refs.directives.TSA.expiryDate === '2026-05-02' (fails until Wave 1)

    quiz-engine.test.js new assertions:
    - Test C: When renderQuiz('logging-auditing', '02', container, 'lesson-id') is called,
      fetch is called with a URL containing 'quizzes/02.json'

    scenario-view.test.js new assertions:
    - Test D: A new multi-branch scenario fixture with phase-2-ot and phase-2-it phases
      (different isFinal outcomes per path) passes validateScenario() — import validateScenario
      from scenario-view.js and call it directly with the fixture object.
    NOTE: Test D should be GREEN immediately (validateScenario already exists and works);
    Tests A, B, C should be RED until Wave 1/2 implementation.
  </behavior>
  <action>
    COMPLIANCE-REFS.TEST.JS: Append two new it() blocks inside the existing
    describe('compliance-refs.json', ...) block (after line 34, before the closing brace):
      it('has directives.TSA.status equal to "expired"', () => {
        expect(refs.directives.TSA.status).toBe('expired')
      })
      it('has directives.TSA.expiryDate equal to "2026-05-02"', () => {
        expect(refs.directives.TSA.expiryDate).toBe('2026-05-02')
      })

    QUIZ-ENGINE.TEST.JS: In the existing test suite, add a new it() block that calls
    renderQuiz('logging-auditing', '02', container, 'lesson-id') using the existing
    global.fetch mock pattern already in the file, then asserts:
      expect(fetchMock).toHaveBeenCalledWith(expect.stringContaining('quizzes/02.json'))
    Place it after the last existing test in the quiz-engine describe block.

    SCENARIO-VIEW.TEST.JS: Add a new describe block 'validateScenario multi-branch scenario'
    at the bottom of the file. Define a fixture with:
      id: 'logging-auditing-scenario-02', moduleId: 'logging-auditing',
      phases: [
        { id: 'phase-1', type: 'decision', isFinal: false, prompt: '...', title: '...',
          options: [
            { id: 'opt-a', text: '...', outcome: '...', correct: false, nextPhaseId: 'phase-2-it' },
            { id: 'opt-b', text: '...', outcome: '...', correct: true,  nextPhaseId: 'phase-2-ot' }
          ]
        },
        { id: 'phase-2-ot', type: 'decision', isFinal: true, title: '...', prompt: '...', options: [
            { id: 'opt-a', text: '...', outcome: '...', correct: true, nextPhaseId: null }
          ]
        },
        { id: 'phase-2-it', type: 'decision', isFinal: true, title: '...', prompt: '...', options: [
            { id: 'opt-a', text: '...', outcome: '...', correct: false, nextPhaseId: null }
          ]
        }
      ]
    Import validateScenario from '../src/views/scenario-view.js'. Assert:
      expect(validateScenario(fixture)).toBe(true)
    This test should be GREEN immediately (validateScenario already handles this shape).
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - tests/compliance-refs.test.js contains two new it() blocks: one checking .status === 'expired', one checking .expiryDate === '2026-05-02'
    - Both new compliance-refs tests are FAIL (RED) because compliance-refs.json currently has no status field
    - tests/quiz-engine.test.js contains a new it() block asserting fetch was called with a URL containing 'quizzes/02.json'
    - tests/scenario-view.test.js contains a new describe block with a multi-branch fixture test that calls validateScenario() — this test should be GREEN
    - npm test shows: previously 167 passing; now 168 passing (scenario validate passes) + 3 new RED failures (compliance status, compliance expiryDate, quiz 02 fetch)
  </acceptance_criteria>
  <done>Wave 0 test scaffold complete: 168 GREEN + correct number of RED failures for Phase 9 gates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files → src modules | Test code imports production modules directly; no external data crosses this boundary |
| Test files → public/data | compliance-refs.test.js reads the JSON file from disk via readFileSync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | tests/badge-expired.test.js | accept | Test files are not served by the app; no runtime attack surface. Vitest runs in Node.js only during development. |
| T-09-02 | Information Disclosure | compliance-refs.json (read by tests) | accept | File contains only public TSA/NIST reference data; no secrets or PII. |
</threat_model>

<verification>
After both tasks:
- npm test shows the 6 badge tests RED, 2 compliance-refs tests RED, 1 quiz-engine test RED
- 1 scenario validateScenario test GREEN (existing engine already handles multi-branch)
- All 167 pre-existing tests remain GREEN
- No import errors or syntax errors in new/modified test files
</verification>

<success_criteria>
Wave 0 complete when:
- tests/badge-expired.test.js exists with 6 failing tests for expired badge rendering
- tests/compliance-refs.test.js has 2 new failing assertions for status/expiryDate
- tests/quiz-engine.test.js has 1 new failing assertion for quizId '02' URL
- tests/scenario-view.test.js has 1 new passing test for multi-branch validateScenario
- npm test shows exactly these failures and no regressions in 167 original tests
</success_criteria>

<output>
After completion, create .planning/phases/09-compliance-currency-content-depth/09-01-SUMMARY.md
</output>
