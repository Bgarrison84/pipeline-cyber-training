---
phase: 09-compliance-currency-content-depth
plan: 02
type: execute
wave: 1
depends_on:
  - "09-01"
files_modified:
  - public/data/compliance-refs.json
  - src/badge.js
  - src/style.css
autonomous: true
requirements:
  - SME-01
must_haves:
  truths:
    - "The compliance badge on the live site renders an expired visual state for SD-02F"
    - "renderBadge('TSA') returns HTML with text-decoration: line-through and [EXPIRED] text"
    - "renderBadge('NIST') is visually unchanged (no [EXPIRED], no strikethrough)"
    - "npm test exits 0 (all badge-expired.test.js tests GREEN)"
  artifacts:
    - path: "public/data/compliance-refs.json"
      provides: "Canonical compliance directive data with TSA expired state"
      contains: '"status": "expired"'
    - path: "src/badge.js"
      provides: "Badge renderer with expired-state branch"
      exports: ["setComplianceRefs", "renderBadge"]
    - path: "src/style.css"
      provides: "Two new CSS custom properties for expired badge palette"
      contains: "--color-badge-expired-bg"
  key_links:
    - from: "public/data/compliance-refs.json"
      to: "src/badge.js"
      via: "main.js calls setComplianceRefs(refs) after fetching compliance-refs.json"
      pattern: 'status.*expired'
    - from: "src/style.css"
      to: "src/badge.js"
      via: "badge.js references var(--color-badge-expired-bg) as CSS class string"
      pattern: "--color-badge-expired-bg"
---

<objective>
Day 1 atomic task: Add `"status": "expired"` to the TSA entry in compliance-refs.json,
add two expired-palette CSS variables to style.css, and update badge.js to render the
expired state. All three files must change in a single logical unit — badge.js reads
the status field, style.css provides the visual tokens.

Purpose: TSA SD-02F expired on 2026-05-02. The badge must reflect this immediately.
This is the critical Day 1 task before any other phase work proceeds.

Output: Green npm test suite; expired badge rendering live for TSA; NIST badge unchanged.
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-PLAN-EXECUTE.md
@C:\Users\psyco\Claude CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\ROADMAP.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-UI-SPEC.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-01-SUMMARY.md
</context>

<interfaces>
From src/badge.js (current, 23 lines — full implementation):
```javascript
import { esc } from './utils/escape.js';
let _complianceRefs = null;
export function setComplianceRefs(refs) { _complianceRefs = refs; }
export function renderBadge(directiveKey) {
  const shortName = _complianceRefs?.directives?.[directiveKey]?.shortName ?? directiveKey;
  const colorClasses = {
    TSA:  'bg-[var(--color-badge-tsa-bg)] text-[var(--color-badge-tsa-text)]',
    NIST: 'bg-[var(--color-badge-nist-bg)] text-[var(--color-badge-nist-text)]',
  };
  const safeClasses = colorClasses[directiveKey] ?? '';
  return `<span class="inline-block rounded px-2 py-0.5 font-mono text-[var(--text-mono)] ${esc(safeClasses)}">${esc(shortName)}</span>`;
}
```

From src/style.css lines 16-20 (badge block — append 2 new lines after --color-badge-nist-text):
```css
--color-badge-tsa-bg: #1e3a5f;
--color-badge-tsa-text: #93c5fd;
--color-badge-nist-bg: #1a3a2a;
--color-badge-nist-text: #86efac;
```

From public/data/compliance-refs.json (current TSA block — lines 5-11):
```json
"TSA": {
  "name": "TSA Security Directive Pipeline-2021-02F",
  "shortName": "TSA SD-02F",
  "effectiveDate": "2025-05-03",
  "expiryDate": "2026-05-02",
  "sourceUrl": "https://www.tsa.gov/sites/default/files/tsa-security-directive-pipeline-2021-02f-and-memo-508c.pdf"
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add status field to compliance-refs.json and CSS expired tokens to style.css</name>
  <files>public/data/compliance-refs.json, src/style.css</files>
  <read_first>
    - public/data/compliance-refs.json (full file — read before editing)
    - src/style.css lines 1-30 (read badge block at lines 16-20 before editing)
  </read_first>
  <behavior>
    - compliance-refs.json TSA entry has "status": "expired" field after this task
    - compliance-refs.json TSA entry has "expiryDate": "2026-05-02" field (already exists — verify)
    - compliance-refs.json top-level "lastVerified" updated to "2026-05-17"
    - style.css @theme block contains --color-badge-expired-bg: #2a2a2a
    - style.css @theme block contains --color-badge-expired-text: #737373
    - NIST entry in compliance-refs.json is unchanged (no status field added)
  </behavior>
  <action>
    COMPLIANCE-REFS.JSON: Edit the TSA block to add "status": "expired" after the
    "expiryDate" field. Also update the top-level "lastVerified" field to "2026-05-17".
    Do NOT add any successor version string. Do NOT modify the NIST block.

    Exact TSA block after edit:
      "TSA": {
        "name": "TSA Security Directive Pipeline-2021-02F",
        "shortName": "TSA SD-02F",
        "effectiveDate": "2025-05-03",
        "expiryDate": "2026-05-02",
        "status": "expired",
        "sourceUrl": "https://www.tsa.gov/sites/default/files/tsa-security-directive-pipeline-2021-02f-and-memo-508c.pdf"
      }

    STYLE.CSS: In the @theme block, locate the line "--color-badge-nist-text: #86efac;"
    and append two lines immediately after it:
      --color-badge-expired-bg: #2a2a2a;
      --color-badge-expired-text: #737373;
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test -- --reporter=verbose 2>&1 | grep -E "compliance-refs|PASS|FAIL" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - compliance-refs.json TSA entry contains "status": "expired" (exact key-value pair)
    - compliance-refs.json TSA entry contains "expiryDate": "2026-05-02"
    - compliance-refs.json "lastVerified" top-level field value is "2026-05-17"
    - compliance-refs.json NIST entry has no "status" field
    - style.css contains --color-badge-expired-bg: #2a2a2a in the @theme block
    - style.css contains --color-badge-expired-text: #737373 in the @theme block
    - The two new compliance-refs tests from plan 09-01 are now GREEN
    - npm test shows no new failures
  </acceptance_criteria>
  <done>compliance-refs.json has expired TSA status; style.css has expired badge color tokens; compliance-refs tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement expired-state branch in badge.js</name>
  <files>src/badge.js</files>
  <read_first>
    - src/badge.js (full file — current 23 lines, read before rewriting)
    - src/utils/escape.js (full file — esc() function signature)
    - tests/badge-expired.test.js (full file — all 6 tests to satisfy)
  </read_first>
  <behavior>
    - renderBadge('TSA') when status === 'expired': returns span with bg-[var(--color-badge-expired-bg)] and text-[var(--color-badge-expired-text)] classes
    - renderBadge('TSA') when status === 'expired': contains inner <span style="text-decoration: line-through;">TSA SD-02F</span>
    - renderBadge('TSA') when status === 'expired': contains literal ' [EXPIRED]' text outside the inner span
    - renderBadge('TSA') when status === 'expired': outer span has title="This directive has expired. Verify successor at TSA.gov."
    - renderBadge('TSA') when status === 'expired': outer span has aria-label="TSA SD-02F — EXPIRED"
    - renderBadge('NIST') when status absent: returns span WITHOUT [EXPIRED], contains 'NIST SP 800-82 Rev 3'
    - renderBadge with unknown key and no refs: returns span with empty class and directiveKey as text (existing behavior unchanged)
  </behavior>
  <action>
    Rewrite src/badge.js with the expired-state branch. The full updated function:

    renderBadge(directiveKey) reads directive once via _complianceRefs?.directives?.[directiveKey].
    Derives shortName (directive?.shortName ?? directiveKey) and status (directive?.status ?? 'active').
    Defines expiredClasses constant: 'bg-[var(--color-badge-expired-bg)] text-[var(--color-badge-expired-text)]'.
    The colorClasses object stays unchanged.

    If status === 'expired':
      Return: outer span with class="inline-block rounded px-2 py-0.5 font-mono text-[var(--text-mono)] {esc(expiredClasses)}"
              title="This directive has expired. Verify successor at TSA.gov."
              aria-label="{esc(shortName)} — EXPIRED"
      Body: <span style="text-decoration: line-through;">{esc(shortName)}</span> [EXPIRED]

    Else (active branch — unchanged from current):
      Return: outer span with class="inline-block rounded px-2 py-0.5 font-mono text-[var(--text-mono)] {esc(safeClasses)}"
      Body: {esc(shortName)}

    Keep the import of esc from './utils/escape.js'. Keep setComplianceRefs() identical.
    Do NOT change the function signature or module-level variable name _complianceRefs.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (all tests GREEN including 6 badge-expired tests)
    - src/badge.js contains: if (status === 'expired')
    - src/badge.js contains: text-decoration: line-through
    - src/badge.js contains: [EXPIRED]
    - src/badge.js contains: This directive has expired. Verify successor at TSA.gov.
    - src/badge.js contains: expiredClasses
    - src/badge.js default export is unchanged (setComplianceRefs and renderBadge still exported)
    - All 173+ tests GREEN (167 original + Wave 0 new tests)
  </acceptance_criteria>
  <done>npm test exits 0; badge.js has expired branch; all badge-expired.test.js tests GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN static file → browser | compliance-refs.json is fetched from GitHub Pages CDN; content is public TSA/NIST reference data |
| compliance-refs.json → badge.js | badge.js reads directive.status field; must default to 'active' if field absent to avoid NIST regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | compliance-refs.json (static CDN file) | accept | File is public reference data; no credentials or user data. Content integrity protected by GitHub's CDN + HTTPS. |
| T-09-04 | Information Disclosure | badge.js HTML output | accept | Badge renders only shortName (TSA SD-02F) and [EXPIRED] label — both public information. esc() function prevents XSS from any future dynamic shortName values. |
| T-09-05 | Denial of Service | badge.js with null _complianceRefs | mitigate | Optional-chain (_complianceRefs?.directives?.[key]) ensures null refs returns directiveKey as fallback. Test: renderBadge before setComplianceRefs() is called must not throw. |
</threat_model>

<verification>
After both tasks:
- npm test exits 0 (all tests GREEN)
- grep "status.*expired" public/data/compliance-refs.json returns a match
- grep "color-badge-expired-bg" src/style.css returns a match
- grep "if (status === .expired.)" src/badge.js returns a match
- No SD-02G or any successor string present in compliance-refs.json
</verification>

<success_criteria>
Wave 1 complete when:
- TSA badge renders with gray strikethrough + [EXPIRED] in the running app
- NIST badge renders unchanged (blue-green, no strikethrough)
- npm test exits 0 with all badge-expired.test.js tests GREEN
- compliance-refs.json TSA entry has "status": "expired" and no successor version string
</success_criteria>

<output>
After completion, create .planning/phases/09-compliance-currency-content-depth/09-02-SUMMARY.md
</output>
