---
phase: 09-compliance-currency-content-depth
plan: 03
type: execute
wave: 2
depends_on:
  - "09-02"
files_modified:
  - public/data/modules/logging-auditing/quizzes/01.json
  - public/data/modules/logging-auditing/scenarios/01.json
  - src/views/compliance-index-view.js
  - CLAUDE.md
autonomous: true
requirements:
  - SME-01
must_haves:
  truths:
    - "No file in public/data/ contains the literal string 'TSA SD-02F' after this plan"
    - "compliance-index-view.js line 123 no longer contains 'TSA SD-02F'"
    - "CLAUDE.md shows marked.js v18 (installed: v18.0.3)"
  artifacts:
    - path: "public/data/modules/logging-auditing/quizzes/01.json"
      provides: "Quiz file with generic directive phrasing (no hardcoded version strings)"
    - path: "public/data/modules/logging-auditing/scenarios/01.json"
      provides: "Scenario file with generic directive phrasing"
    - path: "src/views/compliance-index-view.js"
      provides: "Compliance index view with generic TSA directive reference on line 123"
    - path: "CLAUDE.md"
      provides: "Updated stack docs showing marked.js v18"
  key_links:
    - from: "public/data/compliance-refs.json"
      to: "public/data/modules/logging-auditing/quizzes/01.json"
      via: "Human review — quiz content must not contradict the badge expired state"
      pattern: "current TSA pipeline security directive"
---

<objective>
Remediate all hardcoded 'TSA SD-02F' strings in content files that bypass compliance-refs.json,
and update CLAUDE.md with the correct marked.js version. This is the SME-01 hardcode audit
completion step — once done, compliance-refs.json is the sole source of truth for the TSA
version string.

Purpose: The expired badge makes the directive version visible to learners. Quiz questions and
scenario outcomes that still say "TSA SD-02F" would directly contradict the expired badge state.

Output: Zero 'TSA SD-02F' occurrences in any public/data/ content file; CLAUDE.md accurate.
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-PLAN-EXECUTE.md
@C:\Users\psyco\Claude CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\PROJECT.md
@C:\Users\psyco\Claude CLI\pipeline-cyber-training\.planning\phases\09-compliance-currency-content-depth\09-02-SUMMARY.md

Hardcode audit results from RESEARCH.md — confirmed locations:
- public/data/modules/logging-auditing/quizzes/01.json: q-03 stem + 4 answer feedback strings + explanation
- public/data/modules/logging-auditing/scenarios/01.json: phase-2 opt-a outcome + opt-b outcome
- src/views/compliance-index-view.js: line 123 static string
- CLAUDE.md line 19: "marked.js v17" (version wrong, not an SD-02F issue — housekeeping)
</context>

<interfaces>
From 09-PATTERNS.md — exact replacements:

logging-auditing/quizzes/01.json q-03 (stem + feedback×4 + explanation):
  BEFORE: "Under TSA SD-02F, ..."
  AFTER:  "Under the current TSA pipeline security directive, ..."

logging-auditing/scenarios/01.json phase-2 outcomes:
  BEFORE: "...TSA SD-02F incident notification requirements..."
  AFTER:  "...current TSA pipeline security directive incident notification requirements..."

src/views/compliance-index-view.js line 123:
  BEFORE: "Every TSA SD-02F and NIST SP 800-82 Rev 3 control..."
  AFTER:  "Every TSA pipeline security directive and NIST SP 800-82 Rev 3 control..."

CLAUDE.md line 19:
  BEFORE: **Markdown:** marked.js v17
  AFTER:  **Markdown:** marked.js v18 (installed: v18.0.3)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remediate hardcoded SD-02F in logging-auditing quiz and scenario JSON</name>
  <files>
    public/data/modules/logging-auditing/quizzes/01.json,
    public/data/modules/logging-auditing/scenarios/01.json
  </files>
  <read_first>
    - public/data/modules/logging-auditing/quizzes/01.json (full file — identify q-03 and all affected strings before editing)
    - public/data/modules/logging-auditing/scenarios/01.json (full file — identify phase-2 option outcomes before editing)
  </read_first>
  <action>
    QUIZZES/01.JSON — find question with "id": "q-03" and replace ALL occurrences of
    "TSA SD-02F" with "current TSA pipeline security directive" in these fields:
      - "stem" field of q-03
      - "feedback" field of each of the 4 answer objects in q-03
      - "explanation" field of q-03

    Do NOT modify any other questions (q-01, q-02) or any structural fields
    (id, type, complianceControls, correct, etc.). Validate JSON is still parseable after editing.

    SCENARIOS/01.JSON — find the phase with "id": "phase-2" and replace ALL occurrences of
    "TSA SD-02F" in the "outcome" fields of all options within that phase.
    Replace "TSA SD-02F" with "current TSA pipeline security directive".

    Do NOT modify phase ids, nextPhaseId values, option ids, or isFinal flags.
    Validate JSON is still parseable after editing.

    After both edits, run a grep verification:
      grep -r "SD-02F" public/data/modules/logging-auditing/
    This must return no output (zero matches).
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "SD-02F" public/data/modules/logging-auditing/ returns no output (zero matches)
    - quizzes/01.json is valid JSON (parseable without error)
    - scenarios/01.json is valid JSON (parseable without error)
    - quizzes/01.json q-03 stem contains "current TSA pipeline security directive"
    - quizzes/01.json q-03 contains exactly 4 answer objects (structure unchanged)
    - scenarios/01.json phase-2 opt-a outcome contains "current TSA pipeline security directive"
    - scenarios/01.json phase-2 opt-b outcome contains "current TSA pipeline security directive"
    - npm test exits 0 (no regressions)
  </acceptance_criteria>
  <done>Zero SD-02F occurrences in logging-auditing content JSON; JSON files still valid; tests GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Remediate hardcoded SD-02F in compliance-index-view.js and update CLAUDE.md</name>
  <files>src/views/compliance-index-view.js, CLAUDE.md</files>
  <read_first>
    - src/views/compliance-index-view.js lines 115-135 (read the static string on line 123 before editing)
    - CLAUDE.md lines 15-25 (read marked.js version line before editing)
  </read_first>
  <action>
    COMPLIANCE-INDEX-VIEW.JS: Locate the static string on line 123 that contains
    "Every TSA SD-02F and NIST SP 800-82 Rev 3 control". Replace "TSA SD-02F" with
    "TSA pipeline security directive" so the line reads:
    "Every TSA pipeline security directive and NIST SP 800-82 Rev 3 control..."

    The surrounding code context and template literal structure must remain intact — only
    the version string substring changes. Do not add dynamic renderBadge() calls here;
    the compliance-index-view.js static text approach is intentional per RESEARCH.md.

    CLAUDE.md: Find the line under "## Stack" that reads:
      **Markdown:** marked.js v17
    Replace it with:
      **Markdown:** marked.js v18 (installed: v18.0.3)

    Also, if the "## Open Decisions (resolve in Phase 5)" section still contains
    jQuery Terminal vs. custom vanilla JS questions and PS command parser questions
    (decisions resolved in v1.0), replace the entire section with:
      ## Resolved Decisions (v1.0)
      - Custom vanilla JS terminal (no jQuery Terminal)
      - Regex-based PS command parser; multi-line pipelines supported via continuation

    Only make these two targeted changes; do not edit any other CLAUDE.md content.
  </action>
  <verify>
    <automated>cd "C:\Users\psyco\Claude CLI\pipeline-cyber-training" && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "SD-02F" src/views/ returns no output (zero matches in any view file)
    - compliance-index-view.js line ~123 contains "TSA pipeline security directive" (not "TSA SD-02F")
    - CLAUDE.md contains "marked.js v18 (installed: v18.0.3)"
    - CLAUDE.md does not contain "marked.js v17"
    - npm test exits 0 (no regressions in compliance-index-view tests)
    - Final grep across entire codebase: grep -r "SD-02F" src/ public/data/ returns zero matches (only compliance-refs.json is allowed to contain it)
  </acceptance_criteria>
  <done>Zero SD-02F occurrences in src/ and public/data/ (compliance-refs.json remains the sole allowed location); CLAUDE.md accurate; tests GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content JSON files → quiz/scenario engine | Quiz and scenario engines parse JSON fetched from CDN; content strings are rendered as text (not innerHTML directly) |
| CLAUDE.md → developers | Documentation file; no runtime attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | quizzes/01.json content strings | accept | Public reference data; rendered as escaped text by quiz-engine.js. No injection risk. |
| T-09-07 | Information Disclosure | compliance-index-view.js static string | accept | Renders public compliance control labels. No PII or credentials. |
</threat_model>

<verification>
After both tasks, run final audit:
- grep -r "SD-02F" public/data/ src/ — must return zero results
  (compliance-refs.json is in public/data/ but acceptable — it IS the canonical source)
- Actually: grep -r "SD-02F" public/data/modules/ src/ — must return zero results
- npm test exits 0
</verification>

<success_criteria>
Wave 2 remediation complete when:
- grep -r "SD-02F" public/data/modules/ src/ returns zero matches
- CLAUDE.md shows marked.js v18 (installed: v18.0.3)
- npm test exits 0
- SME-01 hardcode audit is complete
</success_criteria>

<output>
After completion, create .planning/phases/09-compliance-currency-content-depth/09-03-SUMMARY.md
</output>
