// src/views/lesson-view.js
// Wave 0 stub — export present so test files load without import errors.
// Implementation ships in Wave 2 (Plan 02-03).

/**
 * Render the lesson view into #app.
 * @param {{ moduleId: string, lessonId: string }} params
 * @returns {Promise<void>}
 */
export async function renderLesson(params) {
  throw new Error('renderLesson: not yet implemented (Wave 2)')
}
