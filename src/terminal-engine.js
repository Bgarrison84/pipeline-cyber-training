// src/terminal-engine.js
// Wave 0 stub — RED state. Implementation arrives in Wave 1 (Plan 05-02).
// This file exists only so Vite import-analysis can resolve the path.
// Tests in tests/terminal-engine.test.js MUST fail against this stub.

// createTerminal is intentionally not implemented — returns undefined.
// All tests calling createTerminal(...) will throw TypeError (RED).
export function createTerminal(_container, _commandHandler) {
  throw new Error('createTerminal: not implemented — Wave 1 stub')
}
